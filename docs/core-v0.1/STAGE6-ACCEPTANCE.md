# Приёмка Stage 6 Core v0.1

Владелец 2026-09-26 сказал: «Принимаю Stage 6 на
`c8a546485e756afc2b0c5c99360e9ea2a42ae84d` в согласованном объёме,
с явно сохранёнными ограничениями. Это не приёмка всего ядра и не
разрешение миграции разработки».

Приёмка относится к четырём проходам `STAGE6-PLAN.md` на указанном
точном коммите, включая реализацию schema 7/8 в согласованном объёме.
Техническое закрытие последнего пакета записано отдельно в
`STAGE6-PASS4-PACKAGE3-REVIEW.md`; его независимые проверки не являются
собственными проверками этой записи. Предметные синтетические Work
принимаются только адресной операцией Core и не приняты этим решением.

Открытые `database is locked`, `rpc_transport` и известное диагностическое
расхождение `stale_basis` внутри `any` этим решением не исправлены.
Владелец отдельно разрешил один пакет их расследования и стабилизации
Core → DBOS → Pi с последующим общим независимым review. Реальные модели,
платный API, миграция разработки, PR и следующий предметный этап не
разрешены.

END_OF_FILE
