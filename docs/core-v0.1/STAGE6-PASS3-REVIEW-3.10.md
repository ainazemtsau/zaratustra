# Закрытие технического review части 3.10

Владелец 2026-09-25 закрыл **только техническое review части 3.10** на точном
коммите `4c9c8ece607bb58602c2573b9b23e8f60134470c` по независимому отчёту
Codex. Все предъявленные замечания части 3.10 закрыты в проверенном объёме;
новых подтверждённых замечаний отчёт не содержит. Это не приёмка schema 7,
прохода 3, Stage 6 или синтетического Work. Реализация следующего среза этим
закрытием не разрешена.

Независимые внешние проверки на чистом точном коммите подтвердили восстановление
адресованного `PENDING/local` workflow прежнего runner до Core launch claim:
сохранился workflow ID, новый runner завершил его с **2 HTTP**, точный replay
дал **0 дополнительных HTTP**. После записанного claim восстановление дало
`process_outcome_unknown`, оставило назначение `unknown` и не отправило HTTP;
повтор также дал **0 HTTP**, конкурирующее назначение получило `resource_busy`.
Отдельный сценарий двух прежних общих маршрутов подтвердил изоляцию соседей:
первый runner не забрал чужую работу, затем обе завершились с **2 HTTP**. Эти
сценарии использовали публичный Core, настоящие DBOS workflows и внешнюю fault
injection без фабрикации строк SQLite. Прежняя регрессия адресного контекста
Choice/Exception прошла в scoped gate; фактический HTTP-захват этого контекста
относится к предшествующему независимому review `8130b1b`, а не к новой проверке.

Независимый Windows scoped `tools.check --deliver --files
src/zaratustra/pi_adapter/assigned.py tests/zaratustra/pi_adapter` прошёл:
**35 тестов Pi adapter**, scoped format/Ruff/mypy, **21 импортный контракт**,
hygiene/report, sdist и wheel. Это не полный gate. Полный Windows
`tools.check --deliver` с **680 тестами** был выполнен в сеансе реализации
на финальном коммите; независимый reviewer его не повторял.

Независимый `tools.probe_install_stage6` проверил wheel из чистого точного
`4c9c8ec` вне checkout (`source_tree_dirty=false`): **3 синтетических HTTP**,
SHA-256 wheel
`2AE24862739BCC3B0262574FD15E0E3CBCBCDD395216C8F7C1987BAFAEDE297B`.
Это совместимый сценарий Stage 6, а не полная новая schema 7 трасса.
Сеанс реализации сообщил о schema 7 трассе с **4 HTTP** на этой ревизии;
предыдущее независимое подтверждение четырёх HTTP относится к `8130b1b`.

Источник: [независимый отчёт](<C:/Users/Anton/AppData/Local/Temp/zaratustra-stage6-3.10-final-review-4c9c8ec-1790350614290/READONLY-REVIEW.md>).
`database is locked` и `rpc_transport` остаются открытыми наблюдениями.
Полная матрица сбоев, реальные модели и миграция пользовательских данных
не проверялись. Следующий отдельный срез имеет только рабочий план
`STAGE6-PARENT-EXECUTION-PLAN.md`; реализации и приёмки нет.
