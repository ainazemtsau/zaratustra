# Core v0.1 Stage 6, проход 3 — контрольная точка 3.4–3.5: waiver по адресному исключению и перепроверка принятых результатов

## Основание и граница

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`d251604e020dfc2149685e678b9c98cc21c30563`:

- полный `tools.check --deliver` — PASS: 600 тестов, типы, Ruff, 21 импортный контракт,
  sdist и wheel;
- исходный независимый сценарий подтвердил исправление P2 (формальный конфликт внутри
  `any`);
- Pi-probe на этом коммите Codex повторно не запускал.

По этому результату владелец закрыл техническую проверку контрольной точки 3.3. Это не
приёмка schema 7, прохода 3 или Stage 6. Затем владелец разрешил один пакет — части 3.4
и 3.5 последовательно, по `STAGE6-PASS3-PLAN.md`:

- адресное исключение и waiver;
- перепроверка принятых результатов при изменении предпосылок.

В пакет входят все предусмотренные права, повторы, backup/restore и удаление. Остальной
состав ядра сохраняется. Промежуточной передачи между 3.4 и 3.5 нет: на общий review
передаётся совокупный результат после обеих частей и итоговой проверки. Существенные
решения вне утверждённого контракта за владельца не принимаются; технические
толкования перечислены в разделе «Толкования».

Известные вопросы остаются открытыми:

- расхождение `stale_basis` внутри `any` (`STAGE6-PASS3-CHECKPOINT-3.3.md`, «Пределы») не
  блокирует продолжение и не считается исправленным; прежняя семантика не менялась;
- наблюдение `database is locked` (контрольная точка 3.2) остаётся открытым для прохода 4.

После пакета работа остановлена для общего review. Приёмка не записывается. Часть 3.6,
реальные модельные вызовы, миграция разработки и PR не начинались. Спецификация
`Zaratustra_Core_Specification_v0.1.md` — та же, SHA-256
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.

Все данные синтетические. Реальной модели, миграции разработки и изменений Pi-адаптера
нет. Новые модули Core — `zaratustra.foundation.waivers` (3.4) и
`zaratustra.foundation.revalidation` (3.5). Schema 7 по-прежнему
`core-v0.1-plan-revision-7`; часть 3.5 добавила её первую таблицу `result_revalidations` с
индексом, SHA-256 набора DDL теперь
`92E5A98CB6E443B206318226B08F5746D0037123BEEE178FC55714150A57340B` (раньше — SHA-256
пустого набора `E3B0C442…B855`). По решению владельца 2 DDL schema 7 дополняется по
частям до выпуска прохода, а тестовые пространства контрольных точек одноразовые.
Пространство schema 7, записанное кодом 3.1–3.3, отказывает `unsupported_schema` при
открытии, а не мигрирует молча: это проверено fault injection записи миграции.

**Исправления по review Windows-проверки `c11d54f`.** Владелец передал результат
независимой Windows-проверки, которую выполнил Codex на
`c11d54f5394c358d4e835b9ac2deb9a0a6962e63`:

- полный `tools.check --deliver` — PASS: 616 тестов, типы, Ruff, 21 импортный контракт,
  sdist и wheel;
- установленный wheel вне checkout: Pi-probe — PASS, 3 localhost HTTP.

Review пакета при этом не закрыт. Владелец поручил исправить одним пакетом:

1. P1 — неполная санация `ResultRevalidation.basis`: `retire_revalidation_bases`
   учитывал только изменившиеся предпосылки и выходы. Переданное воспроизведение через
   публичный Core:
   - у A вход X@1 и готовность `artifact_current(Y@1)`, Y содержит уникальный маркер;
   - A выдан, его результат получен и явно принят; X → X@2;
   - `revalidate_result(A)` с парой X@1 → X@2 и основанием, цитирующим Y;
   - backup, удаление Y, `complete_deletions`.

   Маркер оставался в `read_revalidation` (и в новом процессе), в закрытой SQLite и в новом
   backup; повтор и `read_receipt` были доступны; очистка сообщала
   `live_store_sanitized=true`. Второй подтверждённый вариант — Y как общий вход родителя
   и основание плана.
2. Уточнение контракта: `premise_changed` сохраняет адресованный код через составные
   условия, пока остаётся блокирующей причиной. Истинная альтернатива `any` проходит,
   реально ожидающая — `dependency_open`, полностью закрытые альтернативы и `all` с
   закрытым членом — `dependency_closed`. Покрыть вложенные условия и сочетание с
   `decision_conflict`; семантику `stale_basis` не менять.

Оба варианта P1 воспроизведены здесь тем же порядком шагов скриптом вне репозитория на
`c11d54f` — ровно как передано. При проверке всего пакета найден дефект того же класса в
общем правиле 3.3–3.4: основание решения применимости или waiver, записанное до редакции
плана, переживало удаление Artifact, которое новая ревизия плана уже не называла. Он тоже
воспроизведён до исправления. Исправления — `39f6cde`; правила — в разделах «Удаление
зависимости роли», «Интеграция отказывает» и «Обслуживание данных» ниже.

**Закрытие технической проверки.** Владелец передал результат независимой
Windows-проверки, которую выполнил Codex на `0af70349bedec95ca39981b3312a8a81768ca964`:

- полный `tools.check --deliver` — PASS: 624 теста, типы, Ruff, 21 импортный контракт,
  sdist и wheel;
- установленный wheel вне checkout — PASS, 3 синтетических localhost HTTP;
- оба исходных сценария P1 и дополнительная проверка исторических оснований с
  последовательным удалением после перезапуска прошли;
- приоритеты `decision_conflict` / `premise_changed` в составных условиях владелец считает
  согласованными.

По этому результату владелец закрыл техническую проверку пакета 3.4–3.5. Это закрытие
технического review, а не приёмка schema 7, прохода 3 или Stage 6. Расхождение
`stale_basis` внутри `any` и наблюдение `database is locked` остаются открытыми. Затем
владелец разрешил единый пакет — части 3.6 и 3.7 последовательно:
`STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`. Строки «Пределов» ниже о части 3.6 описывают
состояние этой контрольной точки.

## Часть 3.4. Waiver по адресному исключению

### Исключение — вариант Decision

- `ExceptionState`: `variant: "exception"`, `statement` (случай и причина), цель `target`
  (`ObligationTarget`: `work_id` составного родителя и `key` обязательства), пределы
  `methods` (точные версии Method: id, версия, checksum; не меньше одной, каждая версия
  один раз) и `status` (`active`/`revoked`).
- Исключение создаётся, пересматривается и отзывается прежними `CreateDecisionRequest` и
  `ReviseDecisionRequest` по праву `decision.write`. Канонический вид правил и выборов не
  меняется: у правила поля `variant` нет.
- При создании целевой Work должен существовать и не быть удалённым (`not_found`,
  `content_unavailable`).
- Ревизия сохраняет вариант и цель (`invalid_request`); для другой цели создаётся другое
  исключение. Формулировка, пределы и статус меняются ревизией, отзыв — ревизия со
  статусом `revoked`.
- Исключение не является правилом доступа: `_authorize` учитывает только правила
  `require_grant`/`deny`, поэтому в `decision_refs` audit попадают только правила.
- Ниже schema 7 исключение отказывает `unsupported_schema`, ничего не записано.
- Точное чтение — прежний `read_decision(revision=…)`.

### Операция `waive_obligation`

`WaiveObligationRequest`: `work_id` родителя, `key`, `expected_plan_revision`,
`expected_obligation_revision`, точная ссылка `exception`, `basis`. Права — `work.accept`
на родителе (действие операции) и действующий `method.use` в Activity родителя, как у
подтверждения. Проверки в порядке исполнения; каждый отказ ничего не записывает:

| Проверка | Отказ |
|---|---|
| `work.accept` на родителе (общий gate операции) | `permission_denied`, `decision_denied` |
| schema 7 | `unsupported_schema` |
| родитель существует, не удалён, составной | `not_found`, `content_unavailable`, `wrong_work` |
| действующий `method.use` | `permission_denied`, `decision_denied` |
| ожидаемая ревизия плана | `stale_plan` |
| основание плана и входы родителя актуальны | `stale_basis` |
| ключ объявлен Method | `not_found` |
| ожидаемая ревизия обязательства; экземпляр `open` или его waiver устарел | `stale_obligation` |
| родитель не закрыт | `work_closed` |
| применимость: устаревшее решение, `unresolved`, `inactive`, формальный конфликт | `stale_basis`, `obligation_unresolved`, `obligation_inactive`, `decision_conflict` |
| точная ревизия Decision существует | `not_found` |
| это исключение, а не правило или выбор (Grant, даже широкий, его не заменяет) | `exception_required` |
| цель — ровно этот родитель и ключ | `exception_mismatch` |
| пределы содержат версию Method, закреплённую за родителем | `exception_mismatch` |
| ревизия исключения текущая и действует | `stale_basis` |

Результат — новая ревизия экземпляра `waived` с адресом исключения и текстом основания,
без доказательства. Применимость и адрес выбора (3.3) переносятся без изменений. Схема не
допускает `waived` без адреса исключения, адрес исключения при другом исполнении и
доказательство у `waived`; не-`active` экземпляр по-прежнему только `open`. Поле
`exception` отсутствует в JSON, пока пусто, поэтому прежние полезные нагрузки
побайтно прежние.

Квитанция: Work, ключ, ревизия, `status: waived`, адрес исключения; текста основания в
ней нет. Цели audit — родитель на ревизии плана и точная ревизия исключения. Точный повтор
возвращает прежнюю квитанцию; новая операция по тому же экземпляру — `stale_obligation`.

### `waived` не является результатом

- Waiver не открывает зависимости, которым нужен выход роли: листы `accepted_output` и
  `work_succeeded`, связи выходов и условие завершения вычисляются по детям, как прежде.
  Узел, ждущий выхода закрытой ветви, остаётся `dependency_closed`.
- Подтверждение уже снятого обязательства — `stale_obligation`.
- Когда исключение пересмотрено или отозвано, записанная ревизия не переписывается, но
  читается `waiver_stale`. Следующая ревизия может подтвердить обязательство точным
  результатом (исполнение `satisfied`, адрес исключения снят) или снять его заново по
  текущему исключению.

### Принятие родителя

Отказы принятия в порядке исполнения:

1. формальный конфликт условного обязательства — `decision_conflict` (3.3);
2. решение применимости, чей выбор уже не текущий или не действует, — `stale_basis` (3.3);
3. waiver, чьё исключение уже не текущее, не действует или не допускает закреплённую
   версию Method, — `stale_basis`;
4. активное невыполненное обязательство закрытой ветви — `dependency_closed` (3.2);
5. `obligation_unresolved`, затем `obligation_open`;
6. прежние проверки выходов, условия завершения и точных доказательств — только у
   `satisfied`: снятому и неактивному обязательству результат не нужен.

Принятый родитель показывает снятые требования и адреса исключений:

- в результате принятия — список `waived` (ключ и адрес исключения) только при
  непустом списке, поэтому прежние результаты принятия прежние; адреса исключений входят
  в цели audit;
- в `read_work` — `WorkAcceptance.waived`; пустой список отсутствует в каноническом JSON;
- в `CompositionView` — `ObligationProgress.status` `waived` с `exception`;
- в своём состоянии — `succeeded` с причинами `waived` (ключ, адрес исключения).

Core не называет такое исполнение безусловно соответствующим Method: снятие видно рядом с
принятием и не превращается в `satisfied`.

### Производное состояние родителя

- `ObligationProgress.status`: `open`, `satisfied`, `waived` или производное
  `waiver_stale`; `exception` — записанный адрес.
- `acceptance_pending` возможен, когда каждое объявленное обязательство выполнено,
  действующе снято или действующе неактивно; вместе с ним перечисляются причины `waived`.
- `waiver_stale` — следующий шаг (`ready`) с ключом и адресом устаревшего исключения;
  `confirmation_pending` — и для такого экземпляра, если ребёнок роли принят.
- У действующе снятого обязательства выход роли не входит в предпосылки родителя, и ради
  него закрытая ветвь не попадает в `branch_review`; ветвь текущего плана остаётся в
  `branch_review`, как в 3.2.

### Удаление зависимости роли: общее правило 3.3–3.4

Роль обязательства зависит от удаляемого ребёнка или Artifact по текущей ревизии плана
(действующий индекс санации прохода 1). В schema 7 ревизия обязательства зависит ещё и по
ревизии плана, действовавшей при её записи (исправление `39f6cde`). Решение применимости и
waiver могут быть записаны до первой выдачи, а план затем редактируется; их основание
остаётся связанным с той ревизией, даже если новая её уже не называет. Историческая
ревизия читается через сохранённый индекс. У подтверждения ревизия при записи всегда
совпадает с текущей: подтверждение идёт после выдачи, а после выдачи план не
редактируется. Поэтому правило прохода 1 для него прежнее.

При удалении такой зависимости:

- ревизии с основанием или доказательством заменяются маркером недоступности
  (`content_unavailable` при точном чтении), их квитанции выводятся из повтора
  (`history_unavailable`);
- последней становится ревизия `open` без основания, доказательства и адреса исключения:
  waiver снят и требует повторного адресного применения;
- применимость и адрес выбора сохраняются: сам Decision не удалён;
- независимые экземпляры сохраняют точную историю;
- затронутые пакеты backup загрязнены и удаляются `complete_deletions`, live SQLite
  сжимается. Маркеры оснований отсутствуют в live SQLite и в новом backup; новый процесс
  читает то же состояние.

Ревизии waiver — ревизии обязательств родителя: точное чтение, тот же SQLite-снимок при
backup, сохранение карантинным restore как истории, удаление вместе с родителем. Операции
удаления Decision нет со Stage 2: исключение остаётся обычной записью и только отзывается.

## Часть 3.5. Перепроверка принятых результатов (`premise_changed`)

### Предпосылки и изменение

- Предпосылки принятого результата ребёнка:
  - точные Artifact-входы его принятой ревизии, включая результаты предшественников,
    вставленные при выдаче;
  - точные листы `artifact_current`, `decision_active` и `decision_value` его готовности в
    той ревизии плана, где он был выдан.
- Предпосылка изменилась, если у Artifact есть более новая ревизия или он удалён, а
  Decision пересмотрен или отозван. `PremiseChange` называет вид, запись, удержанную
  ревизию и текущую; у недоступной предпосылки текущей ревизии нет.
- Предпосылки прямые: изменившаяся предпосылка предшественника не меняет предпосылок
  этого результата, пока его собственные входы и листы актуальны.
- Правило действует только в schema 7. В schema 5–6 перепроверить нечем, поэтому
  прежние пути там не меняются; выход — явный upgrade.

### Интеграция отказывает `premise_changed`

Результат остаётся реально выполненным действием: ребёнок остаётся `succeeded`, его
Artifact читается. Интеграция отказывает `premise_changed`; в отказе — роль и адрес
ребёнка и каждое изменение «`Artifact X@1 -> @2`» (или `-> unavailable`). Интеграция:

- выдача зависимых узлов и каждый эффект уже выданного зависимого (лист
  `accepted_output` или `work_succeeded` над ребёнком, gate прохода 2);
- подтверждение обязательства этим результатом;
- связь выхода родителя, привязанного к роли принятого ребёнка;
- принятие родителя: связанные выходы, условие завершения, доказательства выполненных
  обязательств.

Выполненные обязательства с таким доказательством молча не переоткрываются: экземпляр
остаётся `satisfied` с прежним доказательством, а принятие отказывает `premise_changed`
до перепроверки (или замены узла — 3.6).

Составные условия (уточнение владельца по review Windows-проверки Codex на `c11d54f`,
исправление `39f6cde`). `premise_changed`, как и формальный конфликт, снимается только
явным действием — перепроверкой, а не ходом работы. Поэтому он сохраняет код и адреса
через составные и вложенные условия, пока остаётся блокирующей причиной. Отказ несёт
адреса каждого такого члена:

| Условие без выполненного члена | Отказ операции |
|---|---|
| `all` с закрытым членом | `dependency_closed`, как прежде |
| `all` без закрытых, с конфликтом | `decision_conflict` (3.3) |
| `all` без закрытых и без конфликта, с изменившейся предпосылкой | `premise_changed`, даже если другой член открыт или `stale_basis` |
| `all` без всего этого | первый отказ, как прежде |
| `any` с реально ожидающим членом: не закрыт, не в конфликте, без изменившейся предпосылки; `stale_basis` сюда входит, как прежде | `dependency_open` |
| `any`, где остальные члены закрыты, с конфликтом | `decision_conflict` (3.3) |
| `any`, где остальные члены закрыты, с изменившейся предпосылкой и без конфликта | `premise_changed` |
| `any`, где закрыты все члены | `dependency_closed`, как прежде |

Выполненная альтернатива `any` по-прежнему выполняет условие. При сочетании с
формальным конфликтом первым называется конфликт — как в `all` по правилу 3.3; после
отзыва конфликтующего выбора называется `premise_changed`, после перепроверки условие
проходит. Семантика `stale_basis` не менялась: в `any` это по-прежнему член, который ещё
может стать истинным (известное расхождение с производным состоянием остаётся открытым),
в `all` — первый отказ, если нет закрытого члена, конфликта или изменившейся
предпосылки. Производное состояние уже называло эти причины и не менялось.

### Операция `revalidate_result`

`RevalidateResultRequest`: `parent_work_id`, `role`, `work_id` ребёнка, `outputs` — точные
связанные выходы принятой ревизии ребёнка, `expected_plan_revision`, `premises` — пары
«удержанная ревизия → текущая ревизия», `basis`. Каждая предпосылка и выход названы один
раз, у каждой пары есть текущая ревизия, иначе ошибка проверки схемы. Права —
`work.accept` на родителе и действующий `method.use`. Проверки в порядке исполнения;
каждый отказ ничего не записывает:

| Проверка | Отказ |
|---|---|
| `work.accept` на родителе (общий gate операции) | `permission_denied`, `decision_denied` |
| schema 7 | `unsupported_schema` |
| родитель существует, не удалён, составной | `not_found`, `content_unavailable`, `wrong_work` |
| действующий `method.use` | `permission_denied`, `decision_denied` |
| ожидаемая ревизия плана | `stale_plan` |
| основание плана и входы родителя актуальны | `stale_basis` |
| родитель не закрыт | `work_closed` |
| Work заполняет эту роль в текущем плане | `wrong_work` |
| `record.read` на ребёнке | `permission_denied` |
| ребёнок принят | `dependency_open`, для закрытого — `dependency_closed` |
| выходы — ровно связанные выходы принятой ревизии | `output_mismatch` |
| выходы актуальны (право чтения, тип, байты) | `stale_basis`, `content_unavailable` |
| названные удержанные ревизии — ровно изменившиеся предпосылки | `premise_mismatch` с перечнем изменений |
| изменившаяся предпосылка доступна | `content_unavailable` |
| названная текущая ревизия — действительно текущая | `stale_basis` |
| новая ревизия Artifact актуальна (право чтения, байты); Decision — действует | `stale_basis` |

Результат — запись `ResultRevalidation` с ревизией n+1 для пары «родитель, ребёнок»:
адреса роли, ребёнка, ревизии плана, выходов и пар, текст основания. Квитанция содержит
родителя, роль, ребёнка, ревизию и пары, но не текст основания. Цели audit — ребёнок на
его ревизии, родитель на ревизии плана, выходы и текущие ревизии предпосылок. Точный
повтор возвращает прежнюю квитанцию. Новая перепроверка того же состояния допустима и
даёт следующую ревизию записи.

Запись допускает интеграцию, пока выполнены все условия:

- это последняя запись ребёнка;
- её пары совпадают ровно с текущими изменениями;
- её выходы — выходы принятой ревизии;
- её основание не удалено.

Следующее изменение снова даёт `premise_changed`; новая перепроверка называет
удержанную ревизию исходной предпосылки и новую текущую, например `X@1 → X@3`.

Точное чтение — `read_revalidation(path, parent_work_id, work_id, authority,
revision=None)`: по праву `record.read` на родителе, только schema 7. Удалённый родитель —
`content_unavailable`, отсутствующая запись — `not_found`, перехода к latest нет.

### Производное состояние

- Родитель — `blocked` с причинами `premise_changed`: роль, адрес ребёнка и `premise`
  (вид, запись, удержанная и текущая ревизии), по одной на изменение.
- Зависимый ребёнок с листом над таким результатом — `blocked` с теми же причинами, выдан
  он или нет.
- Принятый ребёнок остаётся `succeeded`. Пока родитель открыт, его причины называют
  каждое изменение; после принятия или закрытия родителя изменения — история, а не
  ожидающая перепроверка.
- `ChildProgress.revalidation` в `CompositionView` — ревизия записи, которая сейчас
  допускает интеграцию.
- Непринятые выданные и невыданные дети со своей изменившейся предпосылкой остаются
  `blocked:stale_basis`, как в проходе 2; выход — `stale` (3.1) или редакция плана (3.6).
- Новые поля `StatusReason.premise` и `ChildProgress.revalidation` отсутствуют в JSON,
  пока пусты.

### Обслуживание данных

- Таблица schema 7 `result_revalidations` (родитель, ребёнок, ревизия, роль, полезная
  нагрузка, операция, время) содержит адреса и основание. Точное чтение — выше. Таблица
  входит в тот же SQLite-снимок при backup; карантинный restore сохраняет записи как
  историю.
- Основание перепроверки может цитировать всё, на чём стоит перепроверяемый результат,
  изменилось оно или нет. Поэтому оно снимается при удалении любой известной структурной
  зависимости, а не только названной пары (исправление `39f6cde` по P1). Это те же
  зависимости, что у основания исхода ребёнка в 3.1, вместе с собственными адресами
  записи:
  - сам ребёнок и его выходы;
  - каждый его вход и лист готовности, в том числе неизменившийся;
  - входы родителя и история его плана с основанием и листами условия завершения —
    через сохранённый индекс, когда план санирован;
  - вышестоящие дети и их результаты.

  Зависимые записи находятся до любых изменений удаляющей транзакции:
  - основание записи удаляется (`basis = null`), адреса и audit остаются;
  - квитанция выводится из повтора (`history_unavailable`, `read_receipt` —
    `not_found`);
  - пакеты backup с родителем загрязнены и удаляются `complete_deletions`;
  - запись больше ничего не допускает; интеграция возвращается к `premise_changed`
    (недоступная предпосылка) или `content_unavailable` (удалены выход, ребёнок или
    план, санированный действующим индексом);
  - исходы и независимые записи сохраняются.

  Цитата без сохранённой структурной связи не покрыта, как и у оснований исходов.
- Удаление родителя после детей удаляет все его записи и выводит их квитанции из
  повтора. Маркеры оснований отсутствуют в live SQLite и в новом backup.
- Текст основания не попадает в закрепления, outbox, вход DBOS workflow и снимок
  исполнения: там только адреса и ревизии.

## Толкования

Контракт плана назван так, что следующие технические детали требовали выбора. Решения
приняты в пределах контракта и не меняют его:

- «Пределы (например, эта версия Method)» — множество точных версий Method (id, версия,
  checksum). Waiver требует, чтобы в нём была версия, закреплённая за родителем. Список,
  а не одна версия, оставляет 3.8 место для исключения, допускающего v1 и v2.
- «Точный выход» перепроверки — все связанные выходы принятой ревизии ребёнка:
  предпосылки общие для всего результата, а лист `work_succeeded` интегрирует его целиком.
- Пары перепроверки всегда называют удержанную ревизию исходной предпосылки и её текущую
  ревизию. Поэтому каждая запись самодостаточна.
- Отказ «предпосылка изменилась» до перепроверки для отзыва Decision называет текущую,
  отозванную ревизию; перепроверить на неё нельзя (`stale_basis`).
- Коды `exception_required`, `exception_mismatch`, `premise_mismatch` и `wrong_work`
  названы в плане. `stale_basis` для недействующего исключения и для нетекущей ревизии в
  перепроверке и `output_mismatch` для неточных выходов — прежние коды Core в том же
  значении.
- Уточнение владельца требует покрыть сочетание `premise_changed` с `decision_conflict`,
  но порядок не задаёт. Здесь первым называется конфликт, как в `all` по правилу 3.3; оба
  адреса остаются в производном состоянии. В `all` изменившаяся предпосылка, как и
  конфликт, называется раньше отказа первого члена, в том числе `stale_basis`; условие
  только со `stale_basis` называет его, как прежде.
- Структурные зависимости основания перепроверки — те же, что у основания исхода ребёнка
  (3.1), вместе с адресами самой записи. Основание решения применимости или waiver связано
  с ревизией плана, действовавшей при его записи, и с текущей.

## Изменения принятого поведения

- Изменение 1 плана (3.5): в schema 7 принятый результат ребёнка с изменившейся
  предпосылкой не интегрируется без перепроверки. Для этого один прежний тест
  контрольной точки 3.2 получил одну дополнительную ожидаемую причину. В
  `test_failed_branch_is_reviewed_while_independent_branches_go_on` после ревизии общего
  источника родитель, кроме `stale_basis(source)` и `branch_review(a2)`, называет
  `premise_changed(a1)`: A1 принят на этом источнике, и его подтверждённый результат
  больше не интегрируется. Других изменений тестов нет.
- Подтверждение принимает экземпляр `waived`, чьё исключение уже не действует
  (`waiver_stale`). Для прежних состояний (`open`, `satisfied`) правило прежнее.
- `_authorize` и чтение Decision различают три варианта. Для правил и выборов поведение
  прежнее.
- Исправление `39f6cde` меняет отказ составного условия только при `premise_changed` —
  коде этого пакета. Для условий без него, в том числе со `stale_basis` и формальным
  конфликтом, результат прежний: тесты 3.3 проходят без изменений.
- Санация обязательств в schema 7 учитывает ревизию плана при записи. Для подтверждений
  (проходы 1–2) она всегда совпадает с текущей, поэтому их санация прежняя.

Для данных, которые могли существовать до 3.4, поведение прежнее. Тесты проходов 1–2 и
частей 3.1 и 3.3 не менялись и проходят. Закреплённые константы прохода 2 (fingerprint
запросов и checksum Method) совпадают.

## Воспроизведение

Тесты без модели на schema 7 (schema 6 — только в проверках отказа).

`tests/zaratustra/foundation/test_waivers.py`, 9 тестов (8 — `b6b66f1`, 1 — исправление
`39f6cde`). Method 3.3: `checked` (роль A) и `final` (роль B) безусловные, `art_review`
(роль `art`) условный. План: A, B и `art` независимы, выход родителя связан с B, условие
завершения — `B succeeded`.

- `test_exact_exception_waives_a_failed_branch_and_acceptance_names_it`
  - A закрыт `failed`; принятие отказывает `dependency_closed` с адресом A.
  - Waiver `checked` по исключению: квитанция с адресом без текста основания; повтор —
    та же квитанция; новая операция и подтверждение — `stale_obligation`.
  - Узел D после выхода A остаётся `dependency_closed`: waiver не результат.
  - Точное чтение ревизий 1 (`open`) и 2 (`waived`, адрес, основание); у `final` в
    хранимых байтах нет `exception`. `CompositionView` показывает `waived` с адресом.
  - Родитель `ready`: `acceptance_pending`, `waived(checked)`, `branch_review(a)`; новый
    процесс читает то же.
  - Результат принятия содержит `waived`, `read_work` — `WorkAcceptance.waived`, состояние
    — `succeeded` с `waived`; у принятия B в хранимых байтах нет `waived`.
- `test_waiver_refusals_name_the_reason_and_write_nothing`:
  - `exception_required` — ссылка на правило доступа при широком Grant владельца и на
    выбор;
  - `exception_mismatch` — исключение для другого ключа, другого Work, другой версии
    Method;
  - `stale_basis` — отозванное исключение и его прежняя ревизия;
  - `not_found` — ревизия и ключ; `stale_obligation`; `stale_plan`;
  - `obligation_unresolved` и `obligation_inactive` — отказ с состоянием экземпляра;
  - без `method.use` или без `work.accept` — `permission_denied`;
  - после всего waiver проходит. Каждый отказ сохраняет ревизию пространства.
- `test_revoked_exception_reads_waiver_stale_until_confirmed` — A принят, `checked` снят,
  родитель `acceptance_pending` с `waived`. Отзыв исключения: экземпляр хранится `waived`,
  читается `waiver_stale`, родитель `ready` с `confirmation_pending(a)` и `waiver_stale`.
  Принятие и waiver по отозванной ревизии — `stale_basis`. Подтверждение точным
  результатом A даёт ревизию 3 `satisfied` без исключения; родитель принят без `waived`.
- `test_revised_or_revoked_exception_needs_a_new_waiver` — после пересмотра исключения
  `waiver_stale` и `stale_basis`; waiver по текущей ревизии 2 снова действует. После
  отзыва — новое исключение и waiver (ревизия 4); результат принятия называет его адрес.
- `test_waiver_recomputes_applicable_choices_in_its_transaction` — `art_review` решён
  `active` по выбору Work. Выбор Activity с другим значением: waiver — `decision_conflict`
  с обоими адресами. После отзыва противоположного выбора и пересмотра указанного —
  `stale_basis`. После нового решения waiver проходит, применимость и адрес выбора
  сохранены.
- `test_exception_is_never_an_access_rule_and_keeps_its_target`
  - Исключение не меняет `decision_refs` audit и чтение с `record.read`.
  - Цель, которой нет, — `not_found`; без `decision.write` — `permission_denied`.
  - Смена цели, ревизия исключения в правило и правила в исключение — `invalid_request`.
  - Ревизия формулировки и пределов проходит, ревизия 1 читается точно.
  - Пустые и повторяющиеся пределы и неверный ключ — ошибки проверки схемы.
- `test_schema_6_refuses_exceptions_and_waivers` — исключение, ревизия правила в
  исключение и waiver отказывают `unsupported_schema` без записей.
- `test_waiver_history_survives_restore_and_deletion_takes_the_waiver_off`
  - `art_review` решён `active` и снят, `checked` снят. Backup → карантинный restore →
    recover: ревизия `waived` с адресом и основанием, исключение и состояние совпадают.
  - Удаление A: ревизия 3 `checked` — `open` без исключения и основания, ревизия 2 —
    `content_unavailable`, повтор waiver — `history_unavailable`. История `final` точно
    прежняя, старый пакет удалён, маркера нет в live SQLite, новый процесс читает то же.
  - Удаление `art`: ревизия 4 `art_review` — `active`, прежний адрес выбора, `open`, без
    исключения; маркер удалён.
  - Удаление B и родителя: обязательства недоступны, маркеров нет ни в live SQLite, ни в
    новом backup; исключение остаётся записью Decision.
- `test_bases_recorded_under_an_earlier_plan_revision_go_with_its_dependency` (дефект того
  же класса, найденный при проверке пакета):
  - готовность `art` в ревизии плана 1 — `artifact_current(Y)`, Y содержит маркер;
  - `art_review` решён `active` и снят, оба основания цитируют Y;
  - редакция плана до выдачи убирает Y из готовности `art`; Y удалён.

  Результат: ревизия 4 `art_review` — `open` без исключения и основания, с прежними
  применимостью и адресом выбора. Ревизии 2 и 3 — `content_unavailable`, повтор waiver —
  `history_unavailable`, `read_receipt` обеих операций — `not_found`. Независимые
  обязательства прежние, старый пакет удалён, маркера нет ни в live SQLite, ни в новом
  backup, новый процесс читает то же.

`tests/zaratustra/foundation/test_revalidation.py`, 15 тестов с вариантами (8 — `b6b66f1`,
7 — исправление `39f6cde`).
Method: `checked` (роль A) и `final` (роль B). Выходы родителя: `checked` ← A,
`final` ← B. План: A на своём входе X@1; B после принятого выхода A; C после успеха A;
условие завершения — `all(B succeeded, C succeeded)`.

- `test_changed_premise_refuses_integration_until_an_exact_recheck`
  - A принят на X@1, затем X → X@2. A `succeeded`, Artifact A читается.
  - Выдача B (`accepted_output`), выдача C (`work_succeeded`), подтверждение результатом
    A и связь выхода родителя отказывают `premise_changed` с адресом A и `X@1 -> @2`.
  - Родитель, B и C — `blocked` ровно с этой причиной; A — `succeeded` с ней же; новый
    процесс читает то же.
  - Отказы перепроверки: `premise_mismatch` (постороннее, лишнее, неверная удержанная
    ревизия), `stale_basis` (`X@1 -> @3` при текущей 2), `wrong_work` в обе стороны,
    `dependency_open` (непринятый B), `output_mismatch`, `stale_plan`.
  - Правильная перепроверка: квитанция без основания, повтор — та же, audit называет
    X@2, запись читается точно. A без причин, `revalidation = 1`.
  - B выдан и получил тот же точный результат A, подтверждение и связь выхода проходят.
  - X → X@3: выдача C и связь снова `premise_changed` (`X@1 -> @3`), B — `blocked`.
    Перепроверка 2 (`X@1 → X@3`) восстанавливает путь.
- `test_acceptance_refuses_premise_changed_and_keeps_the_satisfied_obligation` — всё
  выполнено, `acceptance_pending`. X → X@2: принятие — `premise_changed`, `checked`
  остаётся `satisfied` с прежним доказательством, родитель `blocked`. Результат B на
  неизменном выходе A — без причин. После перепроверки родитель принят. Изменение после
  принятия не даёт причин A.
- `test_result_accepted_on_c7_integrates_only_after_a_recheck` — пример W22: A принят на
  C@7, затем C@8. Выдача B — `premise_changed` (`C@7 -> @8`), после перепроверки B выдан с
  точным результатом A.
- `test_changed_decision_premise_is_rechecked_only_to_an_active_revision` — готовность A —
  `decision_value(tone@1, formal)`. Пересмотр tone: `premise_changed` вида `decision`,
  перепроверка на tone@2, C выдан. Отзыв tone@3: снова `premise_changed`, а перепроверка
  на отозванную ревизию — `stale_basis`.
- `test_recheck_refusals_rights_and_schema` — без `method.use` или без `work.accept` —
  `permission_denied` без записей. Закрытый родитель — `work_closed`. Schema 6 — операция
  и чтение `unsupported_schema`. Неверные формы запроса и пары без изменения — ошибки
  проверки схемы.
- `test_recheck_history_survives_restore_and_deletion_removes_its_basis[premise|output|child]`
  - Backup → restore → recover: запись и состояние B совпадают.
  - Удаление X, выхода A или детей A и B:
    - основание записи `null`, адреса прежние, повтор — `history_unavailable`;
    - старый пакет удалён, маркера нет в live SQLite;
    - подтверждение результатом A отказывает `premise_changed` или `content_unavailable`;
    - новый процесс читает то же.
  - Удаление родителя после детей: чтение записи — `content_unavailable`, маркера нет ни в
    live SQLite, ни в новом backup.
- `test_recheck_basis_goes_with_any_premise_it_quotes[readiness|plan_basis|unchanged_input]`
  — P1. Основание перепроверки (X@1 → X@2) цитирует неизменившуюся предпосылку Y:
  - `readiness` — готовность A, как в переданном сценарии;
  - `plan_basis` — общий вход родителя и основание плана, второй подтверждённый вариант;
  - `unchanged_input` — второй вход A.

  Backup, удаление Y, `complete_deletions`: очистка честно сообщает
  `live_store_sanitized`, старый пакет удалён. У записи `basis = null`, в том числе в
  новом процессе; адреса пар и выходов прежние. Повтор — `history_unavailable`,
  `read_receipt` — `not_found`, audit с адресами читается. Маркера нет в закрытой SQLite и
  в новом backup, A по-прежнему `succeeded`.
- `test_recheck_of_a_downstream_result_goes_with_its_upstream[upstream_artifact|upstream_child]`
  — B принят на результате A. Новая ревизия результата A меняет предпосылку B, B
  перепроверен с основанием, цитирующим готовность A. Удаление Artifact готовности A или
  самого A снимает основание B тем же образом; B остаётся `succeeded`.
- `test_recheck_bases_follow_sequential_deletions_after_restart` — A и B независимы, у
  каждого свои вход и Artifact готовности, оба перепроверены.
  - Удаление Artifact готовности A снимает основание A. Запись B сохраняет основание,
    повтор и квитанцию, маркер B есть в новом backup.
  - Новый процесс удаляет Artifact готовности B. План уже санирован, запись находится
    через сохранённый индекс, основание B снято, пакет с маркером B удалён.
  - Исходы A и B прежние, маркеров нет в новом backup.
- `test_premise_changed_keeps_its_code_through_composite_conditions` — уточнение
  владельца. Узлы на листах над A с изменившейся предпосылкой, отменённым `dropped`,
  открытым B, листом `decision_value` в формальном конфликте и устаревшим Z@1:
  - `any(A, dropped)`, `all(открытый B, A)`, `all(any(A, dropped), выполненный лист)`,
    `any(all(A, выполненный лист), dropped)`, `all(устаревший Z, A)` — `premise_changed` с
    адресом;
  - `any(A, открытый B)` — `dependency_open`; `all(dropped, A)` — `dependency_closed`;
    `any(A, выполненный лист)` выдаётся;
  - `any(конфликт, A)` и `all(A, конфликт)` — `decision_conflict` с обоими адресами;
  - `stale_basis`: `any(устаревший Z, dropped)` и `any(устаревший Z, A)` —
    `dependency_open`, `all(устаревший Z, открытый B)` — `stale_basis`, как прежде.

  Производное состояние (`read_work_status`, `read_execution`, новый процесс) называет
  все причины с адресами. Отказы ничего не записывают. После отзыва конфликтующего
  выбора `any(конфликт, A)` выдаётся, а `all(A, конфликт)` — `premise_changed`. После
  перепроверки выдаются все узлы, кроме реально ожидающего, закрытого и устаревших.

Проверка чувствительности тестов. Временные поломки, после каждой исходники
восстановлены побайтно:

| Поломка | Результат |
|---|---|
| принятие не перепроверяет waiver | падает тест |
| принятый выход не перепроверяет предпосылки | падает тест |
| удаление зависимости сохраняет waiver | падает тест |
| перепроверка принимает любой набор предпосылок | падает тест |
| связь выхода родителя не проверяет привязанный результат | падает тест |
| действие записи не сверяет выходы | проходит |
| зависимости перепроверки — только адреса записи | падают тесты P1 |
| зависимости перепроверки без вышестоящих детей | падает `upstream_child` |
| `premise_changed` не блокирующий | падает тест составных условий |
| `premise_changed` раньше конфликта | падает тест составных условий |
| `all` снова берёт первый отказ | падают тесты составных условий 3.5 и 3.3 |
| санация обязательств без ревизии плана при записи | падает тест истории плана |
| ревизия плана при записи заменена текущей | падает тест истории плана |

Поломка «действие записи не сверяет выходы» эквивалентна при нынешних операциях: выходы
принятого Work не меняются, а при записи совпадение уже проверено. Проверка оставлена как
явное утверждение инварианта.

Все 8 новых тестов исправления падают на снимке `c11d54f` (`git archive` с новыми файлами
тестов), 16 прежних тестов обоих модулей там проходят.

## Проверки в Linux-контейнере

Сессия работала в облачном Linux-контейнере, а не на Windows. Эти прогоны — обратная
связь, а не доказательство выпуска. Обязательный полный gate — независимый
Windows-прогон, результат которого передаёт владелец.

Код и тесты пакета — `b6b66f1`, исправление по review `c11d54f` — `39f6cde`. Проверки ниже
шли на `39f6cde`: `tools.check --deliver` и probe — на чистом дереве. Итоговый коммит с
документами меняет только Markdown-файлы, среди них `src/zaratustra/foundation/AGENTS.md`,
который входит в wheel. Поэтому SHA-256 wheel итогового коммита отличается от
проверенного; Python-код и тесты совпадают. Те же проверки на `b6b66f1` описаны в записи
`RESULT.md` о пакете.

- Воспроизведение P1 одним скриптом вне репозитория через публичный Core, оба варианта:
  на `c11d54f` — ровно как передано; на `39f6cde` маркера нет в `read_revalidation`, новом
  процессе, закрытой SQLite и новом backup, повтор — `history_unavailable`,
  `read_receipt` — `not_found`. Так же воспроизведены до и после исправления дефект
  истории плана и составные условия.
- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3 с FTS5): 623 прошли, 1 упал,
  149,96 с. Это 616 тестов `c11d54f` и 8 новых. Единственное падение — ожидаемое на Linux
  `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет (179 файлов).
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver`:
  - hygiene, структура отчёта, Ruff format (205 файлов) и Ruff lint прошли;
  - затем остановка на mypy: 35 ошибок `attr-defined` в 11 файлах, все про атрибуты,
    существующие только на Windows; пакет и исправление эти файлы не меняли;
  - шаг сборки выполнен отдельно: `uv build --no-sources` собрал sdist и wheel.
- Существующие probe без изменений, обычный Pi 0.87.0 RPC с синтетическим
  localhost-провайдером — `passed`:
  - `tools.probe_stage6_rpc` (checkout) — 3 HTTP; удаление в нём делает upgrade до
    schema 7 с новой таблицей;
  - `tools.probe_install_stage6` — wheel SHA-256
    `144634AAF24D93A9183A1EB10D686D51FF996DCC62CDA582C4D206145F3AD2A4` в новом venv вне
    checkout, `source_commit` `39f6cde`, `source_tree_dirty: false`, 3 HTTP;
  - `tools.probe_stage5_rpc` — 2 вызова провайдера, отказ до отправки без HTTP, restore в
    эпоху 2, санация при удалении.
- Отчёты сверены с прогонами на `b6b66f1` с маскировкой id, дайджестов, времени и
  маркеров. Stage 5 совпадает полностью. Stage 6 из checkout и из wheel отличается
  только выборочным переходным состоянием `running:stop_requested`: наблюдатель фиксирует
  его не в каждом прогоне.
- Отчёты — `_scratch/stage6-pass3-345fix-*`, прежние — `_scratch/stage6-pass3-345-*`
  (игнорируются git).
- Скриптом вне репозитория проверен отказ открытия пространства schema 7 с прежней
  записью DDL.

Команды для Windows-проверки — те же, что в `STAGE6-PASS2-IMPLEMENTATION.md`, с новыми
каталогами вывода:

```powershell
$env:UV_CACHE_DIR = Join-Path (Get-Location) '_scratch\stage5-plan-uv-cache'
$env:ZARATUSTRA_SQLITE_DLL = Join-Path (Get-Location) '_scratch\stage4-interactive-20260923-a\sqlite\sqlite3.dll'
$env:PYTHONPATH = Join-Path (Get-Location) 'tools\sqlite_bootstrap'
uv run --locked python -m tools.check --deliver
uv run --locked python -m tools.probe_stage6_rpc --output _scratch\stage6-pass3-345fix-rpc --pi-runtime '_scratch\stage4-pi-runtime'
uv run --locked python -m tools.probe_install_stage6 --output _scratch\stage6-pass3-345fix-installed --pi-runtime '_scratch\stage4-pi-runtime' --sqlite-dll $env:ZARATUSTRA_SQLITE_DLL
```

## Пределы

- Pi-адаптер не менялся. `/zara-status` печатает новые причины как код и роль
  (`premise_changed:a`, `waived`, `waiver_stale`) без ключа, адресов и ревизий, а
  обязательства — ключ и исполнение (в том числе `waived`, `waiver_stale`) без адреса
  исключения. Waiver с адресом исключения, `premise_changed` и перепроверки в Pi — часть
  3.10.
- Замена узла (3.6) — второй выход из `premise_changed` — не реализована. Сейчас выход —
  перепроверка, а для непринятых зависимых ещё `stale` или `cancelled` (3.1).
- Известное расхождение `stale_basis` внутри `any` (операция — `dependency_open`,
  производное состояние — `blocked:stale_basis`) остаётся открытым и не менялось.
  `premise_changed` его больше не разделяет: по уточнению владельца он сохраняет код.
- Основания перепроверки, решения применимости и waiver снимаются по известным
  структурным связям. Цитата без сохранённой связи не покрыта, как и у оснований
  исходов 3.1.
- Операции удаления Decision нет: исключение и выбор только отзываются. Их формулировки
  остаются в записи Decision после удаления целевого Work.
- Исключения касаются только обязательств Work; общая семантика исключений правил доступа
  (`deny`) — вне прохода 3.
- Предпосылки результата прямые. Переходной перепроверки через цепочку предшественников
  нет.
- Не начаты части 3.6–3.10, отдельный срез исполнения родителя и обязательства в том же
  Work, проход 4.

## Ограничения Linux-проверок

Gates не ослаблены: `tools/check.py`, `validation.config` и `REVIEW.md` не менялись. DDL и
checksum schema 1–6 не менялись; DDL schema 7 дополнен по решению владельца 2 до выпуска
прохода.

- `tools.check --deliver` на Linux останавливается на mypy по платформенной причине (см.
  выше). Остальные шаги выполнены отдельно, `mypy --platform win32` чист.
- `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process` на Linux падает: тест
  ожидает неподдерживаемый встроенный SQLite, а conda-forge Python уже содержит точный
  3.53.3.
- Хук `.githooks/pre-commit` вызывает `#!/usr/bin/env python`. В контейнере это системный
  Python 3.11, поэтому коммиты делались с Python 3.13.7 первым в `PATH`.
- Pi 0.87.0 установлен из npm в игнорируемый `_scratch/stage6-pass3-pi-runtime`. Это
  окружение, а не продукт.
