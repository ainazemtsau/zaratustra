# Preserve exact captured bytes, including native log whitespace and CRLF.
*.txt -text -diff
*.diff -text -diff
*.json -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
*.zip binary
