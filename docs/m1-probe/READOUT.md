# Разные правила работают через неизменённое ядро — ранняя проба

Проверены два вымышленных правила на отдельных тестовых workspace.
Оба используют настоящие контекст Work, Mutation API, Result и продолжение M0.

| Процесс и состояние | Вход | Наблюдаемый результат |
|---|---|---|
| Проверка партии | Наблюдение есть, записи нет | Отказ; DB и журнал не изменились |
| Проверка партии | Есть обе отметки | Result и новая Work; revision 9→10 |
| Цикл, фаза наблюдения | Наблюдение есть, записи нет | Result и Work фазы записи; revision 6→7 |
| Цикл, фаза записи | Тот же прежний вход | Отказ; DB и журнал не изменились |
| Цикл, фаза записи | Запись есть, наблюдения нет | Result и Work фазы наблюдения; revision 13→14 |

Решение о продолжении принимает правило вне Core. При подключении второго
Core, первое правило и общий обработчик не менялись. Каждый из трёх успешных
переходов воспроизведён из сохранённого состояния через штатный API.
Полная проверка репозитория: 148 тестов, сборка, типы, форматирование и четыре
ограничения импортов — PASS; [полный вывод](evidence/check-deliver.txt).

Вывод ограничен этим контрастом: разные правила исполнимы на основании M0.
Полный контракт пакетов, семь возможностей, совместное размещение процессов
и общий обзор ещё не сделаны. M0 остаётся partial; M1/T1 не закрыты.
Личная приёмка владельца и отдельная свежая проверка доказательств pending.

Доказательства: [машинный результат](evidence/contrast-summary.json),
[изменения второго правила](evidence/second-connection.diff),
[полный отчёт](../../RESULT.md), [план и открытые вопросы](PLAN.md).
Повтор: `uv run --locked python -m tools.probe_m1 --output _scratch/owner-probe-01`
в execution worktree; имя выходной папки должно быть новым.

END_OF_FILE: docs/m1-probe/READOUT.md
