# T6 — исправление пятнадцати возвратов свежих G5

Свежая G5 по source 87db23ae19355e6730c46782535f0db267fee10f:
отчёт docs/g5/m1-overview-20260910/G5-REPORT.md, commit
9d46e2e8b3af2924d19c17835c0e5647eedd8ff6, branch codex/g5-m1-overview-20260910.
Verdict рецензента: deliver FAIL; done_when 1 и 3 выполнены; done_when 2 не
выполнено; неизменность Core и обоих правил PASS, восемь идентификаторов
baseline совпали. Все возвраты принимаются полностью, ни одна проверка не
ослабляется, ни один вывод рецензента не оспаривается. Ниже семь разборов по
кругам: два пункта первого круга, три после повторной проверки и два после
третьей. Восемь возвратов четвёртого круга разобраны последним разделом.

## G5-T6-01 — deliver FAIL на порядке импортов

`ruff check` отвергал tools/probe_overview.py, строки 26–27: два отдельных
импорта из `.probe_process_host`, где алиасная строка стояла после обычной.
Алиас `confirm as host_confirm` был не нужен: в probe_overview нет своего
`confirm`. Строки заменены одним `from .probe_process_host import confirm,
footprint`, вызовы переименованы обратно в `confirm`. Ветвей и поведения
изменение не касается.

Обратная связь рецензента о прочих инструментах — mypy чист, 11 контрактов
целы, 250 tests, сборка идёт — относится к прежнему source и не является
пройденным gate. Полный `--deliver` обязателен заново.

## G5-T6-02 — свободный текст пакета подделывал строки обзора

Дефект настоящий. `overview_lines` возвращал элементы, а probe склеивал их
переносом строки; `current_status` приходит от пакета свободным текстом, в
котором перенос разрешён. Двухстрочный статус давал в человекочитаемом виде
лишнюю строку, читаемую как настоящая строка обзора: рецензент воспроизвёл
несуществующую третью строку, чужие счётчики и фразу «одна общая транзакция»,
прямо отрицающую заголовок. Безобидный статус вида «Этап A готов / Этап B
ждёт владельца» даёт то же самое; злого умысла не требуется.

Исправление в `overview_lines`: добавлен `_flat`, через который проходит
каждое прочитанное из документа значение — статус, состояния, counts, титул
строки, process, revision и поля envelope. Непечатаемые символы заменяются
пробелом, пробельные последовательности схлопываются. Инвариант: один
возвращённый элемент — ровно одна печатаемая строка, поэтому текст пакета не
может добавить строку, счётчик или заявление envelope. Отступы и постоянные
подписи остаются нашими и не приходят из документа.

Границы исправления: JSON-документ не меняется. Он остаётся дословным
вложением ответа T3 вместе с точным значением статуса, включая переносы, и
байтовое равенство строки собственному `read_capabilities` сохраняется. Это
важно: рецензент отдельно подтвердил, что сам документ честен. Правится
только человекочитаемый вид. Нормализация означает, что вид не воспроизводит
форматирование пакета дословно; авторитетный источник — документ.

Новая проверка `test_pack_text_cannot_forge_a_row_a_count_or_an_envelope_claim`
и воспроизводимый контроль `overview-forged-status` в tools.probe_overview
используют один и тот же `NOISY` статус: перенос строки, поддельная третья
строка, поддельные counts, противоречащее заявление и ESC-последовательность.
Проверяется, что каждый элемент вида остаётся одной строкой, что строк обзора
по-прежнему две, что строки, начинающейся с «3.», нет, что подделка свёрнута
внутрь одной строки статуса и что ESC не доходит до вывода, а документ хранит
исходное значение побайтно.

## Что не менялось

Core, оба fictional пакета и четыре прежних модуля process_packs остаются
байтово равными basis; ожидаемые идентификаторы — docs/m1-overview/
baseline-ids.json. Публичный контракт T3, W15/W16, права, scope, revisions и
отдельно подтверждаемый selected context не затронуты. Ни новых поверхностей,
ни установленного загрузчика пакетов, ни изменения гарантий P§30 и P§40.
Автор снова ничего не запускал, кроме commit hygiene: полный `--deliver`,
сценарий и повторная G5 назначены свежему Claude Code.

## Вторая G5 и три оставшихся возврата

Повторная G5 по source fix 4dad08e: addendum
docs/g5/m1-overview-recheck-20260910/G5-ADDENDUM.md, commit
31e6a47056ebc2056327910265bfbd1afe31111a, branch
codex/g5-m1-overview-recheck-20260910. G5-T6-02 закрыт: собственный probe
рецензента прогнал двадцать случаев подделки статуса, включая CR, CRLF, VT, FF,
U+0085, U+2028, U+2029, ESC, безобидный двухстрочный статус, границы усечения,
статус из девяноста девяти строк и синтетический документ с подделкой во всех
читаемых видом полях. Во всех двадцати каждый элемент остался одной печатаемой
строкой, поддельной строки обзора не возникло, ESC до вида не дошёл, обе
workspace остались побайтно неизменными. Прежний рендерер на том же документе
давал двенадцать строк с поддельной строкой в колонке ноль, нынешний — десять.

Отмеченное рецензентом расхождение одного случая из девятнадцати — краевой
перенос строки, снимаемый прежним ограничением `Text` контракта T3. Внутренние
байты сохраняются, вид не затронут. Это поведение T3, не регрессия T6, и здесь
оно не исправляется.

Первый отчёт вернул пять пунктов; исправлены были два. Три остальных
исправляются здесь.

### G5-T6-03 — ожидание вместо прочитанной revision

У отказанной строки текстовый вид печатал `revision=` из эха собственного
запроса потребителя, хотя строка ничего не прочитала: на прогоне 18 при
фактической 19. Состояние не выдумывалось, но подпись вводила в заблуждение.
Теперь строка без envelope печатает `requested_revision=`, а строка с envelope
берёт `state_revision` из самого ответа. У успешной строки Core требует
равенства этих чисел, поэтому её вид не меняется.

### G5-T6-04 — коды отказа не были видны

Проекция хранила только состояние и счётчик, поэтому conflict и missing_pack
в человекочитаемом виде не различались, а REPRODUCE.md обещал их наблюдать.
Проекция теперь зеркалит и `code`, который уже принадлежит тому же ответу, а
вид печатает его как `state:code`. Новых сведений это не раскрывает: словарь
кодов задают Core и capabilities, свободного текста пакета в нём нет, и `_flat`
применяется по-прежнему. Успешные и законно пустые ответы кода не имеют, их
вид не меняется.

### G5-T6-05 — учёт baseline

В `changed_by_design` добавлены корневой AGENTS.md и tools/AGENTS.md. Восемь
записанных идентификаторов неизменности не менялись, заявление не ослаблено.

### Что снова не менялось

Core, оба fictional пакета и четыре прежних модуля process_packs остаются
байтово равными basis. Публичный контракт T3, вложенный ответ строки и её
байтовое равенство собственному `read_capabilities` не затронуты: изменилась
только производная проекция и текстовый вид. Гарантии P§30 и P§40 не ослаблены.
Автор снова ничего не запускал, кроме commit hygiene.

## Третья G5 и два возврата о точности документов

Третья G5 по source 456776a: addendum
docs/g5/m1-overview-third-20260910/G5-ADDENDUM.md, branch
codex/g5-m1-overview-third-20260910, коммиты 969ba40, 969895d, 000c4a8.
Обязательный полный `tools.check --deliver` назван явно и пройден: ruff format
и ruff check на 83 файлах чисто, mypy 73 файла без ошибок, import-linter
11 contracts kept и 0 broken, pytest 252 passed, build и структура отчёта PASS,
exit 0. G5-T6-03, G5-T6-04 и G5-T6-05 закрыты и проверены отдельно; причина
изолирована прямым сравнением прежнего и нынешнего рендерера на одном
документе. Подделка статуса не вернулась. Строка обзора байтово равна
собственному `read_capabilities` на четырнадцати строках семи сценариев и на
девятнадцати случаях подделки.

Оба оставшихся возврата касаются точности документов, ни один — кода.

### G5-T6-06 — неверное изложение прошлого evidence

RESULT.md и CLAUDE-RECHECK-2.md утверждали, что второй отчёт не назвал
результат обязательного полного deliver на pin 4dad08e. Утверждение неверно.
Отчёт назвал его отдельным разделом «Verdict — полный обязательный deliver
gate: PASS», exit 0, 251 passed, 11 contracts kept, с двумя committed raw в
коммите 31e6a47. Ошибка автора: утверждение написано по краткой передаче в
чате, а не по самому отчёту, и попало в два документа как факт о нём.

Оба документа исправлены с явной пометкой, прежний текст не переписан молча.
Требование прогнать gate заново на новом pin остаётся верным по другой
причине: pin другой, а не потому, что прошлый verdict отсутствовал.

### G5-T6-07 — неполный учёт изменённых путей

Отчёт назвал две вещи: `changed_by_design` перечислял не все изменённые от
basis пути, RESULT.md среди них не был назван, а описание правки
`__init__.py` говорило «gains export lines only», хотя переписана и строка
docstring. Замена записи `docs/m1-overview/`, обозначавшей каталог, на
отдельные файлы — собственное решение автора, а не требование отчёта.

baseline-ids.json переписан: каждый изменённый путь назван отдельно и разделён
на продуктовый источник, development-only и документы; правка `__init__.py`
описана как одна переписанная строка docstring, одна добавленная строка импорта
и четыре записи `__all__`. Восемь идентификаторов неизменности не менялись.

### Что снова не менялось

Правки этого круга — только документы. Байты src, tools и tests равны 456776a,
поэтому пройденный на нём полный gate и вердикты третьей G5 по коду остаются в
силе. Core, оба fictional пакета и четыре прежних модуля process_packs
байтово равны basis. Гарантии P§30 и P§40 не ослаблены. Автор снова ничего не
запускал, кроме commit hygiene.

## Четвёртая G5 и восемь неточностей документов

Четвёртая G5 по source f7bf592: addendum
docs/g5/m1-overview-docs-20260910/G5-ADDENDUM.md, branch
codex/g5-m1-overview-docs-20260910, коммиты c933abe и 11cc05a. Блокирующих
находок нет. Полный обязательный deliver PASS: exit 0, повторов 0 из 3, ruff
format и ruff check на 83 файлах чисто, mypy 73 файла без ошибок, lint-imports
11 kept и 0 broken, pytest 252 passed, build и структура отчёта PASS. Байты src,
tools и tests подтверждены идентичными 456776a, три дерева совпали по id.
G5-T6-06 и G5-T6-07 закрыты: семнадцать объявленных путей равны семнадцати
фактическим, записей-каталогов нет, правка `__init__.py` описана буквально,
восемь идентификаторов неизменности совпали и на basis, и на HEAD.

Возвращены восемь неточностей документов, ни одна о коде. Все исправлены.

| # | Место | Что было неверно | Правка |
|---|---|---|---|
| 08 | module AGENTS | «state/count projection only», хотя зеркалится и code | описание названо по факту |
| 09 | PLAN контракт документа | то же расхождение | описание названо по факту |
| 10 | PLAN размещение | «получает лишь строки экспорта» | названа и строка docstring |
| 11 | CLAUDE-RECHECK-2 и REPRODUCE | «прежние числа относятся к неполному gate»: для 251 gate был полным | оговорка убрана |
| 12 | RESULT manual-acceptance | «три круга свежей проверки» о самом 456776a | приведено к одному кругу |
| 13 | FIX-G5 разбор G5-T6-07 | требование про запись-каталог приписано отчёту | отнесено к решению автора |
| 14 | FIX-G5 карта разделов | обещала пять разборов при семи | карта приведена к факту |
| 15 | CLAUDE-RECHECK-2 пометка | «текст сохранён, не переписан» при удалённых четырёх строках | сказано, что заменены заголовок и три строки |

Правки PLAN объявлены в нём отдельным разделом: он написан до реализации и по
существу не переписан, исправлены ровно две описательные строки.

### Что снова не менялось

Ни один файл Python. Изменены документы и одна строка описания в
`src/zaratustra/process_packs/AGENTS.md`. Байты всех `.py` под src, tools и
tests равны 456776a и f7bf592, где полный gate пройден. Core, оба fictional
пакета и четыре прежних модуля process_packs байтово равны basis. Гарантии
P§30 и P§40 не ослаблены. Автор снова ничего не запускал, кроме commit hygiene.

END_OF_FILE: docs/m1-overview/FIX-G5-20260910.md
