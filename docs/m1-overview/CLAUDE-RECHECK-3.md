# Свежий Claude Code — короткая сверка после двух возвратов о документах

Владелец поручил реализацию автору, проверки Claude Code. Правки уже
закоммичены. Проведи проверку сам, без запроса владельцу собирать команды.
Это новый handoff; CLAUDE-CHECK.md относится к source 87db23ae,
CLAUDE-RECHECK.md — к 4dad08e, CLAUDE-RECHECK-2.md — к 456776a.

Проверенный код: 456776a1e346e28952a6da7813a629e4a079a2ba. На нём третья G5
назвала обязательный полный `tools.check --deliver` пройденным, exit 0,
252 passed, 11 contracts kept, и закрыла три возврата второго круга.
Текущий handoff pin: см. сообщение владельца; это прямой потомок 456776a,
в котором изменены только RESULT.md и docs/m1-overview/.
PLAN/baseline 05398cd894be9f0a74730d95bafea7d2961d4f39;
basis c4b8aea4db38fea4c39a473c47d0d8b82d8552a0.
Авторская worktree C:/projects/zaratustra/_scratch/m1-overview-20260910,
branch codex/m1-overview-20260910; общий Git repo C:/projects/zaratustra.
Три прошлые G5: 9d46e2e (deliver FAIL, пять возвратов),
31e6a47 (deliver PASS, подделка закрыта, три возврата),
969ba40/969895d/000c4a8 в branch codex/g5-m1-overview-third-20260910
(deliver PASS, три возврата закрыты, два возврата о документах).
Автор снова ничего не запускал, кроме commit hygiene. PASS не заявлен.

Начни в свежей физической reviewer-сессии, отдельной от авторской. Прочитай
AGENTS.md, src/zaratustra/process_packs/AGENTS.md, tools/AGENTS.md,
validation.config (PROBA36), STOP/STEER, а рядом CALL.md, PLAN.md,
FIX-G5-20260910.md, REPRODUCE.md, baseline-ids.json и корневой RESULT.md.
Создай NEW reviewer worktree, например
C:/projects/zaratustra/_scratch/g5-m1-overview-docs-20260910,
branch codex/g5-m1-overview-docs-20260910, от handoff pin. При занятом имени
выбери новый суффикс: не очищай и не переключай чужие worktree и ветки.
Прежние три отчёта не перезаписывать.

## Почему круг короткий

Изменены только документы. Байты src, tools и tests обязаны совпадать с
456776a: проверь это первым делом через `git diff --stat 456776a HEAD` и
через сверку деревьев. Если хоть один из них отличается, круг перестаёт быть
коротким: тогда выполняй полную программу CLAUDE-RECHECK-2.md и сообщи drift.

Полный `tools.check --deliver` всё равно обязателен, потому что RESULT.md
входит в проверку структуры отчёта. Выполни его целиком на новом pin и назови
результат явно. Точные команды — REPRODUCE.md рядом; замени имя scratch на
новое, например `_scratch/t6-docs-01`. Сценарий и поведенческие проверки
переиспользуй с 456776a после подтверждения, что источник не менялся; повторять
их не обязательно, но своя дополнительная проверка допустима с явным статусом.

## Проверить ровно оставшееся

1. G5-T6-06. RESULT.md и docs/m1-overview/CLAUDE-RECHECK-2.md больше не
   утверждают, что второй отчёт не назвал результат обязательного deliver.
   Оба несут явную пометку об ошибке автора, прежний текст не переписан молча,
   а обоснование повторного прогона теперь опирается на смену pin. Сверь это с
   реальным разделом второго отчёта в коммите 31e6a47.
2. G5-T6-07. docs/m1-overview/baseline-ids.json перечисляет каждый изменённый
   от basis путь отдельно, без записей, обозначающих каталог. Сверь список с
   `git diff --name-only c4b8aea HEAD`: множества должны совпасть точно, и
   заявленное число путей должно совпасть с фактическим. Проверь описание
   правки `__init__.py`: одна переписанная строка docstring, одна добавленная
   строка импорта, четыре записи `__all__` и ничего больше.
3. Восемь идентификаторов неизменности не изменились; Core и оба fictional
   пакета байтово равны basis.
4. FIX-G5-20260910.md излагает все семь возвратов так, как они были даны, и не
   присваивает автору чужих выводов и не смягчает своих ошибок.

Не редактируй продукт, авторские tests или контракт. Разрешены свои probes и
retained evidence/docs в reviewer-ветке. При FAIL верни точный вывод автору,
не исправляй вместо него. Перечисли все возвраты явно и пронумеруй.
Не читать и не искать Work8, original attachment, archive/**. Не работать с
реальными данными, не запускать T7, не делать product remote/push/merge,
не менять Direction live/**. Установка владельца этой проверке недоступна.

## Возврат

Сохрани и локально закоммить `docs/g5/m1-overview-docs-20260910/G5-ADDENDUM.md`
и полный raw. Назови exact source/reviewer commits и свежую session identity.
Отдельные verdict: (а) полный deliver с явным PASS или FAIL, (б) неизменность
байтов src/tools/tests относительно 456776a, (в) два возврата о документах.
Укажи, что переиспользовано с 456776a, что выполнено заново и что NOT RUN.

Владельцу коротко: результат, ошибки если есть, полный путь к отчёту и commit.
Owner acceptance T6, Direction close, T7 и M1 этим отчётом не объявляются:
их решает владелец и HOME.

END_OF_FILE: docs/m1-overview/CLAUDE-RECHECK-3.md
