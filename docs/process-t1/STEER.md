# HOME engineering feedback — 2026-09-13

This is parent-session technical feedback on the active T1 CALL, not new owner
words, owner approval, a scope change, or a binding fresh review. The current
CALL remains authoritative. Continue autonomously within T1.

A read-only parent pre-pass reproduced a history-continuity defect in the initial
adapter: after `request_done` produces the next Core Work requirements, replacing
the earlier request result's data in a structurally valid full snapshot, appending
the research result, and passing that snapshot to `rule.next_work` succeeds.
The next Work was derived from the original accepted bytes, but its requirements
currently pin only definition/node/occurrence, so those replaced grounds escape
validation. This conflicts with T1 exact data/grounds and the accepted preservation
scope. Evaluate the finding independently and fix the corresponding integrity
class within existing Core APIs; add a behavioral regression. No Core semantic
change is authorized. Include the disposition in READOUT/RESULT, then continue
the planned native checks, probes and technical handback. The parent retains its
scratch reproducer; this note supplies the full minimal behavior without requiring
access outside the product repository.

Windows environment: the already-prepared managed Python launched successfully
with a narrowly scoped `require_escalated` tool call. A process creation refusal
is environment evidence, not a semantic failing test.

Retain this note as reviewed engineering evidence; do not treat it as owner
manual acceptance. No need to ask for the word 'continue'.

END_OF_FILE: STEER.md
