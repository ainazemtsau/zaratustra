# Process creation T2 correction plan

Basis: returning CALL `c-solmax-zaratustra-process-t2-r1-20260913`, contract 36
PROBA, product candidate `c9a8d6ee68371f6f68e6971a8d51677125d70f06`.
The original `PLAN.md`, `READOUT.md`, failed fresh-review evidence, and both HOME
steering notes remain unchanged. No root STOP or STEER is present.

## F1 — bind the bytes that are shown

The pre-fix isolated reproduction under `_scratch/process-t2-correction` proves the
reported class: ASCII retained/shown preview digests match and authorize, while one
valid case with Unicode designation, research and proposal retains a different
digest and refuses with `permission_denied: Changed activation preview`.

Change only activation-preview digest construction to SHA-256 of the same readable
UTF-8 representation returned by `activation_preview_bytes`. Retention,
authorization and fresh revalidation will therefore bind one canonical preview byte
sequence. Keep `_wire` unchanged for journals, requests, definitions and existing
source digests; require no migration and preserve already saved pending drafts.
Retain all digest, authorization, stale/tamper, rights and recovery checks.

## Regression and delivery

Extend the nearest behavioral regression so both an ASCII control and a combined
Unicode designation/research/proposal case confirm the displayed digest, authorize,
execute all six Core operations, and open the first Work through common entry. Pin a
pre-fix canonical request/source digest to demonstrate saved-source compatibility.
Keep the existing adversarial and recovery suite intact.

Use patch version 0.14.1, then run meaningful focused tests, the required native
`tools.check` and `tools.check --deliver`, and an isolated corrected-wheel proof.
The installed harness may consume external fictional fixtures only after proving all
product module origins are wheel `site-packages` and checkout `src` cannot satisfy
product imports. Preserve synthetic local-chat/research labels. Commit the bounded
repair and its `FIX-READOUT.md`/root `RESULT.md`; manual acceptance stays pending and
HOME must open a new fresh physical review. T3 safe evolution and T4 full physical
reader work remain out of scope.
