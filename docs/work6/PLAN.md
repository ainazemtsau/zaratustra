# Work 6 — current bounded Work context

call: c-solmax-zaratustra-m0-context-20260908-work6
mode: PROBA; engineering_contract: 36
base: 589861c4657f19763b0af4ab965bd84646eecb70 (0.5.0)
actual_checkout: C:/projects/zaratustra/_scratch/work6-context
branch: codex/work6-context
status: technical HOW recorded before dependent implementation

## Authority and size

Compared the full CALL, fresh Direction context (no owner inputs pending), Product
AGENTS/validation.config, Work 5 PLAN/INSTALL and binding F1 aggregate PASS,
Work 3 exact owner words, architecture §§3–7/10/25/28–29/41–43 (SHA-256
4f90d19fc742ab616301b6b4121880f76c75faaeb1f606c8190a84853d8b3e0e),
converge-arch W22/W23/W27 and CONTRACTS C02/C04–C07/E1–E12. Conforming HOW under
owner-ack:solmax-plan-conforming-20260907; this is not new owner runtime acceptance.
Both no-automation owner acknowledgments remain. Setup evaluator is a bounded
read-only in-session smoke, not a feature reviewer or binding fresh G5.

One read seam, one query schema, one deterministic compiler, CLI, hidden tests,
installed evidence and retained trial. No domain schema/migration or new dependency.
Estimated within half a focused working day; no new deadline, spending or scope cut.
Only NEW selected copies of accepted fictional Work 5 main workspace are used.
Old worktrees, refs, DBs, wheels, receipts and G5 evidence remain intact.

## W19 / W20 — open is a read, existing effects stay exact

open_work does not start an autonomous executor, change Work status, append events,
register output, repair files, or issue a receipt. Reopening a currently ready Work
is a fresh read, not a second domain effect. Only ready with current metadata rights
can open; draft/revoked/cancelled refuse. A future completed status is not admitted
by today's Work schema and cannot open. Work 7 owns completion and next linkage.
All managed changes still use apply_mutation in literal §5 order: input -> current
Work/authority -> revision -> duplicate -> artifact references -> DB/event/receipt
-> projections. Released migrations and old request serialization remain unchanged.

Registered hashes do not prove availability. Every included content version is
read and verified at assembly and again at final validation. Missing/corrupt bytes
refuse the entire package. Work 4 file-before-DB/orphans/exact repair/rebuild and
Work 5 v2 restore stay unchanged. Output is immutable returned bytes, not operational
state; the caller may retain stdout outside workspace. No automatic repair/retry.

## W21 / W22 — envelope, dependencies and freshness

ContextQuery v1 binds workspace, Work, Process, expected global revision, max_bytes
and zero or more exact additionally requested artifact references. The trusted
local console displays the exact query and current Work; the existing in-process
local-chat seam requires actual prior permission. No serialized approval field or
file/model self-authorization. No new identity/account/same-user isolation gate.
Current ready Work metadata authority permits reading its own metadata and exact
Artifact refs, as in Work 5 import; the separately confirmed query bounds this read.

The deterministic mandatory set is current Process, entire Work (including authority,
acceptance, boundaries, budget and executor requirements), its Artifact metadata,
ALL committed accepted Handoffs for this Work with provenance/confirmation/receipt,
the active Artifact content and all result/basis/additional exact content refs.
All accepted Handoffs are retained: no latest-wins/supersession semantics are invented.
Their source revisions are historical; current Work/global revision governs opening.
Publication-event references are followed transitively with per-edge scope/hash
validation and deduplication. Each content's publication request/confirmation/receipt
is retained as provenance. At least one accepted Handoff is required for this
post-Work-5 consumer. No optional selection or silent removal of mandatory sources.

Allowed namespaces are this workspace/Process/Work and its declared Artifact only.
An exact reference must match all membership ids and registered version/hash before
reading bytes. Text/URI/path/projection/Handoff owner text is inert content: no link
fetching, filesystem discovery or recursive interpretation. No memory, skills or
external tools are admitted; their manifest lists are explicitly empty.

First collect one validated DB snapshot plus verified content. Compile outside its
transaction. Then acquire existing BEGIN IMMEDIATE, recheck caller/current rights,
expected revision, full validated snapshot/journal/membership/acceptances/descriptors
and every included byte; compare the complete collected value before returning.
Any managed change during assembly conflicts (revocation/terminal refuses authority
first). The final lock serializes managed writers through validation; BUSY follows
the existing five-second refusal, no retries. Currentness is at final validation,
not a perpetual lease through a later chat. A subsequent mutation must still pass
expected_revision; opening from an old manifest/Next Call requires a fresh query.

Global revision conservatively invalidates on ANY domain mutation, including rights,
requirements, publication/active switch, exact repair and accepted decisions. Process
membership is fixed in this single graph and checked through snapshot/journal and
each descriptor; future membership changes must bump revision through Mutation API.
No unversioned dependency is silently accepted. Physical file loss can invalidate
without DB revision and is separately checked. Snapshot, schema version and complete
source data are compared as well as revision. No newly invented Process API.

## W22 / W23 / W24 — exact wire output and budget

Product 0.6.0, DB schema 5 unchanged, explicit default migrate target still 4.
CLI: zara work open <work-id> --workspace <path> --process <id>
--workspace-id <id> --expected-revision <n> --max-bytes <n>, with optional
--reference <exact-reference-json>. No inferred budget; query maximum 1 MiB.
This technical context budget is an additional exact read limit, not conversion
of the Work's preserved human effort budget or authority to extend it.

The full output is canonical sorted compact ASCII-escaped JSON encoded UTF-8 with
one LF. Artifact content is exact base64; descriptors carry original byte hashes.
The context object contains envelope and actual ordered sources. Manifest lists
each actual source locator, revision, canonical value hash/size, content hash/size
when applicable, dependency revisions and context-object hash. Manifest accounts
for the ENTIRE emitted JSON including itself and trailing LF, using a deterministic
size fixed point. Caller max_bytes is hard: overflow reports required/allowed,
returns no partial stdout, and never silently raises the limit.

ContextPackage exposes immutable output bytes and their SHA-256 (external, avoiding
self-hash recursion). CLI writes these exact bytes once; approval prompts go to
stderr. Save output.bin/json and external SHA-256 together. Future Work 8 must verify
exact delivered input and separately save actual clean-session response on five
facts; current manifest/automated checks do not establish understanding or cleanliness.

## W25 — fictional fixtures and evidence

New copies of accepted Work 5 main graph (including tests). Positive repeated read,
source/manifest reconstruction, hash and full byte-budget boundary; original stale
query, changed decision/requirements/rights/active Artifact, terminal Work; changed
dependency during assembly, BUSY, missing/corrupt bytes including during assembly;
foreign workspace/Process/Work/ref/hash and inert forbidden link to a minimal
sentinel file. No second complete Process. Mutations use public Core; filesystem
damage occurs only in labeled negative copies, never by repairing operational DB.
Native full build/hygiene/types/boundaries/tests and --deliver are required.
Installed wheel runs from unrelated cwd with -I; source/wheel/installed hashes,
raw commands/stdout/stderr/refusals, exact query/output, preservation and new trial
archive with ALL files AND directories are retained. Restore is exercised anew.

## Carried W19–W27 — answerer, moment and rewrites

| ID | Decision here / remaining answerer and moment | rewrites |
|---|---|---|
| W19 | Product PLAN before Work 6 reads: read-only open; old acceptance/replay/stale/collision unchanged. Product PLAN before Work 7: Result/next effect unit. | importer/receipts/linkage/failures/migrations; cheap reversal unproved |
| W20 | Product PLAN before content reads: exact bytes twice, no repair, retain restore v2. Product PLAN before Work 7: continuation/recovery. | durable refs/recovery/evidence/migrations |
| W21 | Product PLAN before reads: exact trusted query/current rights and nontransitive authority above; accepted Work 3 owner words reused. Product PLAN before Work 7: Result consumer. | adapters/permissions/consumer boundaries; decision is not runtime PASS |
| W22 | Product PLAN before Work 6: complete revalidation, global invalidation, membership, namespaces, mandatory closure, byte overflow and actual manifest above. Later dependent PLAN owns extensions. | snapshot/context/tests/possible migrations; no freshness/scope/budget cut |
| W23 | Product PLAN before Work 6: exact output bytes/hash plus manual delivery protocol. Product PLAN/owner at Work 8: actual clean session/five facts. | delivery/demo/repeat Work 8; unprovable cleanliness is blocker |
| W24 | Product PLAN before implementation: query/wire v1, 0.6.0/schema5, no migration, fixed-point bytes, existing BUSY/layout. Later PLAN per admitted Work. | mechanics/tests/migrations; semantic change returns W19–W22 |
| W25 | Product PLAN before fixtures: new copies of one fictional graph and minimal foreign sentinel above. Product PLAN/owner before Work 8 actual demo. | fixtures/tests/demo; no second full or real Process |
| W26 | Product PLAN before start: actual isolated checkout/ref above and separate local installed trial. Owner before permanent location/hosting/independent external install-upgrade. | package/layout/docs; no new external rights |
| W27 | Product PLAN before public consumer: only versioned M0 ContextQuery/open_work/ContextPackage seam. Future consumer PLAN at its own admission. E1–E12 remain in Direction CONTRACTS; E4/E5/E7/E12 get no invented direct M0 APIs. | public Core/data/consumer migrations; M1+ not prerequisite; cheap replacement unproved |

Product REPORT/ESCALATE returns HOME solmax only. No Direction writes, T5/CALL/M0
close, successor Direction CALL, Work 7–8 or owner runtime PASS. Behavioral T5
closure needs binding fresh physical G5 of the exact delivered version.

END_OF_FILE: docs/work6/PLAN.md
