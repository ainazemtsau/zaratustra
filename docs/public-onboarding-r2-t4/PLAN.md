# Public onboarding R2 T4 — pinned installation and standard connections

call: c-solmax-zaratustra-public-onboarding-r2-t4-correction-20260914
original-call: c-solmax-zaratustra-public-onboarding-r2-t4-20260914
mode: PROBA; engineering_contract: 36
basis: 335e4dadeeabef5552052d0d1982d40e4adcc788
correction-basis: c4a0576c56be5427cffdf1c9a6ec300da5b16995
original-implementation: 06d95080bd9d51bbe0dffa88036acffad26e5ed6
branch: codex/public-onboarding-r2-t4-correction

## Exact HOW

W18: README starts in a clean folder and fetches only the full implementation
commit named in this step's separate RESULT report. It verifies HEAD, uses locked
non-editable installation and an absolute executable. The report supplies that
immutable pin because an implementation cannot embed its own Git commit hash.
The README at the report tree and the implementation pin must travel together.
Public availability requires dated evidence for that exact pin in the report.
Durable instructions make no assertion about a moving public branch. This
correction leg does not publish; HOME must obtain exact-SHA owner authorization.
No local build, cached source, or offline installation is called a public download.

W19: add common installed `zara entry ready` presentation. It reports the running
program version and absolute Python/package paths, explicitly supplied catalog and
designation, exact connection-file comparison, and the existing T3 onboarding read.
No default catalog, workspace, last-used selection or Boolean ready file is added.
With no designation, current Work is unknown, never absent. For a selected Process,
the same exact ProcessStateQuery confirmation and public Core read as T3 resume
remain required. Current/no-current, stage, materials, Results and next action come
from that read. Refusal displays unknown continuation and the actual error, never
an earlier successful answer. Before activation, workspace is explicitly unselected
or the already saved activation target, without creating it.
The first absence probe exposed a read-side creation-lock write inherited from T3.
Common `inspect_process_creation` now refuses an absent journal before opening its
existing lock, and inspection opens that lock without creation. Missing lock refuses
without repair. Writers retain the original lock behavior; the journal is reloaded
under that same lock and all T3 Core reconciliation/confirmation stays unchanged.

W20: ship instruction-only resources in the wheel. `entry connection` exports one
connection into a NEW explicitly selected chat project: Codex uses
`.agents/skills/zaratustra/SKILL.md`; Claude Code uses
`.claude/skills/zaratustra/SKILL.md`. The only rendered values are the installed
Python/version, agent kind and chosen connection root. The assets retain no data
selection or domain state, grant no rights, and call the same entry commands.
Existing destinations refuse without overwrite. Exact resource comparison proves
files only; agent discovery/session loading is explicitly unverified by readiness.
Explicit invocation is `$zaratustra` / `/zaratustra`; ordinary discovery stays enabled.
Absolute `python -I -m zaratustra` is the common fallback if PATH or skill discovery
is unavailable. A fresh chat supplies the catalog/designation again and rereads it.

Standard format sources inspected September 14, 2026:
[Codex skills](https://learn.chatgpt.com/docs/build-skills) and
[Claude Code skills](https://code.claude.com/docs/en/skills).

## Evidence and limits

Behavioral tests cover export collision, absent/changed/moved connection bytes,
fresh unselected/draft/current/no-current reads, exact authorization refusal and
stale revision, and byte-stable selected data. The installed probe builds a wheel,
uses locked dependencies, then runs its executable and module fallback in clean
temporary homes and cwd with no PYTHONPATH. Both shipped asset command contracts
run in fresh processes; synthetic confirmation is confined to the dev harness.
This is local command-contract evidence, not model interaction, an independent
user's trial, or a public remote download.
Optional `--codex <absolute binary> --claude <absolute binary>` also runs actual
native discovery twice per host in new homes/projects outside checkout. Codex
receives only initialize/initialized/skills-list; Claude receives only SDK control
initialize. The probe verifies the exact enabled Codex project skill path and
Claude project command, retaining versions and complete protocol traces. No model
turn, provider permission or interactive user acceptance is supplied by that probe.
It does not establish actual agent execution of the instructions.
The unchanged T3 correction probe retains F1/F2/F3 coverage.

The correction additionally uses the existing configured Codex and Claude Code
accounts for actual model turns in new disposable fictional connection roots.
Use the exact installed host binaries, wheel-only product runtime and unchanged
exported skills. Each host receives an explicit skill invocation, catalog and
designation, then a second new session receives the same explicit selection
without the prior conversation or response. Retain prompts, full sanitized
responses/tool events, session identities, versions, commands, exit status,
product stdout and before/after data/skill hashes. Do not infer execution from
discovery or file matching. A successful unfinished-stage continuation and a
refused authorized Process-state read must remain distinct observations.

The model must never type a confirmation digest, pipe approval, patch the console
adapter or infer permission from fixture setup. Fictional setup and existing
synthetic native probes remain explicitly separate from actual skill execution.
If the host cannot obtain the existing user-controlled console confirmation,
retain its exact refusal and identical manual command; return the missing
confirmation as a blocker to successful current/no-current chat evidence.
Do not replace that evidence with synthetic authorization. Missing authentication,
new purchase/account/configuration or new authority likewise stops this leg.

Focused and full `uv run --locked python -m tools.check --deliver` run locally.
One implementation commit, then only root RESULT.md in a separate report commit;
full deliver repeats on the exact report tree. Manual acceptance and separate fresh
binding G5 remain pending HOME (solmax).

Reproduce local installed evidence from the repository, choosing new output:

```text
uv run --locked python -m tools.probe_public_onboarding_r2_t4 --output _scratch/t4-installed
```

To include both installed native loaders, append the two absolute host-binary
arguments above. The temporary Python runtime is cleaned after its observations;
native host roots are retained at the exact temporary paths in the traces because
host subprocesses can briefly retain Windows directory handles after disconnect.
The wheel, installation identity, generated skill copies and complete
stdout/stderr/authorization/discovery traces remain under the selected output.
`native-hosts` records two fresh sessions per host; `commands` records executable,
module fallback, collisions, missing/changed connections and exact continuation.

W01-W17/W26-W27 and K01-K11 are preserved. W18-W20 receive only the local T4 HOW
above; remote pin evidence remains pending. W21-W23 and W28-W32/A05-A09 retain
their accepted/residual T1-T3 dispositions. W24-W25/A03 remain T5+; no updater,
two-release fence, lifecycle, planner, hidden Work, auto-next, historical reopen,
automatic research, user JSON, private connection, owner content choice, Core/Pack
change, schema, paid service, Direction write, push or release is added.

END_OF_FILE: docs/public-onboarding-r2-t4/PLAN.md
