# Core v0.1 Stage 6 — исправление двух собственных выходов родителя

## outcome

Исправлен P2 открытого технического review schema 8: один родительский Work
может завершить два объявленных собственных выхода как публикациями одной
Attempt, так и публикациями связанных Attempt в разные слоты. Пакет передаётся
на один общий review; приёмка schema 7, прохода 3 и Stage 6 не записана.

## evidence

Независимое review Codex коммита `5eec56b` подтвердило полный Windows gate
**687 PASS** и установленную schema 8 пробу с **3 HTTP**, затем сообщило P2.
В сеансе исправления обе точные регрессии сначала дали `stale_work` и
`stale_attempt` на исходном коде, затем прошли после исправления. Отдельная
регрессия проверила, что второй слот не получает подтверждение автоматически;
ещё одна проверила запрет после чужого изменения Work между публикациями.
Scoped `tools.check --files` прошёл до последней регрессии: 10 тестов, типы,
Ruff, 21 импортный контракт и сборка. Предварительный полный Windows gate
прошёл до неё: **690 тестов**, типы, Ruff, 21 контракт, sdist и wheel;
журнал `_scratch/parent-multi-output-deliver.log`.
Итоговые полный gate и установленная проба будут записаны
после выполнения. Техническая запись:
`docs/core-v0.1/STAGE6-PARENT-EXECUTION-IMPLEMENTATION.md`.

## assumptions

Слот закреплён за Work в плане. Доказательство публикации адресует точную
Attempt и результат. Поздняя связанная Attempt может заполнить другой слот;
назначение без новой публикации не возвращает прежний результат в готовность.

## cuts

Проход 4, реальные модели, пользовательская миграция, PR и приёмка не начаты.
`database is locked` и `rpc_transport` остаются открытыми наблюдениями.

## cost

Только локальные одноразовые synthetic spaces, localhost HTTP, тесты и сборка.
Платных вызовов и новых внешних прав нет.

## manual-acceptance

Владелец разрешил исправление и передачу на review, не приёмку.

## next

solmax

---

# Core v0.1 Stage 6 — отдельный срез исполнения родителя

## outcome

Реализована schema 8 для собственной назначенной Attempt корневого и вложенного
составного Work, собственного выхода и обязательства без роли ребёнка. Пакет
готовится для одного технического review. Приёмка schema 7, прохода 3 и Stage 6
не записана.

## evidence

В сеансе реализации новые Core-регрессии прошли: **7 passed**. Отдельная живая
синтетическая трасса обычного Pi/DBOS выполнила A/B/P с **3 HTTP** по шагам
1/2/3; дальнейшие подтверждение, принятие, статус Pi, backup/restore и
удаления не отправляли HTTP. Доказательство собственной Attempt видно по адресу.
Собственный полный Windows `tools.check --deliver` прошёл: **687 тестов**,
215 файлов format-clean, mypy для 189 файлов, Ruff, **21** импортный контракт,
sdist и wheel. Журнал `_scratch/parent-execution-deliver.log`.
Сохранённые отдельные probes прошли: Stage 5 — **2 HTTP**, Stage 6 — **3 HTTP**,
schema 7/pass 3 — **4 HTTP**.
Установленный wheel точного чистого кодового SHA
`3d6adcce5ab4c561a1c4204d341e72d32272d3f5` прошёл вне checkout:
`source_tree_dirty=false`, Python 3.13.7, SQLite 3.53.3, **3 HTTP**;
SHA-256 wheel
`C4CB44C45C6CAC1D3AB4D32F553EADD812E5F4D6AA3805FB89464D9D51C71C75`.
Отчёт `_scratch/parent-installed-3d6adcc/report.json`. Первая установка
`8dfeeb7` выполнила сценарий на 3 HTTP, но её отчётная обёртка завершилась
`KeyError: python`; метаданные отчёта исправлены в `3d6adcc`.
Это проверки реализации; независимого review этого среза пока нет.
Техническая запись:
`docs/core-v0.1/STAGE6-PARENT-EXECUTION-IMPLEMENTATION.md`.

## assumptions

План объявляет собственный слот через Work; Attempt и её закрепление фиксируются
в публикации и подтверждении. Новая Attempt того же Work допустима без редакции
плана, но прежнее подтверждение не переносится молча.

## cuts

Проход 4, реальные модели, пользовательская миграция, PR и приёмка не начаты.
`database is locked` и `rpc_transport` остаются открытыми наблюдениями.

## cost

Только локальные одноразовые synthetic spaces, localhost HTTP, тесты и сборка.
Платных вызовов и новых внешних прав нет.

## manual-acceptance

Владелец разрешил реализацию и один общий review, не приёмку.

## next

solmax

---

# Core v0.1 Stage 6 — закрытие review 3.10 и план отдельного среза

## outcome

По решению владельца закрыто только техническое review части 3.10 на точном
`4c9c8ece607bb58602c2573b9b23e8f60134470c`. Подготовлен рабочий план
«Исполнение родителя и обязательство в том же Work», без реализации. Приёмка
schema 7, прохода 3 и Stage 6 не записана.

## evidence

Независимый отчёт Codex:
`C:/Users/Anton/AppData/Local/Temp/zaratustra-stage6-3.10-final-review-4c9c8ec-1790350614290/READONLY-REVIEW.md`.
Его внешние сценарии подтвердили восстановление адресованного PENDING/local
workflow до claim (тот же ID, 2 HTTP, replay без новых HTTP), политику
`unknown` после claim (0 HTTP) и изоляцию двух legacy соседей (2 HTTP).
Независимый scoped Windows gate прошёл **35 Pi adapter тестов**, scoped
format/Ruff/mypy, 21 импортный контракт и обе сборки. Независимый установленный
wheel чистого точного коммита прошёл с **3 синтетическими HTTP**, SHA-256
`2AE24862739BCC3B0262574FD15E0E3CBCBCDD395216C8F7C1987BAFAEDE297B`.
Полный Windows gate **680 тестов** выполнен сеансом реализации, а не этим
независимым review. Подробности и ограничения —
`docs/core-v0.1/STAGE6-PASS3-REVIEW-3.10.md`; рабочий план —
`docs/core-v0.1/STAGE6-PARENT-EXECUTION-PLAN.md`.
Для этого документального пакета выполнен отдельный собственный полный
Windows `tools.check --deliver`: **680 passed**, 214 format-clean файлов,
строгий mypy для 188 файлов, Ruff, **21** импортный контракт, sdist и wheel;
журнал `_scratch/stage6-parent-plan-deliver.log`. Первый запуск без
`ZARATUSTRA_SQLITE_DLL` остановился при collection на встроенной SQLite
3.50.4; итоговый PASS получен с закреплённой SQLite 3.53.3 DLL. Этот
последующий прогон не приписывается независимому reviewer.

## assumptions

План опирается на согласованный раздел `STAGE6-PASS3-PLAN.md`. Для его
исполнения нужно отдельное разрешение владельца; явная schema 8 является
будущим контрактом, не изменением текущего хранилища.

## cuts

Реализация среза, проход 4, реальные модели, миграция пользовательских
данных, PR и приёмка не начаты. `database is locked` и `rpc_transport`
остаются открытыми наблюдениями.

## cost

Только локальное чтение независимого отчёта, документальные изменения и
проверка репозитория. Реальных модельных вызовов, платных сервисов и новых
внешних прав нет.

## manual-acceptance

Владелец закрыл техническое review 3.10 отдельно и запросил план;
приёмку schema 7, прохода 3 и Stage 6 не давал.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — восстановление адресного PENDING workflow

## outcome

Исправлен один P1 части 3.10: runner восстанавливает свой уже адресованный
PENDING DBOS workflow, записанный прежней версией с executor ID `local`.
Workflow ID и Core launch claim сохраняют границу replay. Пакет передаётся
на одно техническое review; приёмка schema 7, прохода 3 и Stage 6 не записана.

## evidence

Независимый Codex review точного `8130b1ba2b2344284d523cf420a2327610aa133e`
закрыл прежние два замечания и подтвердил Windows gate (**678 тестов**),
schema 7 трассу (**4 HTTP**) и установленный wheel (**3 HTTP**). Он обнаружил
P1: адресный PENDING workflow старого executor `local` не подхватывается
новым UUID executor. Отчёт:
`C:/Users/Anton/AppData/Local/Temp/zaratustra-stage6-3.10-fix-review-8130b1b-1790347475489/READONLY-REVIEW.md`.
До правки этот внешний сценарий повторён точно: старый runner завершился
до claim с кодом 90; новый оставался в ожидании 15 секунд без вопроса/HTTP,
а старый контроль восстановился на тех же данных с **2 HTTP**.

После правки межверсионный живой probe через обычные DBOS/Pi дал до claim
SUCCESS на том же ID с **2 HTTP**, а повторный запуск — **0 новых HTTP**.
После уже записанного claim восстановление вернуло `process_outcome_unknown`,
Core assignment `unknown`, **0 новых HTTP**. Прежний внешний сценарий двух
legacy соседей сохранил A2 в `ENQUEUED` после A1 и затем выполнил обоих с
**2 HTTP**. Сохранённый fault probe текущего маршрута прошёл, включая
unknown и отсутствие повтора потерянной HTTP-отправки. Затронутые тесты:
**29 passed**; две новые регрессии создают PENDING/local через DBOS без
правки SQLite. Поддерживаемая schema 7 трасса прошла с **4 HTTP**.

Собственный полный Windows `tools.check --deliver`: **680 тестов**,
214 файлов format-clean, строгий mypy для 188 файлов, Ruff, **21** импортный
контракт, sdist и wheel; лог `_scratch/fix-pending-deliver.log`.
Техническая запись: `docs/core-v0.1/STAGE6-PASS3-IMPLEMENTATION.md`.
Установленный wheel проверен вне checkout с точного чистого кодового коммита
`89b5d03f3f01e77f6bb74d3ab09b59dc22f1f510`: `source_tree_dirty=false`,
Python 3.13.7, SQLite 3.53.3, **3 синтетических HTTP**, wheel SHA-256
`2AE24862739BCC3B0262574FD15E0E3CBCBCDD395216C8F7C1987BAFAEDE297B`.
Отчёт: `_scratch/fix-pending-installed-89b5d03/report.json`. Это проверка
совместимого Stage 6, не всей новой schema 7 трассы. Атрибуция wheel —
последующее документальное изменение после полного gate кодового коммита.

## assumptions

DBOS resume меняет только выбранный PENDING/local workflow адресной версии
на очередь его Attempt. Прежний общий маршрут по-прежнему изолирован.
Записанный Core launch claim не разрешает повторный Pi/HTTP.

## cuts

Следующий срез, исполнение составного родителя, проход 4, реальные модели,
миграция, PR и приёмка не начаты. `database is locked` и `rpc_transport`
остаются отдельными открытыми наблюдениями.

## cost

Только локальные одноразовые synthetic spaces, localhost HTTP, тесты и сборка.
Платных вызовов и новых внешних прав нет.

## manual-acceptance

Владелец разрешил исправление и один общий review, не приёмку.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — исправление review части 3.10

## outcome

Одним пакетом исправлены P1 общей legacy DBOS очереди и P2 контекста
назначенного ребёнка. Часть 3.10 остаётся на техническом review; это не
приёмка schema 7, прохода 3 или Stage 6.

## evidence

Независимый Codex review `8a74cecd35fc8506bc45c032339bccfa3c842c12`
подтвердил прежний полный Windows gate (**676 тестов**) и выявил P1/P2.
Сеанс исправления сначала повторил оба его внешних сценария на чистом
`8a74cec`: A1 исполнился, но ошибочно записал A2 устойчивый
`resource_unavailable` (**1 HTTP**); назначенный A получил пустые
`obligations`, без Choice/Exception в реальном синтетическом HTTP (**1 HTTP**).
Отчёт review:
`C:/Users/Anton/AppData/Local/Temp/zaratustra-stage6-3.10-review-8a74cec-1790342288289/READONLY-REVIEW.md`.

После исправления те же внешние скрипты дали A1/A2 `proposed` с **2 HTTP**,
при этом A2 оставался `ENQUEUED` после запуска A1; дочерний контекст Pi
передал три адресных состояния, Choice и Exception в фактическом HTTP
(**1 HTTP**). Добавлены две регрессии с DBOS workflow и назначенным Bridge;
вложенная проекция проверяет раздельные обязательства предка и потомка.
Затронутые файлы: **16 passed**. Поддерживаемый pass 3 probe прошёл с
**4 HTTP**, Stage 5 RPC — с **2 HTTP**. Stage 6 RPC сначала упал с
`rpc_transport`, повтор в новом space прошёл с **3 HTTP**; это не устранение
наблюдения. Собственный полный Windows `tools.check --deliver`: **678 тестов**,
214 файлов format-clean, строгий mypy для 188 файлов, Ruff, **21** импортный
контракт, sdist и wheel. Журнал: `_scratch/fix-3.10-deliver.log`.

Техническая запись: `docs/core-v0.1/STAGE6-PASS3-IMPLEMENTATION.md`.
Установленный wheel проверен вне checkout с точного чистого кодового коммита
`224f00c5694707379262ecdf264d7563b16768ca`: `source_tree_dirty=false`,
Python 3.13.7, SQLite 3.53.3, **3 синтетических HTTP**. Wheel SHA-256:
`53CADB8063B693D25CAA6DB14899818C6D8EA875C7EBF6B153C5E8D663025BF3`;
отчёт `_scratch/fix-3.10-installed-224f00c/report.json`. Это совместимый
установленный Stage 6 сценарий, не новая schema 7 трасса. Атрибуция wheel —
последующее документальное изменение после полного gate кодового коммита.

## assumptions

Старый workflow сохраняет устойчивый ID и версию, но перед запуском своего
runner переводится публичным DBOS resume из общей очереди в очередь собственной
Attempt. Runner слушает только её и получает отдельный executor ID.
Независимые старые записи не забираются. Дочерняя проекция наследует только
адресные состояния обязательств непосредственного родителя, без текста плана
и новых прав. Синтетический provider не подтверждает поведение реальной модели.

## cuts

Следующий срез, исполнение составного родителя, проход 4, реальные модели,
миграция, PR и приёмка не начаты. `database is locked`, `rpc_transport` и
прежнее расхождение `stale_basis` внутри `any` остаются открытыми.

## cost

Только локальные синтетические space, localhost HTTP, тесты и сборка.
Платных вызовов и новых внешних прав нет.

## manual-acceptance

Владелец разрешил исправление и одно общее review. Решение о приёмке не записано.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — часть 3.10, Pi и живая трасса

## outcome

Часть 3.10 реализована в границах согласованного плана: адресная видимость
новых состояний в Pi и полный синтетический localhost-сценарий через обычный
Pi/DBOS. Пакет передаётся на одно общее техническое review. Это не приёмка
schema 7, прохода 3 или Stage 6.

## evidence

Владелец отдельно закрыл техническое review 3.9 на точном
`73fffa7afb7bdfcc815938a132ab73d3955115e0` по независимому Codex;
атрибуция его **193 foundation тестов** и установленного wheel с **3 HTTP**
зафиксирована в `docs/core-v0.1/STAGE6-PASS3-REVIEW-3.9.md`.

Собственный live probe 3.10 прошёл через Pi 0.87.0/DBOS 3.0.0: B ждёт
адресного ответа (1 HTTP), A принят при ожидающем B (2), после ревизии
плана B перенесён и продолжил (3), G → N → P принят под Method v2 (4).
Конфликт ресурса, перепроверка, ограждение Art, отдельный Pi-статус,
перезапуск и backup/restore не добавили HTTP. После каждого адресного
удаления live store и новый backup не содержали маркеров удалённых Work и
Artifact; старый backup очищен. Финальный отчёт:
`_scratch/probe-stage6-pass3-live-final/report.json`. Новый процесс
`python -I -X utf8` подтвердил итог.

Адресные регрессии дали **22 passed**. Неизменённый Stage 5 probe прошёл
с **2 HTTP**. Неизменённый Stage 6 probe сначала упал с
`storage: database is locked` при `_record_stop` через `_space_info` /
`PRAGMA application_id`; второй запуск выявил несовместимую строку
`/zara-status`, которую сохранили в расширенном формате; третий прошёл с
**3 HTTP**. Причина блокировки БД не установлена, успешный повтор её не
устранил. Полный Windows `uv run --locked python -m tools.check --deliver`
собственного сеанса прошёл: **676 тестов**, строгие типы, Ruff, **21**
импортный контракт, sdist и wheel. Это не результат независимого review.
Подробности реализации и границ: `docs/core-v0.1/STAGE6-PASS3-IMPLEMENTATION.md`.
На чистом точном кодовом коммите
`a80c2531b2a8e861706c3fd6af283badb55920c2` неизменённый
`tools.probe_install_stage6` прошёл вне checkout с **3 синтетическими HTTP**,
Python 3.13.7 и SQLite 3.53.3; `source_tree_dirty=false`, wheel SHA-256
`654F0CCCC62BB90E8A3A6825448DB52F4E6638EFB59D01B7E4304AE9C010CAAB`.
Отчёт: `_scratch/probe-install-stage6-pass3-clean/report.json`.
Это проверка совместимого установленного Stage 6 сценария; новая schema 7
трасса запускалась в checkout. Последние добавления атрибуции wheel —
документальные изменения после чистого кодового коммита; код и тесты не
менялись и полный gate не приписывается этому документальному коммиту.

## assumptions

Одновременные назначенные Attempts используют свои рабочие каталоги и
адресные DBOS queue/app version; уже существующий workflow сохраняет свой
записанный маршрут. Проекция Pi содержит состояния и адреса, без текста
плана. Синтетический provider не подтверждает поведение реальной модели.

## cuts

Собственное исполнение составного родителя, проход 4, реальные модели,
миграция, PR и приёмка не начаты. `database is locked`, `rpc_transport`
и прежнее расхождение `stale_basis` внутри `any` остаются открытыми.

## cost

Только локальные одноразовые синтетические space, localhost HTTP, проверки
и сборка. Платных вызовов и новых внешних прав нет.

## manual-acceptance

Владелец разрешил реализацию и один общий технический review части 3.10.
Результаты проверок не записывают приёмку schema 7, прохода 3 или Stage 6.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — P2 замены принятого корня H

## outcome

Узко исправлен оставшийся P2: при замене уже принятого составного H в плане
N открытого необязательного потомка S можно закрыть адресно в той же
транзакции. Исход H и обязательного L сохраняется. Техническое review 3.9
остаётся открытым, приёмка не записана.

## evidence

Независимый Codex review точного чистого коммита
`d0a117cf4cc59713ff96bdb6db6e82de2022897a` подтвердил прежние три
исправления, полный Windows gate с 674 тестами и установленный wheel с 3
синтетическими HTTP. Оно же выявило P2 при замене H с открытым S, включая
вариант с действующей Attempt. Сеанс исправления точно воспроизвёл оба
варианта до правки и после неё проверил успешное закрытие. Подробные шаги,
результаты и атрибуция — в
`docs/core-v0.1/STAGE6-PASS3-REVIEW-3.9-OPEN.md` и
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.9.md`.

Два новых публичных регрессионных варианта проверяют отсутствие S,
корректное адресное закрытие, устаревшие ревизии, отказ по правам, прямой
запрет действий под закрытым H, сохранение H/L и replay, остановку Attempt,
`unknown` для отправленного вызова и резерв 5. Прежние сценарии санации и
замены N сохранены. Итоговые проверки этого сеанса указаны в открытом review
отдельно от независимого результата `d0a117c`.
Полный Windows `uv run --locked python -m tools.check --deliver` этого сеанса
прошёл: 676 тестов, mypy для 187 файлов, Ruff, 21 импортный контракт,
sdist и wheel. Checkout Stage 6/Stage 5 RPC probes прошли с 3/2
синтетическими HTTP, установленный wheel вне checkout — с 3.

## assumptions

Принятие H с открытым необязательным S разрешено условием завершения Method.
Разрешение закрыть S под принятым H относится только к атомарному уходу H
из действующего плана; обычные действия под закрытым предком запрещены.
Схема остаётся 7.

## cuts

Часть 3.10, приёмка schema 7/прохода 3/Stage 6, реальные модели, миграция
разработки и PR не начаты. `database is locked` и `rpc_transport` остаются
открытыми наблюдениями без заявленного устранения.

## cost

Только локальные синтетические сценарии, тесты и сборки. Платных модельных
вызовов и новых внешних прав нет.

## manual-acceptance

Пакет передаётся на одно общее техническое review; решение о приёмке
остаётся за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — продолжение исправления 3.9 от `63f64eb`

## outcome

Три дефекта независимого review исправлены одним пакетом в границах 3.9:
принятие вложенного Work проверяет актуальность предков, закрытие уходящего
дерева охватывает открытых потомков под уже принятым составным Work,
создание вложенного Work проверяет тип именованных входов по точной версии
Method. Техническое review 3.9 остаётся открытым; приёмка не записана.

## evidence

Независимый Codex review точного `63f64ebce37502028eca93a77db6c8458d1d5add`
подтвердил прежнее исправление санации и сам выполнил полный Windows gate:
669 тестов, типы, Ruff, 21 импортный контракт, обе сборки. Отдельные
публичные сценарии review обнаружили три дефекта. Сеанс исправления сначала
повторил все три на исходном коде, включая S с действующей Attempt, затем
проверил исправления теми же сценариями. Подробные шаги и атрибуция — в
`docs/core-v0.1/STAGE6-PASS3-REVIEW-3.9-OPEN.md` и
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.9.md`.

Пять новых регрессионных вариантов проходят через публичный Core: изменение
входа предка перед принятием N; закрытие optional S под принятым H с
активной Attempt и без неё; отказ при пропущенном S; неверный и верный
`media_type` при вложенных `add` и `replace`. Прежняя санация и полный цикл
G → N → P сохранены. Финальный Windows `uv run --locked python -m tools.check
--deliver` этого сеанса прошёл: 674 теста, mypy для 187 файлов, Ruff,
21 импортный контракт, sdist и wheel. Checkout Stage 6/Stage 5 RPC probes
прошли с 3/2 синтетическими HTTP, установленный wheel вне checkout — с 3.
Эти результаты зафиксированы в открытом review и не приписываются
независимому Codex.

## assumptions

Правило завершения Method определяет достаточных детей для принятия;
необязательные дети могут оставаться открытыми. При уходе всего поддерева
каждый открытый потомок требует адресного исхода даже под принятым предком.
Схема остаётся 7.

## cuts

Часть 3.10, приёмка schema 7/прохода 3/Stage 6, реальные модели, миграция
разработки и PR не начаты. Наблюдения `database is locked` и
`rpc_transport` остаются открытыми; исправление их причин не заявляется.

## cost

Только локальные синтетические сценарии, тесты и сборки. Платных вызовов
моделей и новых внешних прав нет.

## manual-acceptance

Пакет передаётся на одно общее техническое review. Решение о приёмке
остаётся за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — часть 3.9: вложенный составной Work

## outcome

Реализован кандидат вложенного составного Work с точным Method роли, выдачей по цепочке
предков, собственным принятием, явным атомарным закрытием потомков при замене
и обслуживанием адресной истории. Независимое review коммита `74485ca`
подтвердило открытый P1 по вложенной Artifact-зависимости. Приёмка не записана.

## evidence

Техническое review 3.8 отдельно закрыто на
`512fcd37accce339b487d006420e3e3ce2deec4f`; независимые результаты
Codex и их границы указаны в
`docs/core-v0.1/STAGE6-PASS3-REVIEW-3.8.md`. Сценарии 3.9 через публичный
Core проверяют закрепление N@2 против N@3, выдачу и принятие G→N→P,
отказы `open_children`, `mapping_invalid`, `stale_plan`, `stale_work` и
`permission_denied`, остановку назначенной Attempt, закрытый вопрос,
неизвестный отправленный вызов, поздний отказ, `resource_busy`, независимую
ветвь, последовательное удаление G→N→P, новый процесс и SQLite/backup.
Первый полный Windows gate обнаружил вмешательство проверки предков в
`revalidate_result` с изменённой собственной предпосылкой; адресный gate
ограничен предками, целевые 3.5/3.6–3.7 регрессии повторены. Финальный
`uv run --locked python -m tools.check --deliver`: PASS, 662 теста, Ruff
format/check, mypy для 186 файлов, 21 импортный контракт, sdist и wheel.
Подробности —
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.9.md`.
Открытое замечание: `docs/core-v0.1/STAGE6-PASS3-REVIEW-3.9-OPEN.md`.

## assumptions

Новые поля запроса не меняют сохранённые канонические значения старых
операций. Вложенный Work хранит собственный план и обязательства под своим
ключом schema 7. Удаление выполняется снизу вверх.

## cuts

Часть 3.10, отдельный срез исполнения родителя, проход 4, реальные модельные
вызовы, миграция разработки, PR и приёмка schema 7/прохода 3/Stage 6 не
начаты. `database is locked` остаётся отдельным открытым наблюдением без
установленной причины; устранение не заявляется.

## cost

Сценарии используют синтетические данные локально. Платных вызовов и новых
внешних прав нет.

## manual-acceptance

Пакет передаётся на техническое review. Решение о приёмке остаётся за
владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — исправления review `907a739` в части 3.8

## outcome

Исправлены три дефекта одним пакетом: снятые обязательства не участвуют в
действующих проверках и мутациях; удаление Method очищает исторические копии
его текста требования по точной версии; новое независимое основание больше
не наследует происхождение перенесённого текста. Пакет передаётся на одно
техническое review без записи приёмки.

## evidence

Независимый Codex review коммита `907a7391ae7f7ddb59e7e023873cf1bb5e73ac2e`
подтвердил Windows `tools.check --deliver`: 656 тестов, типы, Ruff, 21
импортный контракт и сборка. Codex передал три дефекта. До исправления четыре
новые регрессии через публичный Core воспроизвели отказ принятия родителя
из-за retired `checked`, повторное открытие retired `art_review`, оставшийся
маркер Method v1 после удаления и ложную санацию нового подтверждения Y при
удалении постороннего Q.

После исправления прошли 4 новые регрессии и 12 прежних сценариев 3.8;
совместный адресный прогон 3.6–3.7 и 3.8 — 35 тестов. Проверено также
последовательное удаление v1 и v2 при повторном использовании ключа:
точные исторические адреса сохраняются, независимая v3 не очищается.
Итоговый локальный Windows `uv run --locked python -m tools.check --deliver`:
PASS, 660 тестов, Ruff format/check, mypy для 185 файлов, 21 импортный
контракт, sdist и wheel. Подробности —
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.8.md`.

## assumptions

Действующим является только ключ текущего закреплённого Method; retired
записи проверяются как история и читаются по точному адресу. Ревизия,
записанная при переходе как retired, принадлежит исходному Method; новый
экземпляр того же ключа принадлежит Method новой редакции плана.

## cuts

Часть 3.9, приёмка schema 7, прохода 3 и Stage 6, отдельный срез,
миграция разработки, реальные модельные вызовы и PR не начаты.
`database is locked` остаётся отдельным открытым наблюдением; устранение
не заявляется.

## cost

Проверки выполнены на синтетических данных локально. Платных вызовов и
новых внешних прав нет.

## manual-acceptance

Владелец запросил исправление и передачу на review. Техническое review
этого пакета и приёмка остаются за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — часть 3.8: переход Method

## outcome

Реализован явный переход начатого составного Work на точную более новую версию
Method через редакцию активного плана. Все действующие обязательства v1
сопоставляются с v2; переносимые результаты, применимость и waiver
перепроверяются, новые требования открываются, старые снимаются с адресами.
Attempt сохранённого узла переносится под v2 после повторного допуска,
остальные ограждаются. Пакет передаётся на одно техническое review.

## evidence

Владелец закрыл техническое review частей 3.6–3.7 на
`98a61f20bf0eec06fa3e9e1501b35f0bb31b6ecc` и разрешил только 3.8.
Атрибуция предыдущей независимой проверки Codex отдельно записана в
`docs/core-v0.1/STAGE6-PASS3-REVIEW-3.6-3.7.md`: пять сценариев,
foundation 161 тест, типы, Ruff, 21 контракт, сборка. Полные 644 теста
относятся к предыдущему сеансу реализации.

Новые сценарии `test_method_transition.py` прошли локально на Windows:
публикация v2 без действия на v1, обязательные сопоставления и отказы,
последовательный переход, перенос и ограждение Attempt, старая `unknown`
попытка, доказательство после замены, применимость и waiver, удаление Method,
backup/restore, очистка копии основания после удаления Artifact и чтение
новым процессом. Детали —
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.8.md`.
Итоговый Windows `uv run --locked python -m tools.check --deliver` этого
сеанса: PASS, 656 тестов, Ruff format/check, mypy для 184 файлов, 21 импортный
контракт, sdist и wheel. Пакет проверен после сценария очистки обеих новых
таблиц при удалении родителя.

## assumptions

До schema 7 смены Method у начатого Work не было; upgrade закрепляет каждую
существующую ревизию плана за тогдашней версией родителя. Снятые ключи
историчны и не требуют повторного сопоставления при следующем переходе.

## cuts

Часть 3.9, приёмка schema 7, прохода 3 и Stage 6, отдельный срез,
миграция разработки, реальные модельные вызовы и PR не начаты.
`database is locked` остаётся отдельным открытым наблюдением; его причина
не установлена и устранение не заявляется.

## cost

Проверки используют синтетические данные локально. Платных вызовов и новых
внешних прав нет.

## manual-acceptance

Владелец разрешил реализацию и передачу на review. Техническое review
пакета 3.8 и приёмка остаются за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — восстановление исторической зависимости outcome

## outcome

Исправлена P1-регрессия review `26a7da4`: основание принятия, закрытия и
перепроверки ребёнка снова зависит от плана, который называл именно этот Work
до его выдачи. Сопоставление с конкретным Work в каждой ревизии оставляет
независимые A2/Y и прежние исправления P1/P2 в силе. Пакет передаётся на одно
общее review; приёмка не записана.

## evidence

Владелец передал результат независимого review `26a7da4`: прежние P1/P2
подтверждены, Windows `tools.check --deliver` PASS (641 тест, типы, Ruff,
21 контракт, сборка). Три новых сценария через публичный Core на неизменном
`26a7da4` упали на сохранённом тексте: принятие A, `close_work(failed)` и
`revalidate_result` после изменения независимого входа Z. Во всех сценариях
Artifact X был только в basis plan 1; plan 2 без X сохранял A, выдача была
после редакции. Прежний backup удалён, но маркер оставался в основании.

После исправления семь адресных регрессий прошли: три новые и четыре прежние
P1/P2. Проверены replay `history_unavailable`, квитанция `not_found`, чтение
новым процессом, закрытая SQLite, новый backup и сохранение исхода Work.
Этот сеанс Codex на Windows выполнил
`uv run --locked python -m tools.check --deliver`: PASS, 644 теста, Ruff,
mypy для 183 файлов, 21 импортный контракт, sdist и wheel. Подробности —
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`.

## assumptions

Историческая ревизия плана структурно доступна основанию исхода, если в ней
уже назван тот же Work. Ревизия выдачи не ограничивает эту историю. Роль сама
по себе не связывает основание нового A2 с планом старого A.

## cuts

Часть 3.8, приёмка, миграция разработки, реальные модельные вызовы и PR не
начаты. Ранее наблюдавшееся у Codex `storage: database is locked` остаётся
отдельным открытым вопросом; этот пакет не заявляет его устранение.

## cost

Проверки выполнены локально на синтетических данных. Платных вызовов и новых
внешних прав нет.

## manual-acceptance

Владелец запросил коррекцию и передачу на review. PASS автоматических проверок
не является приёмкой; решение остаётся за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — исправления P1/P2 review `939c27d`

## outcome

Один пакет исправляет утечку текста удалённого Work через rationale редакции
плана (P1) и ложную зависимость нового исполнителя роли от старого Artifact
(P2). Адресная история решений и ролей, независимое принятие A2, подтверждение
Y, его квитанция и replay сохранены. Review `939c27d` остаётся открытым до
одного общего рассмотрения владельцем. Приёмка не записана.

## evidence

До изменения продукта оба замечания воспроизведены через публичный Core на
коде кандидата `939c27d` (`src`, `tests`, `tools` совпадали с передачей
`0d13a63`). Новые основные регрессии сначала падали на исходном коде. После
исправления регрессии покрывают replace/cancel/release, сохранённый индекс и
последовательные удаления, перезапуск, закрытую SQLite и новый backup.

Codex Windows-проверка исходного кандидата, переданная владельцем:
`tools.check --deliver` PASS, 637 тестов, типы, Ruff, 21 импортный контракт,
sdist и wheel; отдельная цепочка Attempt 1→2→3 прошла. Установленный Pi-probe
первый раз упал с `storage: database is locked`, включая
`_record_stop → read_execution → _space_info → PRAGMA application_id`; один
повтор того же wheel прошёл с 3 HTTP. Причина блокировки не установлена,
повтор не считается её исправлением.

Этот сеанс Codex на Windows: `uv run --locked python -m tools.check --deliver`
PASS, 641 тест, mypy для 183 файлов, Ruff, 21 импортный контракт, sdist и wheel.
`tools.probe_stage6_rpc` PASS (3 localhost HTTP), установленный wheel вне
checkout через `tools.probe_install_stage6` PASS (3 localhost HTTP),
`tools.probe_stage5_rpc` PASS (2 provider calls). Локальная первая попытка
установленной пробы остановилась до trial из-за ошибочного `TEMP` внутри
checkout; повтор без этой настройки прошёл. Она не связана с указанной
блокировкой, её причину не устанавливает.

Точные шаги воспроизведения, исправления и проверок записаны в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`.

## assumptions

Адресованное решение `replace`/`cancel`/`stale`/`release` позволяет считать
текст rationale зависимым от уходящего Work. Для schema 7 историческое
доказательство роли относится к Work, который занимал роль в той ревизии
плана, где оно записано. Для schema 5–6 сохранена прежняя консервативная
обработка исторических доказательств.

## cuts

Часть 3.8, миграция разработки, реальные модельные вызовы и PR не начаты.
Наблюдение `database is locked` остаётся открытым; этот пакет не заявляет его
устранение. DDL и checksum schema 7 не менялись.

## cost

Проверки использовали локальный Windows runtime, синтетические данные,
localhost и игнорируемый `_scratch`; платных вызовов и новых прав нет.

## manual-acceptance

Владелец запросил исправления и один общий review после push. Автоматические
проверки не являются приёмкой. Решение о закрытии review и приёмке остаётся
за владельцем.

## next

solmax

---

# Core v0.1 Stage 6, проход 3 — пакет 3.6–3.7: редакция действующего плана, перенос и ограждение Attempt

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`0af70349bedec95ca39981b3312a8a81768ca964`:

- `tools.check --deliver` — PASS: 624 теста, типы, Ruff, 21 импортный контракт и обе
  сборки;
- установленный wheel вне checkout — PASS, 3 синтетических localhost HTTP;
- оба исходных сценария P1 и дополнительная проверка исторических оснований с
  последовательным удалением после перезапуска прошли;
- приоритеты `decision_conflict` / `premise_changed` владелец считает согласованными.

По этому результату владелец закрыл техническое review пакета 3.4–3.5; это не приёмка
schema 7 или Stage 6. Закрытие записано в `STAGE6-PASS3-CHECKPOINT-3.4-3.5.md`. Затем
владелец разрешил единый пакет — части 3.6 и 3.7 последовательно. Он реализован: код и
тесты — `3c18172`, документы — `STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`.

- 3.6. Новая операция `revise_active_plan`:
  - ревизия N+1 записывается только с явным решением по каждому узлу N и каждому новому
    узлу: `keep`, `replace`, `cancel`, `stale`, `release`, `add`;
  - уходящий незавершённый узел получает исход в той же транзакции, как у `close_work`;
  - выдача сохраняемого узла переносится после перепроверки прав, входов и готовности в
    N+1, иначе редакция отказывает с адресом узла;
  - обязательство, чьё доказательство принадлежит ушедшему из роли Work, переоткрывается
    (`node_replaced`), а применимость и waiver сохраняются;
  - старый allow не наследуется (`stale_plan`).

  Членство, решения по узлам и история ролей — адресные таблицы schema 7 с точными
  чтениями `read_plan_nodes` и `read_role_history`. Сохранённый индекс плана называет Work
  каждой роли, поэтому удаление после перезапуска находит зависимых по истории ролей.
- 3.7. Действующая Attempt сохраняемого узла продолжает работу через явную запись
  переноса; её закрепление не переписывается. Прочие Attempt ограждены:
  - следующий эффект — `stale_plan` раньше `work_closed`, без HTTP;
  - поздний ответ на закрытое ожидание — `stale_wait`;
  - остановка и исход отправленного вызова остаются доступными;
  - `unknown` удерживает резерв и исключительный корень, замещающий узел на нём получает
    `resource_busy`.

  Независимый узел продолжает работу. Restore сохраняет переносы как историю без
  возобновления владения. Удаление ребёнка удаляет его переносы, а технические копии
  DBOS/Pi очищаются по адресу.

Расхождение `stale_basis` внутри `any` и наблюдение `database is locked` остаются
открытыми. Остановка для одного общего review. Часть 3.8, реальные модели, миграция
разработки и PR не начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `0af7034` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

- Новые тесты без модели, 13:
  - `test_active_plan.py`, 8 (3.6): W20–W23; замена принятого узла; удаление узла с
    открытым обязательством до waiver; повторное заполнение роли через `add`; отказы с
    адресами; перепроверка сохраняемого узла; restore и независимое удаление заменённого;
    индекс с историей ролей после перезапуска;
  - `test_plan_transfers.py`, 4 (3.7): перенос и ограждение; повтор проверки закрепления
    прохода 2 через публичную редакцию; ожидающий узел и цепочка переносов; restore и
    удаление;
  - `test_child_assigned.py` +1 — модельно-независимый назначенный bridge и очистка
    технических копий.
- Четырнадцать временных поломок новых правил ловятся тестами. Одну из них — зависимости
  исхода без учёта того, какой Work заполнял роль, — сначала не ловил ни один тест;
  добавлена проверка независимости основания заменившего Work. Исходники восстановлены
  побайтно.
- Пространство schema 7 с записью DDL контрольной точки 3.4–3.5 отказывает
  `unsupported_schema` (скрипт вне репозитория).

Проверки на коде `3c18172`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 636 прошли, 1 упал,
  159,15 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 182 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` на чистом дереве — hygiene, структура
  отчёта, Ruff format (208 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows; пакет эти файлы не менял.
  `uv build --no-sources` отдельно собрал sdist и wheel.
- Probe без изменений, на чистом дереве:
  - `tools.probe_stage6_rpc` — `passed`, 3 HTTP, предметный отказ до запуска — 0 HTTP;
  - `tools.probe_install_stage6` — wheel SHA-256
    `395E01B0E5BD1FD58CEB9E37578350B9E981E18163CAC86E8E14EF278269A5E7`, новый venv вне
    checkout, `source_commit` `3c18172`, `source_tree_dirty: false`, `passed`, 3 HTTP;
  - `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
    restore в эпоху 2, санация при удалении.
- Отчёты — в игнорируемом `_scratch/stage6-pass3-367-*`.

Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

Технические толкования контракта перечислены в `STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`,
раздел «Толкования». Главные:

- роль, покинувшая план, снова заполняется только новым Work через `add`;
- завершённый сохраняемый узел переносит выдачу без перепроверки, потому что эффектов у
  него нет;
- отказ перепроверки сохраняет код причины и называет адрес узла;
- в schema 7 ограждение называет свою причину раньше `work_closed`. На контрольной точке
  3.1 поздний ответ на закрытое ожидание закрытого ребёнка давал `work_closed`, теперь —
  `stale_wait`, как в плане 3.7 и 3.9. Это не принятое поведение.

Промежуточное ограничение `attempt_active` из 3.6 не вводилось, так как 3.7 снимает его в
том же пакете. Результат Windows-проверки `0af7034` передал владелец; эта сессия саму
проверку не наблюдала.

## cuts

- Расхождение `stale_basis` внутри `any` не менялось: известный вопрос владельца.
- Наблюдение `database is locked` не исследовалось и остаётся открытым для прохода 4.
- Составные узлы, закрытие потомков и цепочка предков (3.9), переход Method (3.8) и
  видимость в Pi (3.10) не входят в пакет. Живого прогона огороженной Attempt через DBOS
  нет: её отказ на первой проверке показан модельно-независимым тестом, матрица сбоев —
  проход 4.
- Windows-прогон здесь невозможен; `tools.check --deliver` на Linux останавливается на
  mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было.

## manual-acceptance

Нужен один общий review пакета 3.6–3.7. Он не является приёмкой частей, schema 7 или
прохода 3. Обязательный полный gate — независимый Windows-прогон, результат которого
передаёт владелец; команды — в `docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.6-3.7.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — исправление пакета 3.4–3.5 по review Windows-проверки `c11d54f`

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`c11d54f5394c358d4e835b9ac2deb9a0a6962e63`:

- `tools.check --deliver` — PASS: 616 тестов, типы, Ruff, 21 импортный контракт, sdist и
  wheel;
- установленный wheel вне checkout: Pi-probe — PASS, 3 localhost HTTP.

Review пакета не закрыт. Исправлено одним пакетом, коммит `39f6cde`:

1. P1. Основание `ResultRevalidation` переживало удаление неизменившейся предпосылки,
   которую цитировало. Переданное воспроизведение и второй вариант воспроизведены здесь
   ровно как переданы:
   - готовность A `artifact_current(Y)`;
   - Y как общий вход родителя и основание плана.

   Теперь основание снимается при удалении любой известной структурной зависимости:
   - того же набора, что у основания исхода ребёнка в 3.1 — ревизии ребёнка с входами и
     выходами, история плана родителя с основанием, входами и листами, вышестоящие дети;
   - и собственных адресов записи.

   Зависимые записи находятся до изменений удаляющей транзакции. Вместе с основанием
   снимаются квитанция и повтор, управляемые копии загрязняются и удаляются. Адресный
   audit, исходы и независимые записи сохраняются; последовательные удаления после
   перезапуска находят записи через сохранённый индекс.
2. Уточнение контракта `premise_changed`: код и адреса сохраняются через составные и
   вложенные условия, пока это блокирующая причина. Истинная альтернатива `any` проходит,
   реально ожидающая — `dependency_open`, закрытые альтернативы и `all` с закрытым членом
   — `dependency_closed`. В сочетании с `decision_conflict` первым называется конфликт.
   Семантика `stale_basis` не менялась.
3. При проверке всего пакета найден дефект того же класса в общем правиле 3.3–3.4. Он
   воспроизведён до исправления и исправлен:
   - основание решения применимости или waiver, записанное до редакции плана,
     переживало удаление Artifact, которое новая ревизия уже не называла;
   - теперь в schema 7 ревизия обязательства зависит и по ревизии плана, действовавшей
     при её записи;
   - для подтверждений она всегда текущая, поэтому правило прохода 1 для них прежнее.

Расхождение `stale_basis` внутри `any` и наблюдение `database is locked` остаются
открытыми. Остановка для одного общего review. Часть 3.6, реальные модели, миграция
разработки и PR не начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `c11d54f` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

- Воспроизведения одним скриптом вне репозитория через публичный Core:
  - P1, оба варианта, на `c11d54f`: маркер в `read_revalidation` (и в новом процессе),
    закрытой SQLite и новом backup, повтор и `read_receipt` доступны,
    `live_store_sanitized=true`;
  - на `39f6cde` маркера нет, повтор — `history_unavailable`, `read_receipt` —
    `not_found`;
  - так же до и после — дефект истории плана и составные условия.
- Новые регрессии:
  - `test_revalidation.py` +7 — P1 с тремя вариантами цитируемой предпосылки, вышестоящий
    Artifact и вышестоящий ребёнок, последовательные удаления после перезапуска с
    независимой записью, составные условия;
  - `test_waivers.py` +1 — основания, записанные до редакции плана.
- Все 8 новых тестов падают на снимке `c11d54f` (`git archive` с новыми файлами тестов),
  16 прежних тестов этих модулей там проходят.
- Семь временных поломок новых правил ловятся тестами. Исходники восстановлены
  побайтно.

Проверки на коде `39f6cde`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 623 прошли, 1 упал,
  149,96 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 179 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` на чистом дереве — hygiene, структура
  отчёта, Ruff format (205 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows; исправление эти файлы не меняло.
  `uv build --no-sources` отдельно собрал sdist и wheel.
- Probe без изменений, на чистом дереве:
  - `tools.probe_stage6_rpc` — `passed`, 3 HTTP;
  - `tools.probe_install_stage6` — wheel SHA-256
    `144634AAF24D93A9183A1EB10D686D51FF996DCC62CDA582C4D206145F3AD2A4`, новый venv вне
    checkout, `source_commit` `39f6cde`, `source_tree_dirty: false`, `passed`, 3 HTTP;
  - `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
    restore в эпоху 2, санация при удалении.
- Сверка отчётов с прогонами на `b6b66f1` после маскировки: Stage 5 совпадает полностью,
  Stage 6 отличается только выборочным переходным состоянием `running:stop_requested`.
  Отчёты — в игнорируемом `_scratch/stage6-pass3-345fix-*`.

Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Порядок «конфликт раньше `premise_changed`» уточнение не задаёт. Он выбран по правилу
  3.3 для `all`, а оба адреса остаются в производном состоянии.
- В `all` изменившаяся предпосылка, как и конфликт, называется раньше отказа первого члена,
  в том числе `stale_basis`. Условие только со `stale_basis` называет его, как прежде.
- Структурные зависимости основания перепроверки взяты из принятой в 3.1 модели оснований
  исходов. Цитата без сохранённой структурной связи по-прежнему не покрыта.
- Результат Windows-проверки `c11d54f` передал владелец; эта сессия саму проверку не
  наблюдала.

## cuts

- Расхождение `stale_basis` внутри `any` не менялось: известный вопрос владельца.
- Наблюдение `database is locked` не исследовалось и остаётся открытым для прохода 4.
- Часть 3.6 и дальше, реальные модели, миграция разработки и PR не начинались.
  Windows-прогон здесь невозможен; `tools.check --deliver` на Linux останавливается на
  mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было.

## manual-acceptance

Нужен один общий review пакета 3.4–3.5 вместе с этим исправлением. Он не является
приёмкой частей, schema 7 или прохода 3. Обязательный полный gate — независимый
Windows-прогон, результат которого передаёт владелец; команды — в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.4-3.5.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — пакет 3.4–3.5: waiver по адресному исключению и перепроверка принятых результатов

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`d251604e020dfc2149685e678b9c98cc21c30563`:

- полный `tools.check --deliver` — PASS: 600 тестов, типы, Ruff, 21 импортный контракт,
  sdist и wheel;
- исходный независимый сценарий подтвердил исправление P2;
- Pi-probe на этом коммите Codex повторно не запускал.

По этому результату владелец закрыл техническую проверку контрольной точки 3.3. Это не
приёмка schema 7, прохода 3 или Stage 6. Владелец разрешил один пакет: части 3.4 и 3.5
последовательно по `STAGE6-PASS3-PLAN.md`, с правами, повторами, backup/restore и
удалением. Пакет реализован как контрольная точка разработки (`b6b66f1`). Документ —
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.4-3.5.md`.

- 3.4. Исключение — третий вариант Decision: одно требование (родитель и ключ
  обязательства), точные версии Method как пределы, формулировка; правилом доступа не
  является. `waive_obligation` (`work.accept` + `method.use`) записывает `waived` отдельно от
  `satisfied`, с адресом исключения и без доказательства:
  - ссылка не на исключение, даже при широком Grant, — `exception_required`, чужая цель
    или пределы — `exception_mismatch`, `inactive`/`unresolved` — отказ с состоянием;
  - waiver не открывает зависимости по выходу роли;
  - принятие перепроверяет каждое исключение (`stale_basis`) и называет снятые требования
    в результате, `read_work`, `CompositionView` и своём состоянии;
  - отзыв исключения даёт `waiver_stale`; путь восстанавливают подтверждение точным
    результатом или новое исключение;
  - удаление зависимости роли снимает waiver, а адресная применимость сохраняется.
- 3.5. В schema 7 принятый результат ребёнка с изменившейся собственной предпосылкой
  остаётся `succeeded` и читается, но не интегрируется:
  - выдача зависимых, их эффекты, подтверждение, связь выхода родителя и принятие отказывают
    `premise_changed` с адресами «удержанная → текущая ревизия»;
  - выполненное обязательство не переоткрывается;
  - `revalidate_result` (`work.accept` + `method.use`) принимает только ровно изменившиеся
    предпосылки и их текущие ревизии (`premise_mismatch`, `wrong_work`, `stale_basis`);
  - запись в новой таблице schema 7 `result_revalidations` допускает интеграцию, пока
    названные ревизии текущие.

Известные вопросы остаются открытыми. Расхождение `stale_basis` внутри `any` не менялось и
не считается исправленным. `premise_changed` внутри составных условий следует тем же
прежним правилам; это тот же класс вопроса, решение — за владельцем. Наблюдение `database
is locked` остаётся открытым для прохода 4.

Остановка для общего review. Часть 3.6, реальные модели, миграция разработки и PR не
начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `d251604` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

- Новые регрессии: `test_waivers.py` — 8 тестов, `test_revalidation.py` — 8 (один
  параметризован по удалению предпосылки, выхода и ребёнка). Они покрывают перечни
  «Проверяемый результат» и «Обслуживание данных» частей 3.4 и 3.5, включая пример W22
  (C@7 → C@8), backup → restore → recover, удаление с отсутствием маркеров в live SQLite и
  новом backup, новый процесс.
- Один прежний тест изменён. В `test_failed_branch_is_reviewed_while_independent_branches_go_on`
  (3.2) после ревизии общего источника родитель дополнительно называет `premise_changed(a1)`.
  Это изменение 1 раздела «Изменения принятого поведения» плана. Тесты проходов 1–2 и
  частей 3.1, 3.3 не менялись.
- Временные поломки: пять из шести ловятся тестами. Шестая эквивалентна при нынешних
  операциях: выходы принятого Work не меняются. Исходники восстановлены побайтно.
- Скриптами вне репозитория проверены `premise_changed` в `any`/`all` (прежние правила) и
  отказ `unsupported_schema` для пространства schema 7 с прежней записью DDL.

Проверки на коде `b6b66f1`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 615 прошли, 1 упал,
  148,85 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 179 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` на чистом дереве — hygiene, структура
  отчёта, Ruff format (205 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows; пакет эти файлы не менял.
  `uv build --no-sources` отдельно собрал sdist и wheel.
- Probe без изменений, на чистом дереве:
  - `tools.probe_stage6_rpc` — `passed`, 3 HTTP; его удаление делает upgrade до schema 7 с
    новой таблицей;
  - `tools.probe_install_stage6` — wheel SHA-256
    `8D76E9D4220A8A7687428CC3DA5D52498B07227F0FB463C508A3CD9D102406E0`, новый venv вне
    checkout, `source_commit` `b6b66f1`, `source_tree_dirty: false`, `passed`, 3 HTTP;
  - `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
    restore в эпоху 2, санация при удалении.
- Сверка отчётов с прогонами на `29e53cc` после маскировки id, дайджестов, времени и
  маркеров: Stage 5 совпадает полностью. Stage 6 из checkout и из wheel отличается только
  выборочным переходным состоянием `running:stop_requested`. Отчёты — в игнорируемом
  `_scratch/stage6-pass3-345-*`.

Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Пределы исключения — множество точных версий Method; waiver требует закреплённую версию
  родителя. Список оставляет 3.8 возможность исключения, допускающего v1 и v2.
- «Точный выход» перепроверки — все связанные выходы принятой ревизии ребёнка.
- Пары перепроверки называют удержанную ревизию исходной предпосылки и её текущую
  ревизию (`X@1 → X@3` после второй ревизии). Действует последняя запись, и только пока
  она совпадает с текущими изменениями.
- Предпосылки прямые: изменившаяся предпосылка предшественника не делает результат
  зависимого `premise_changed`, пока его собственные входы и листы актуальны.
- В schema 5–6 `premise_changed` не действует: там нет записи перепроверки, и прежние пути
  сохраняются до явного upgrade.
- Результат Windows-проверки `d251604` передал владелец; эта сессия саму проверку не
  наблюдала.

## cuts

- Замена узла (3.6) — второй выход из `premise_changed` — не реализована по границе пакета.
- Видимость waiver, `premise_changed` и перепроверок в Pi — часть 3.10; Pi-адаптер не
  менялся.
- Составные условия для `premise_changed` и `stale_basis` не менялись: вопрос владельца.
- Наблюдение `database is locked` здесь не исследовалось и остаётся открытым.
- Windows-прогон здесь невозможен; `tools.check --deliver` на Linux останавливается на
  mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен общий review пакета 3.4–3.5. Он не является приёмкой частей, schema 7 или
прохода 3, а часть 3.6 требует отдельного разрешения. Обязательный полный gate —
независимый Windows-прогон, результат которого передаёт владелец; команды — в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.4-3.5.md`. Вопрос владельцу: нужно ли
`premise_changed` (и `stale_basis`) внутри `any`/`all` особое старшинство, как у
формального конфликта; сейчас действуют прежние правила.

## next

solmax

# Core v0.1 Stage 6, проход 3 — исправление 3.3 по review Windows-проверки `05e089e`

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`05e089ed47a3a7ec6bac556c017f2762c97984b2`:

- полный `tools.check --deliver` — PASS, 598 тестов;
- Stage 6 Pi-probe из checkout и из установленного wheel — PASS, по 3 HTTP.

Техническая проверка 3.3 не закрыта. Владелец поручил исправить одно замечание P2:
`_evaluate` в `composition.py` (строки 582–588) — `any` превращал `decision_conflict` в
`dependency_open`. Переданное воспроизведение через публичный Core:

- выбор D@1 уровня Activity `art_review = required`;
- готовность ребёнка `art` — `any(decision_value(D@1, art_review, required),
  decision_value(D@1, art_review, not_required))`;
- выбор E@1 уровня родительского Work `art_review = not_required`.

`read_work_status` и `read_execution.status`, в том числе в новом процессе, показывали
`blocked:decision_conflict` с обоими адресами, а `IssueChildWorkRequest` отказывал
`dependency_open`. Пространство не менялось; после отзыва E выдача проходила.

Исправление `29e53cc`. `_evaluate` сохраняет `decision_conflict` с адресами каждого
конфликтующего члена через составные условия, в том числе вложенные:

- `any`: выполненная альтернатива по-прежнему выполняет условие. Член, который ещё может
  стать истинным (не закрыт и не в конфликте), по-прежнему даёт `dependency_open`. Если
  кроме конфликта все члены закрыты — `decision_conflict`; если закрыты все —
  `dependency_closed`.
- `all`: закрытый член по-прежнему даёт `dependency_closed`; иначе конфликт называется
  раньше первого отказа, даже при открытом члене.

Для условий без конфликта результат прежний.

Прежнее наблюдение `database is locked` остаётся открытым: успешные текущие прогоны
probe не доказывают его исправления.

Остановка для review. Часть 3.4, реальные модели, миграция разработки и PR не
начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `05e089e` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

- Переданный сценарий воспроизведён тем же порядком шагов одним скриптом через
  публичный Core (scratchpad вне репозитория). На `05e089e` — ровно как передано. На
  `29e53cc` выдача отказывает `decision_conflict` с адресами D@1 и E@1, пространство не
  меняется, после отзыва E выдача проходит.
- Две новые регрессии в `tests/zaratustra/foundation/test_applicability.py` (теперь 15):
  - `test_formal_conflict_keeps_its_code_through_composite_conditions` — переданный
    случай и вложенные `all`/`any`: одинаковые адресные причины в `read_work_status`,
    `read_execution` и новом процессе; отказы `decision_conflict` без записей; прежние
    `dependency_closed` и `dependency_open`; выполненная альтернатива выдаётся; после
    отзыва узлы выдаются;
  - `test_formal_conflict_in_a_nested_completion_refuses_acceptance` — тот же разрыв на
    пути принятия через `any(dropped, all(B, decision_value))`.
- Обе падают на снимке `05e089e` (`dependency_open`), остальные 13 проходят и там.
  Каждое из четырёх правил старшинства проверено временной поломкой, после которой падает
  хотя бы один тест; исходники восстановлены побайтно.
- `git diff 05e089e 29e53cc` затрагивает только `composition.py` и файл тестов.

Проверки на коде `29e53cc`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 599 прошли, 1 упал,
  119,84 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 175 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` на чистом дереве — hygiene, структура
  отчёта, Ruff format (201 файл) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- Probe без изменений, на чистом дереве:
  - `tools.probe_stage6_rpc` — `passed`, 3 HTTP;
  - `tools.probe_install_stage6` — wheel SHA-256
    `849C43E559B7CDF25B3A23F8E07D66FB488350FD28090E5AC504A5B0F24E2EF4`, новый venv вне
    checkout, `source_commit` `29e53cc`, `source_tree_dirty: false`, `passed`, 3 HTTP;
  - `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
    restore в эпоху 2, санация при удалении.
- Отчёты совпадают с прогонами на `a52ee22` с точностью до id, времени, маркеров и
  выборочного переходного состояния `running:stop_requested`. Отчёты — в игнорируемом
  `_scratch/stage6-pass3-33fix-*`.

Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Член `any`, который не закрыт и не в конфликте, считается ещё способным стать истинным,
  как в правиле 3.1. Тогда отказ — `dependency_open`, а конфликт — одна из причин в
  чтениях.
- В `all` закрытый член важнее конфликта: конъюнкция окончательно ложна при любом
  выборе.
- Если в одном условии несколько конфликтов, отказ перечисляет адреса каждого.
- Родственный случай до 3.3 не менялся. `any(лист stale_basis, закрытый член)` читается
  `blocked:stale_basis`, а операция отказывает `dependency_open`, как требует правило 3.1
  «иначе, как прежде, `dependency_open`». Здесь это воспроизведено на `05e089e`. Менять
  это правило — решение владельца.
- Результат Windows-проверки `05e089e` и замечание передал владелец; эта сессия саму
  проверку не наблюдала.

## cuts

- Случай `stale_basis` внутри `any` не менялся (правило 3.1, решение владельца).
- Наблюдение `database is locked` здесь не исследовалось и остаётся открытым.
- Часть 3.4 и дальше, отдельный срез, проход 4, PR, реальные модели и миграция разработки
  не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на Linux
  останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен review исправления контрольной точки 3.3. Он не является приёмкой части 3.3,
schema 7 или прохода 3, а часть 3.4 требует отдельного разрешения. Обязательный полный
gate — независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.3.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — контрольная точка 3.3: применимость обязательств по адресному выбору

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`c554cf8290ac84e3144d217e739a8c7bfd4026ed`:

- исправление готовности подтверждено прежним публичным воспроизведением, включая
  `read_execution` и новый процесс;
- полный `tools.check --deliver` — PASS: 585 тестов, 21 импортный контракт, типы, стиль и
  сборки;
- первый прогон Pi-probe из checkout завершился `FoundationError(storage: database is
  locked)`, стек `assigned._record_stop` → `read_execution` → `space_connection` →
  `_space_info` (`PRAGMA application_id`). Остались Attempt `active`, назначение
  `assigned` и один вызов `prepared`, вопрос не сохранён;
- один повтор после завершения gate, в новом каталоге, прошёл с 3 localhost HTTP; probe
  из установленного wheel — тоже 3 HTTP.

Причина первого сбоя не установлена. По поручению владельца он записан отдельным открытым
наблюдением для аварийной матрицы прохода 4 (`STAGE6-PASS3-CHECKPOINT-3.2.md`,
`STAGE6-PASS3-PLAN.md`) и не назван исправленным или ошибкой среды. С этим наблюдением
владелец закрыл техническую проверку контрольной точки 3.2. Это не приёмка schema 7,
прохода 3 или Stage 6.

Затем владелец разрешил только часть 3.3. Она реализована в `a52ee22` как контрольная
точка (`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.3.md`):

- **Адресные выборы.** Decision получил вариант «выбор»: имя, значение и одна область
  (Work или Activity). Он пишется прежними операциями Decision по праву `decision.write`.
  Проверка доступа учитывает только правила. Ревизия не меняет вариант, имя и область
  выбора. `read_decision` читает точные ревизии.
- **Применимость.** Условное обязательство Method материализуется `unresolved`.
  `resolve_obligation_applicability` (`work.write` и `method.use`) после проверок выбора,
  области, значения и конфликта записывает `active/open` или `inactive` с адресом выбора.
- **Конфликты.** К Work применимы действующие выборы, чья область охватывает сам Work, его
  составного родителя или Activity. Разные значения — формальный `decision_conflict` с
  адресами всех выборов; ни область, ни время не дают старшинства.
- **Перепроверки.** Выдача узла с листом `decision_value`, эффекты ребёнка, подтверждение,
  связь выхода и принятие родителя заново вычисляют применимые выборы. Записанная ревизия
  не переписывается. После пересмотра или отзыва указанного выбора экземпляр читается
  `applicability_stale`, принятие отказывает `stale_basis`.
- **Производные состояния.** `ready` с `applicability_pending` или
  `applicability_stale` (ключ и имя), `blocked:decision_conflict` со всеми адресами.
- **Обслуживание.** Выборы — обычные Decision; операции удаления Decision нет со Stage 2,
  этот предел сохранён. Ревизии применимости — ревизии обязательств: точное чтение,
  backup/restore, удаление вместе с родителем. При удалении зависимости санация снимает
  основание решения и сохраняет адрес выбора.

Остановка для review. Часть 3.4, реальные модели, миграция разработки и PR не
начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `c554cf8` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

- Новые регрессии — `tests/zaratustra/foundation/test_applicability.py`, 13 тестов. Они
  покрывают каждый пункт проверяемого результата 3.3 в плане и обслуживание данных:
  backup, карантинный restore, удаление зависимости и родителя, новый процесс.
- Восемь намеренных временных поломок (проверка применимости при подтверждении,
  перепроверка связи выхода, охват Activity, старшинство Work, устаревание решения,
  закрытие листа, `obligation_unresolved`, `applicability_pending`) валят хотя бы один
  новый тест; исходники восстановлены побайтно. На снимке `c554cf8` модуль тестов падает
  при сборке: контракта 3.3 там нет.
- `git diff c554cf8 a52ee22`: `__init__.py`, новый `choices.py`, `composition.py`,
  `models.py`, `operations.py`, `work_status.py` в `zaratustra.foundation` и новый файл
  тестов. Прежние тесты не менялись.

Проверки на коде `a52ee22`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 597 прошли, 1 упал,
  127,63 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux. Тест закреплённых констант прохода 2 проходит без изменений.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 175 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` на чистом дереве — hygiene, структура
  отчёта, Ruff format (201 файл) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- Probe без изменений, на чистом дереве:
  - `tools.probe_stage6_rpc` — `passed`, 3 HTTP;
  - `tools.probe_install_stage6` — wheel SHA-256
    `4C4C62D739010F7ED3C70265CE73A59E8E37B90D119D3C975CE4ED0A0BCB3871`, новый venv вне
    checkout, `source_commit` `a52ee22`, `source_tree_dirty: false`, `passed`, 3 HTTP;
  - `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
    restore в эпоху 2, санация при удалении. Первый запуск был без обязательного
    `--pi-cli` и остановился на разборе аргументов; повтор прошёл.
- Отчёты Stage 6 совпадают с прогонами 3.2 с точностью до id, времени, маркеров и
  выборочного переходного состояния `running:stop_requested`. Отчёты — в игнорируемом
  `_scratch/stage6-pass3-33-*`.

Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Выбор сохраняет имя и область; ревизия меняет значение, формулировку и статус.
- Каждое новое решение начинает исполнение с `open` («active/open» плана). Повторное
  решение возможно, только если экземпляр `unresolved` или его выбор уже не действует;
  иначе `applicability_resolved`.
- Подтверждение сначала отказывает по состоянию экземпляра (`obligation_unresolved`,
  `obligation_inactive`, устаревшее решение — `stale_basis`), затем по конфликту.
  Принятие сначала проверяет конфликт любого условного обязательства, затем устаревшие
  решения, затем закрытые ветви, `obligation_unresolved` и `obligation_open`.
- Связь выхода проверяет конфликт только у решённых обязательств: нерешённые не мешают
  независимой работе. Конфликт у родителя читается `blocked`, как устаревшая предпосылка,
  с адресами активных ветвей и разбора.
- Лист `decision_value` вычисляется для Work, чьё это условие. Он может указывать выбор,
  область которого этот Work не охватывает. План требует существующую точную ревизию
  выбора с этим именем.
- Основание решения — `basis` его ревизии обязательства. Поэтому санация прохода 1 при
  удалении зависимости снимает его как скопированное основание и сохраняет адрес выбора;
  полное правило — в 3.4.
- Результат Windows-проверки `c554cf8` и наблюдение передал владелец; эта сессия саму
  проверку не наблюдала.

## cuts

- Waiver, исключения и полное правило удаления зависимости для 3.3–3.4 — часть 3.4.
- Применимость с адресом Decision в Pi — часть 3.10; Pi-адаптер не менялся.
- Операции удаления Decision нет (предел Stage 2 сохранён).
- Открытое наблюдение `database is locked` здесь не исследовалось; оно в аварийной матрице
  прохода 4.
- Часть 3.4 и дальше, отдельный срез, проход 4, PR, реальные модели и миграция разработки
  не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на Linux
  останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен review контрольной точки 3.3. Он не является приёмкой части 3.3, schema 7 или
прохода 3, а часть 3.4 требует отдельного разрешения. Обязательный полный gate —
независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.3.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — исправление готовности 3.2 по Windows-проверке `4b65fed`

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`4b65fedaa58a89d051da2fbecdab3f7986d29ae7`: `tools.check --deliver` — PASS (582 теста,
21 контракт); Stage 6 probe из checkout и из установленного wheel — PASS, по 3
синтетических localhost HTTP.

Перед закрытием 3.2 владелец поручил исправить одно расхождение готовности и передал его
воспроизведение через публичный Core:

- Method с `obligations=()`, независимые A1 и A2, условие завершения `any(A1, A2)`;
- выход родителя привязан к A1;
- A2 явно закрыт как `failed`;
- A1 выдан, его результат отдельно принят и связан с родителем.

`read_work_status` и `read_execution` показывали только `ready:branch_review(a2)`, в
том числе в новом процессе, а `AcceptWorkRequest` родителя проходил. Дефект унаследован:
на `d07ffa5` тот же сценарий читался `blocked:obligation_open`.

Исправление `6dd83e3`. Производное состояние спрашивает структурные правила принятия,
как только выполнены все объявленные обязательства (их может не быть), а не после успеха
всех детей. В этом сценарии родитель теперь `ready` с `acceptance_pending` и сохранённым
`branch_review(a2)`. Сохранены:

- приоритет устаревшей предпосылки и фаз `waiting`/`running`;
- отдельная операция принятия с проверкой текущих прав;
- отказы при невыполненных условиях.

Остановка для review. Часть 3.3, реальные модели, миграция разработки и PR не
начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `4b65fed` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

Воспроизведение шло одним скриптом через публичный Core (scratchpad вне репозитория):

- на `4b65fed` — `ready [branch_review(a2)]` в `read_work_status`, `read_execution` и
  новом процессе, принятие `succeeded`;
- на снимке `d07ffa5` — `blocked [obligation_open]` во всех трёх чтениях, принятие
  `succeeded`;
- на `6dd83e3` — `ready [acceptance_pending, branch_review(a2)]` во всех трёх чтениях,
  принятие `succeeded`.

Новые регрессии в `tests/zaratustra/foundation/test_parallel_branches.py` (теперь 9):

- переданный сценарий: до связи выхода `output_link_pending` и отказ `output_mismatch`;
  после связи `acceptance_pending` в `read_work_status`, `read_execution` и новом
  процессе; субъект только с `record.read` — `permission_denied`, родитель остаётся
  `proposed`; принятие владельцем проходит;
- при исполняющемся и затем ждущем A2 родитель остаётся `running`, затем `waiting`, и
  только после `failed` у A2 читается `acceptance_pending`;
- без выжившей ветви ожидаемого принятия нет, принятие отказывает `dependency_closed`.

Первые два падают на `4b65fed`; третий проходит и там и защищает от ложного
`acceptance_pending`.

Проверки на чистом дереве `6dd83e3`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 584 прошли, 1
  упал, 124,58 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 173 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (199 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- `tools.probe_stage6_rpc` без изменений — `passed`, 3 HTTP; отчёт совпадает с прогоном на
  `ec38910` с точностью до id, времени, маркеров и выборочного переходного состояния B.
- `tools.probe_install_stage6` — wheel SHA-256
  `74DE9DC5204E06CCFCB4C1BDC890B5E8430969B53C21D968F84C58DF1E39F0E3`, новый venv вне
  checkout, `source_commit` `6dd83e3`, `source_tree_dirty: false`, `passed`, 3 HTTP.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении.
- Отчёты — в игнорируемом `_scratch/stage6-pass3-32fix-*`.

`git diff 4b65fed 6dd83e3` затрагивает только `work_status.py` и
`test_parallel_branches.py`. Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Правила ожидаемого принятия — ровно структурные проверки операции принятия
  (`check_parent_acceptance` без субъекта). Если они выполнены, показывается только
  `acceptance_pending` с разбором ветвей, как у родителя с выполненными обязательствами;
  следующие шаги других детей в этом состоянии не перечисляются.
- Если в итоговом `blocked` у родителя без обязательств нет заблокированных детей,
  причиной теперь называется фактический отказ принятия, а не `obligation_open`.
- Результат Windows-проверки `4b65fed` и воспроизведение передал владелец; эта сессия
  саму проверку не наблюдала.

## cuts

- Поведение родителя с объявленными обязательствами не менялось.
- Одновременный живой запуск двух Pi RPC-процессов по-прежнему относится к 3.10, окна
  сбоев — к проходу 4.
- Часть 3.3 и дальше, отдельный срез, проход 4, PR, реальные модели и миграция
  разработки не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на
  Linux останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен review исправления контрольной точки 3.2. Он не является приёмкой части 3.2,
schema 7 или прохода 3, а часть 3.3 требует отдельного разрешения. Обязательный полный
gate — независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.2.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — контрольная точка 3.2: независимые дети и разбор ветви

## outcome

Владелец передал результат независимой Windows-проверки, которую выполнил Codex на
`d07ffa579dc92c6dd5871ee525a3a9b307732309`:

- полный `tools.check --deliver` — PASS: 575 тестов, 21 импортный контракт;
- Stage 6 Pi-probe из checkout и из установленного wheel — PASS, по 3 localhost HTTP;
- отдельно проверены прежние дефекты, историческое пространство старой схемы и
  сохранение позднего основания.

По этому результату владелец закрыл техническую проверку контрольной точки 3.1; это не
приёмка schema 7, прохода 3 или Stage 6. Правило позднего основания оставлено.
Комментарии уточнены в `ee3bf6c`: порядок операций определяет ретроспективную санацию, но
не доказывает невозможность копирования удалённого текста.

Затем владелец разрешил только часть 3.2. Она реализована в `ec38910` как контрольная
точка (`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.2.md`):

- **Параллельные дети.** Независимые дети одного родителя исполняются одновременно, каждый
  через свою Attempt и свой ресурс. Повтор доставки launch даёт один DBOS workflow на
  Attempt.
- **Исключительный ресурс.** Прежнее правило Stage 5 закреплено для детей: пока корень
  держит действующая Attempt (в том числе с незаписанной остановкой) или неизвестный
  исход, другое назначение — `resource_busy` без записей; другой корень продвигается.
- **Адресные ветви.** Приоритет фаз прохода 2 сохранён. Причины родителя называют все
  активные ветви и каждую закрытую ветвь, на которую ещё ссылается план или открытое
  обязательство (`branch_review`), с ролью и адресом Work. Если ничто не ждёт и не
  исполняется, неуспешная ветвь делает родителя `ready` для разбора, а не `blocked`.
- **Зависимая интеграция.** Выдача зависимого, подтверждение обязательства ветви, связь
  выхода, привязанного к ней, и принятие родителя отказывают `dependency_closed` с её
  адресом. Независимые ветви не отменяются.
- **Обслуживание.** Новых записей и DDL нет. Backup с двумя назначениями восстанавливается
  как история, удаление одной ветви не трогает соседа и обязательства.

Одна проверка теста контрольной точки 3.1 изменена под уточнённый контракт 3.2: родитель
с закрытой нужной ветвью теперь `ready:branch_review`, а не `blocked:dependency_closed`.
Остановка для review. Часть 3.3, реальные модели, миграция разработки и PR не
начинались, приёмка не записана.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `d07ffa5` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

Новые регрессии:

- `tests/zaratustra/foundation/test_parallel_branches.py`, 6 тестов: одновременный
  запуск и один исключительный корень; разбор `failed` ветви при продвижении D; условие
  `any`; выход, привязанный к закрытой ветви; backup/restore двух назначений; удаление
  одной ветви.
- `tests/zaratustra/pi_adapter/test_child_assigned.py`: два ребёнка, одна DBOS workflow на
  Attempt при повторной доставке, адресная очистка при удалении одного.

Четыре первых теста нового модуля падают на исходниках `ee3bf6c` (тот же файл тестов на
снимке `git archive`): фаза `waiting` называла одного ребёнка, родитель читался
`blocked`, `branch_review` не было, связь выхода закрытой ветви проходила. Тесты
backup/restore, удаления и доставки проходят и там: они закрепляют существовавшее
поведение для параллельных детей.

Проверки на чистом дереве `ec38910`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 581 прошли, 1
  упал, 118,55 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 173 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (199 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- `tools.probe_stage6_rpc` без изменений — `passed`, 3 HTTP. Отчёт совпадает с прошлым
  прогоном с точностью до id, времени, маркеров и одного выборочного переходного
  состояния B.
- `tools.probe_install_stage6` — wheel SHA-256
  `87A36E245BB94BEC99698CE914C089BB5A265D2DC5A8DB5B5F4ACEF8849C9D55`, новый venv вне
  checkout, `source_commit` `ec38910`, `source_tree_dirty: false`, `passed`, 3 HTTP.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении.
- Отчёты — в игнорируемом `_scratch/stage6-pass3-32-*`.

`git diff d07ffa5 ec38910` затрагивает `composition.py` (включая уточнённый docstring),
`work_status.py`, новый `test_parallel_branches.py`, `test_work_outcomes.py`,
`test_child_assigned.py`, комментарий в `test_outcome_bases.py` и
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.1.md`. Итоговый коммит меняет только
Markdown-файлы, включая `src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- «Ссылается текущий план» — роль закрытого ребёнка есть среди детей текущего плана;
  «открытое обязательство» — последняя ревизия обязательства этой роли в состоянии
  `open` (в 3.2 все обязательства применимы).
- Причины добавляются к каждому состоянию родителя после основных, в том числе к
  `blocked` по устаревшей предпосылке и к `acceptance_pending`.
- Связь выхода отказывает только тогда, когда сама привязанная роль закрыта; выход,
  привязанный к ребёнку, который зависит от закрытой ветви, отказывает при принятии.
- Результат Windows-проверки `d07ffa5` передал владелец; эта сессия её не наблюдала.

## cuts

- Одновременный живой запуск двух Pi RPC-процессов не выполнялся; живой сценарий
  параллельных ветвей — часть 3.10, окна сбоев — проход 4.
- Представление ветвей в Pi не менялось (3.10); `/zara-status` уже печатает причины как
  `код:роль`.
- Разбор ветви — производное состояние. Замена неуспешного узла появится только с
  редакцией плана (3.6).
- Часть 3.3 и дальше, отдельный срез, проход 4, PR, реальные модели и миграция
  разработки не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на
  Linux останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен review контрольной точки 3.2. Он не является приёмкой части 3.2, schema 7 или
прохода 3, а часть 3.3 требует отдельного разрешения. Обязательный полный gate —
независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.2.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — порядок для старых схем по review `f13ffa8`

## outcome

Владелец передал результат Windows-review `f13ffa8`: 572 теста и полный
`tools.check --deliver` прошли, прежние замечания по санации на schema 7 технически
закрыты, явный upgrade проверен независимо. Для schema 2–6 владелец выбрал порядок и
разрешил узкое изменение поведения. Оно реализовано в `7694d9d`:

- **Отказ до изменений.** На schema 2–6 удаление Artifact или Work, после которого
  пришлось бы снять основание `WorkAcceptance`/`WorkClosure` другого Work, отклоняется
  `upgrade_required`. Проверка идёт в общем gate после прав и до любой записи. Текст
  отказа называет удаляемый адрес и каждый зависимый Work. Схема сама не повышается; тот
  же запрос после явного upgrade выполняется один раз. Удаления без зависимых оснований
  идут как прежде.
- **Удержание при чтении.** Основание, которое оставило прежнее удаление, чтение Work
  возвращает как `basis = null`, не переписывая байты и не мигрируя пространство. Исход,
  id операции, источник полномочия, производное состояние и адресный audit сохраняются.
  Повтор и `read_receipt` этой операции отвечают `upgrade_required`, чтобы квитанция не
  подтверждала текст.
- **Незавершённое обслуживание.** При таких основаниях `complete_deletions` ничего не
  меняет. Он возвращает `live_store_sanitized = false`, `completed_at = null`, адреса
  Work в `retained_bases` и `upgrade_required = 7`.
- **Правило.** Основание удержано, если его структурная зависимость удалена после записи
  исхода — именно его schema 7 сняла бы в транзакции удаления. Upgrade 6 → 7 теперь
  снимает ровно такие основания. Основание, записанное после удаления, остаётся: в
  `f13ffa8` upgrade снимал и его.
- **После upgrade и `complete_deletions`** действуют прежние гарантии: основание
  недоступно, квитанции и повтор недоступны, пакеты очищены, `succeeded`/`failed`/
  `cancelled`/`stale` сохранены.

Пять прежних тестов и `tools.probe_stage6_rpc` точечно переведены на этот порядок: сначала
отказ без изменений, затем явный upgrade и то же удаление. Проверка B как `succeeded`,
проверки прав, истории, повторов и числа HTTP сохранены.

Цитирование материала без сохранённой структурной связи записано как непокрытый случай
среза; очистка произвольных неадресованных копий не заявляется. DDL и checksum schema
1–7 не менялись. Часть 3.2 не начата, приёмка не записана. PR, реальных моделей и
миграции разработки не было.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `f13ffa8` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

Новые тесты в `tests/zaratustra/foundation/test_outcome_bases.py` (теперь 10):

- schema 2: отказ `upgrade_required` с адресами только после права на удаление,
  пространство не изменилось; после upgrade 2 → 7 тот же запрос снимает основание;
- schema 2, прежнее удаление: удержание при чтении (и в новом процессе), отчёт
  `complete_deletions` без изменений на каждой схеме 2–6, upgrade без
  `maintenance.delete` откатывается, после upgrade владельца и обслуживания пакеты
  удалены и маркера нет;
- schema 6, составной Work: удаление A требует upgrade ради B, удаление выхода A — ради
  A и B; прежнее удаление удерживает оба, upgrade снимает, B остаётся `succeeded`;
- основание, записанное после удаления, не удерживается и не снимается; upgrade с одним
  `maintenance.backup` проходит.

Прежнее удаление моделируется явной подменой `require_outcome_upgrade` на одну операцию.
Все четыре теста падают на исходниках `f13ffa8` (тот же файл тестов на снимке
`git archive`): удаление не отклонено, основание читается с маркером, upgrade снимает
позднее основание и требует `maintenance.delete`. Пять изменённых прежних тестов тоже
падают там, потому что удаление не отклонено.

Проверки на чистом дереве `7694d9d`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 574 прошли, 1
  упал, 113,81 с. Падение — `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`,
  ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 172 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (198 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- `tools.probe_stage6_rpc` — `passed`, 3 HTTP. На schema 6 записаны отказ с зависимыми
  A, B и родителем, неизменное пространство и upgrade до 7. После удаления все трое
  `succeeded` со снятыми основаниями, в новом процессе B `succeeded`. В
  восстановленном пространстве тот же порядок. Прочие поля отчёта совпадают с прошлым.
- `tools.probe_install_stage6` — wheel SHA-256
  `1052F85992592DD04F3C1CE9006850B9A3F30D0B0DC16270B173DB637977D885`, новый venv вне
  checkout, `source_commit` `7694d9d`, `source_tree_dirty: false`, `passed`, 3 HTTP.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении (schema 4 удаляет только простые Work).
- Отчёты — в игнорируемом `_scratch/stage6-pass3-31old-*`.

`git diff f13ffa8 7694d9d` затрагивает `composition.py`, `models.py`, `operations.py`,
четыре тестовых модуля и `tools/probe_stage6_rpc.py`. Итоговый коммит меняет только
Markdown-файлы, включая `src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Порядок операций исхода и удаления задают номера ревизии пространства у операций;
  строки операций и ревизий удаления сохраняются при удалении.
- Удержание касается `read_work` и чтений через него (`read_execution`, мост Pi).
  Квитанция содержит fingerprint запроса с текстом основания, поэтому её повтор и чтение
  тоже удерживаются.
- При удержанных основаниях `complete_deletions` не выполняет частичного обслуживания,
  включая техническую очистку и пакеты других удалений. Выбран явный незавершённый
  исход, а не частичная санация.
- Результат review `f13ffa8` передал владелец; эта сессия его не наблюдала.

## cuts

- Непокрытый случай: основание, процитировавшее материал без сохранённой структурной
  связи с Work, при его удалении не снимается и не удерживается.
- В schema 2–6 оставшийся текст лежит в live SQLite и управляемых backup до явного
  upgrade и `complete_deletions`; чтение его не выдаёт.
- Upgrade вычисляет зависимости по текущей сохранённой структуре; в редкой составной
  истории он может снять больше, чем schema 7 сняла бы в момент удаления.
- Часть 3.2, проход 4, представление исходов в Pi (3.10), PR, реальные модели и миграция
  разработки не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на
  Linux останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен технический review этого порядка. Он не является приёмкой части 3.1, schema 7 или
прохода 3, а часть 3.2 требует отдельного разрешения. Обязательный полный gate —
независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.1.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — санация оснований исхода Work по review `a7226f8`

## outcome

Владелец передал результат повторного Windows-review `a7226f8`: 565 тестов и полный
`tools.check --deliver` прошли, прежние воспроизведения исправлены. Затем он разрешил
узкое исправление санации оснований исхода Work (`WorkClosure` и `WorkAcceptance`).
Решения владельца:

- зависимости через собственный план и план родителя учитываются;
- отсутствие продублированной ссылки в `WorkState` не делает основание независимым;
- приёмка Stage 3 не разрешает хранить удалённый текст в `WorkAcceptance.basis`.

Три случая review сначала воспроизведены на `a7226f8`, закреплены регрессиями и
исправлены в `2b29471`:

1. X только в `WorkPlan.basis`, его цитируют `cancelled` ребёнка и `failed` родителя.
2. Родитель `failed` цитирует goal ребёнка A, затем A удалён.
3. Основание принятия цитирует удалённый вход.

Что теперь делает удаление в schema 7:

- **Что снимается.** Удаление Artifact или дочернего Work в той же транзакции снимает
  каждое основание принятия или закрытия, которое структурно от него зависит.
- **Зависимости.** Это собственные ревизии Work. У составного родителя добавляются
  история собственного плана, все дети и доказательства. У ребёнка — история плана
  родителя: глобальные адреса, адреса своей и вышестоящих ролей, вышестоящие дети.
- **Санированный план.** Он читается через сохранённый индекс, поэтому
  последовательные удаления работают и после перезапуска.
- **Что остаётся.** Исход, адресный audit, обязательства и независимые основания.
  `basis = null` не выдаётся как оригинал.
- **Повтор и backups.** Квитанции следуют контракту `not_found`/`history_unavailable`,
  backups с такими Work очищаются.

Правила версий соблюдены:

- чтение не мигрирует пространство;
- DDL и checksum schema 1–7 не менялись;
- снятое `WorkAcceptance` пишется только в schema 7. В schema 2–6 формат прежний, а
  явный upgrade 6 → 7 снимает основания, чьи зависимости уже удалены; тогда upgrade
  требует ещё `maintenance.delete`.

Часть 3.2 не начата. Приёмка не записана. PR, реальных моделей и миграции разработки не
было.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `a7226f8` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

Воспроизведение шло одним скриптом через публичный Core (scratchpad вне репозитория).

- **На `a7226f8`, случай 1.** План недоступен, но оба основания содержат маркер (и после
  перезапуска), как и закрытая SQLite и новый backup. Оба повтора `close_work` вернули
  квитанции.
- **На `a7226f8`, случай 2.** Основание родителя, SQLite и новый backup содержат маркер,
  повтор вернул квитанцию.
- **На `a7226f8`, случай 3.** То же для основания принятия, в schema 7 и schema 2.
- **После `2b29471`.** В случаях 1–3 на schema 7 маркера нет, повторы дают
  `history_unavailable`, `read_receipt` — `not_found`. Случай 3 на schema 2 сохраняет
  прежний формат до явного upgrade.

Регрессии в новом `tests/zaratustra/foundation/test_outcome_bases.py` (7 тестов):

- основание плана без дублирования в `WorkState`;
- историческая ревизия плана;
- удаление ребёнка: сняты основания родителя и нижестоящего B, независимый сосед
  сохранён;
- основание принятия в schema 7;
- последовательные удаления, где второй шаг в отдельном процессе удаляет ребёнка или
  его вход и находит B через сохранённый индекс;
- schema 2: формат прежний до upgrade; upgrade без `maintenance.delete` откатывается,
  upgrade владельца снимает основание, `complete_deletions` удаляет backup, созданный
  после удаления.

Каждый тест проверяет закрытую SQLite, старые и новые backups, квитанции, audit и
независимые основания. Все 7 падают на исходниках `a7226f8` (тот же файл тестов на
снимке `git archive`) и проходят на `2b29471`.

Проверки на чистом дереве `2b29471`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 571 прошёл, 1
  упал, 107,19 с. Это 565 тестов `a7226f8` и 7 новых. Падение —
  `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`, ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 172 файлах.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (198 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- `tools.probe_stage6_rpc` — `passed`, 3 HTTP, schemas 5–6.
- `tools.probe_install_stage6` — wheel SHA-256
  `D8F329A54490B52307722049175874E17A38BF6AB7A6642E243E4C178B93D5F4`, новый venv вне
  checkout, `source_tree_dirty: false`, `passed`, 3 HTTP.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении.
- Все probe — обычный Pi 0.87.0 RPC с синтетическим localhost-провайдером на schema
  5–6; отчёты в игнорируемом `_scratch/stage6-pass3-31basis-*`.

`git diff a7226f8 2b29471` затрагивает только `composition.py`, `models.py` и новый
тестовый модуль. Итоговый коммит меняет только Markdown-файлы, включая
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel.

## assumptions

- Структурные зависимости основания:
  - собственные ревизии Work;
  - у родителя — все ревизии его плана, все дети с их ревизиями и доказательства
    подтверждений;
  - у ребёнка — все ревизии плана родителя, ревизии родителя, адреса своей и вышестоящих
    ролей и вышестоящие дети.

  Независимым считается основание, где такой связи нет.
- Санированный план без индекса (старый формат) считается зависимым от любого удаления,
  как в проходе 1.
- `basis = null` в `WorkAcceptance` и `WorkClosure` означает снятое основание.
- Снятие оснований при upgrade завершает прежние удаления, поэтому требует
  `maintenance.delete`. Событие upgrade перечисляет адреса Work со снятым основанием.
- Результат review `a7226f8` передал владелец; эта сессия его не наблюдала.

## cuts

- В schema 2–6 удалённый текст в `WorkAcceptance.basis` остаётся до явного upgrade до 7.
  - **Почему.** Иначе пришлось бы молча изменить опубликованный формат, сделать
    принятый Work нечитаемым или отказать в удалении. Последнее ломает принятый probe
    прохода 2 на schema 6: он удаляет результат A и затем читает B как `succeeded`.
  - Вопрос вынесен владельцу.
- Upgrade, снявший основания, не сжимает SQLite сам: это делает следующий
  `complete_deletions`.
- Основание, процитировавшее структурно несвязанный материал, при его удалении не
  снимается.
- Часть 3.2, проход 4, представление исходов в Pi (3.10), PR, реальные модели и миграция
  разработки не начинались. Windows-прогон здесь невозможен; `tools.check --deliver` на
  Linux останавливается на mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен повторный технический review исправления. Он не является приёмкой части 3.1,
schema 7 или прохода 3, а часть 3.2 требует отдельного разрешения. Обязательный полный
gate — независимый Windows-прогон, результат которого передаёт владелец; команды в
`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.1.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — исправления 3.1 по review `e2d5b90`

## outcome

Владелец передал результат независимого Windows-review `e2d5b90`:
`tools.check --deliver` прошёл (562 теста), оба неизменённых Pi-probe, включая
итоговый установленный wheel, прошли. Review воспроизвёл два дефекта части 3.1. Оба
сначала воспроизведены здесь на `e2d5b90` через публичный Core, затем закреплены
регрессиями и исправлены в `210399b`.

1. **Санация основания закрытия.** Удаление Artifact теперь снимает основание каждого
   закрытия, чья закрытая ревизия Work называет этот Artifact во входах, связанных
   выходах или предпосылках `stale`. Используется механизм адресов зависимостей
   `sanitize_deleted_dependency`, как для оснований подтверждений прохода 1.
   - Исход, адреса и прочее состояние Work сохраняются. `basis` становится `null` и не
     выдаётся как оригинал.
   - Квитанция закрытия выходит из повтора (`history_unavailable`, `read_receipt` —
     `not_found`). Backups с этим Work загрязняются и очищаются `complete_deletions`.
   - Независимые закрытия сохраняют основание и повтор.
2. **Существование предпосылки.** `stale` принимает предпосылку, только если она
   существовала по точному адресу. Это проверяется по сохранённым метаданным
   `record_revisions`, без чтения и восстановления удалённого содержимого: у Artifact —
   ревизия с содержимым, у Decision — действующая ревизия. Иначе отказ
   `premise_unknown` до удержания, поэтому Work и исполнение не меняются. Реальный
   пересмотр, удаление известного Artifact и отзыв Decision по-прежнему дают `stale`.

Часть 3.2 не начата. Приёмка части 3.1 или прохода 3 не записывается. PR, реальных
модельных вызовов и миграции разработки не было.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `e2d5b90` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют.

Воспроизведение шло одним и тем же скриптом, который вызывает только публичный Core
(scratchpad вне репозитория):

- **На `e2d5b90`, дефект 1.** `live_store_sanitized: true`, старый backup удалён. Но
  маркер остался в `closure.basis`, закрытой SQLite, новом backup и после перезапуска
  нового процесса, а повтор `close_work` вернул прежнюю квитанцию.
- **На `e2d5b90`, дефект 2.** Для ребёнка с `any(source@1, source@999, D@1)`, выданного,
  назначенного и ждущего ответа, `stale` по `source@999` был записан. Он включил
  удержание: назначение `waiting → stop_requested`, вопрос `open → closed`. `stale` по
  несозданному `D@1` тоже был записан.
- **После исправления.** Маркера нет ни в `closure.basis`, ни в SQLite, ни в backup, ни
  после перезапуска. Повтор — `history_unavailable`, `read_receipt` — `not_found`. Оба
  `stale` — `premise_unknown`, ребёнок остаётся `proposed`/`waiting`.

Регрессии в `tests/zaratustra/foundation/test_work_outcomes.py`:

- **`test_deleted_artifact_sanitizes_only_dependent_closure_bases`** проходит шаги
  review. Два зависимых закрытия (`stale` по X@2 и `failed` со входом X@3) сохраняют исход
  и адреса. Их основания сняты, повтор — `history_unavailable`. Два независимых
  закрытия сохранили основание и прежние квитанции. Маркеров нет в закрытой SQLite и
  новом backup, независимое основание там есть. Новый процесс читает `stale` с
  `basis = null`.
- **`test_stale_refuses_premise_addresses_that_never_existed`** проверяет четыре
  несуществовавших адреса: ревизию 999, несозданный Artifact, несозданный Decision и
  Decision, записанный отозванным. Каждый даёт `premise_unknown`; снимок исполнения до
  и после совпадает.
- **`test_stale_accepts_a_deleted_known_premise`** — положительный случай удалённого
  известного Artifact.
- **Дополнен собственный тест обслуживания 3.1.** После удаления основания плана
  основания `stale` у B и `cancelled` у A сняты.

До исправления два теста дефектов упали по тем же причинам, а положительный прошёл.

Проверки на чистом дереве `210399b`:

- `pytest tests -q` (conda-forge Python 3.13.7, SQLite 3.53.3, FTS5): 564 прошли, 1 упал,
  106,78 с. Это 562 теста `e2d5b90` и 3 новых. Падение —
  `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`, ожидаемое на Linux.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 171 файле.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (197 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок
  `attr-defined` в 11 файлах, только атрибуты Windows. Изменённых файлов среди них нет.
- `tools.probe_stage6_rpc` — `passed`, 3 HTTP, schemas 5–6.
- `tools.probe_install_stage6` — wheel SHA-256
  `5AE5D2AD6740C92191DD7199521CBB44B9ACCDDE38C13453BEF500E870087D1E`, новый venv вне
  checkout, `source_tree_dirty: false`, `passed`, 3 HTTP.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении.
- Все probe — обычный Pi 0.87.0 RPC с синтетическим localhost-провайдером; отчёты в
  игнорируемом `_scratch/stage6-pass3-31fix-*`.

`git diff e2d5b90 210399b` затрагивает только `composition.py`, `models.py` и
`test_work_outcomes.py`. Gates и прежние тесты других модулей не менялись. Итоговый
коммит меняет только Markdown-файлы. Среди них `src/zaratustra/foundation/AGENTS.md`,
который входит в wheel, поэтому SHA-256 wheel итогового коммита отличается от
проверенного.

## assumptions

- Зависимость основания закрытия определяется адресами Artifact, записанными в закрытой
  ревизии Work: входами, связанными выходами и предпосылками. Проход 1 описывает
  зависимости состояния Work теми же адресами.
- Artifact, который есть только в плане родителя и не назван в ревизии ребёнка,
  основание ребёнка не снимает, как и принятие ребёнка в проходе 1.
- `basis = null` означает снятое основание: операция всегда задаёт основание.
- Decision-ревизия, записанная отозванной, никогда не была действующей предпосылкой,
  поэтому `premise_unknown`.
- Результат review `e2d5b90` передал владелец; эта сессия его не наблюдала.

## cuts

- Основание принятия Stage 3 (`WorkAcceptance.basis`) при удалении входа или выхода
  по-прежнему не снимается. Это принятое поведение Stage 3; оно вынесено вопросом
  владельцу, а не изменено молча.
- Часть 3.2, проход 4, представление исходов в Pi (3.10), PR, реальные модельные вызовы
  и миграция разработки не начинались.
- Windows-прогон здесь невозможен. `tools.check --deliver` на Linux останавливается на
  mypy по платформенной причине.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Новой сетевой загрузки не было: использованы ранее установленные Pi 0.87.0 в
игнорируемом `_scratch`, conda-forge Python/SQLite в scratchpad и кэш uv.

## manual-acceptance

Нужен повторный технический review исправлений 3.1. Он не является приёмкой части,
schema 7 или прохода 3. Следующая часть требует отдельного разрешения. Обязательный
полный gate — независимый Windows-прогон, результат которого передаёт владелец; команды
в `docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.1.md`.

## next

solmax

# Core v0.1 Stage 6, проход 3 — контрольная точка 3.1: schema 7 и исходы Work

## outcome

По решениям владельца от 2026-09-24 уточнён `docs/core-v0.1/STAGE6-PASS3-PLAN.md`:

- **3.3.** Конфликт выбора проверяется среди всех решений, применимых к Work, включая
  пересекающиеся области Activity и Work. Локальное решение не отменяет общее. Если
  противоположное решение появилось после разрешения применимости, его обнаруживают
  последующие выдача, подтверждение и принятие.
- **3.9.** Замена вложенного составного Work следует правилу `open_children`. Каждый
  открытый потомок называется явно, со своей ревизией и исходом. Права проверяются на
  каждом уровне. Attempt ограждается, `unknown` и резервы сохраняются. Проверка включает
  ветвь с исполняющимся внуком.
- **Отдельный срез.** Для собственного исполнения составного родителя и обязательства в
  том же Work запланирован отдельный срез до окончательной приёмки Stage 6. Отдельный
  ребёнок не становится требованием продукта.
- **Атрибуция.** В `STAGE6-PASS2-ACCEPTANCE.md` и прежней записи этого файла исправлена
  атрибуция: Windows-проверку `d33b676` выполнил Codex, владелец передал результат.

Реализована только часть 3.1, как контрольная точка разработки
(`docs/core-v0.1/STAGE6-PASS3-CHECKPOINT-3.1.md`):

- **Upgrade и запись исхода.** Явный upgrade 6 → 7 (`upgrade_plan_revision_space`); на
  этой точке schema 7 без DDL. Операция `close_work` записывает `failed`, `cancelled` или
  `stale` одной записью `WorkClosure` в ревизии Work.
- **Права.** `work.accept`; для составных Work — ещё `method.use`.
- **`stale`.** Только после проверки действительно изменившихся собственных предпосылок
  (`premise_mismatch`, `premise_current`).
- **Удержание.** Как при принятии: `stop_requested`, закрытие вопросов, отмена outbox,
  `unknown` для отправленных вызовов с удержанием резерва и ресурса.
- **Закрытый Work.** Новые действия получают `work_closed`. Составной родитель
  закрывается только после всех детей (`open_children`), каскада нет.
- **Зависимые условия.** Закрытый ребёнок даёт зависимым листам `dependency_closed`.
  `any` закрыто, только когда закрыты все члены.
- **Activity.** Её завершение считает новые исходы закрытыми.
- **Данные.** Исходы проходят чтение, backup, карантинный restore и удаление.

Принятый путь прохода 2 сохранён: канонические формы и повтор не изменились, пространства
schema 5–6 работают как прежде. Работа остановлена для технического review. Это не
приёмка schema 7 и не приёмка прохода 3; часть 3.2 не начата. Коммиты: `aa26b31` (код и
тесты), `d08ea38` (уточнение плана и атрибуции) и итоговый коммит с этими документами.

## evidence

Сессия продолжила работу в облачном Linux-контейнере от `40fc555` на ветке
`claude/pensive-gates-47w6z7`. `STOP` и `STEER.md` отсутствуют. SHA-256 спецификации
совпал: `1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.

Новый модуль `tests/zaratustra/foundation/test_work_outcomes.py` (11 тестов) покрывает:

- явный upgrade и повтор прохода 2;
- канонические fingerprint и checksum Method, вычисленные кодом `d33b676`;
- `failed` простого Work с правами, повтором, точными ревизиями и прерыванием
  интерактивной Attempt;
- `cancelled` назначенного ребёнка с вопросом до и после записи остановки;
- `unknown` с удержанием резерва и `resource_busy`;
- `stale` по входу, листу Decision и предпосылке родителя, `premise_current` и
  `premise_mismatch`;
- санированный план;
- `dependency_closed`, `open_children` и завершение Activity;
- `any` над закрытым и открытым членом;
- restore/Recover без возобновления, новый процесс, `unavailable_refs` и санацию
  маркера.

Проверки на чистом дереве `d08ea38`:

- `pytest tests -q` в venv на conda-forge Python 3.13.7 с SQLite 3.53.3 и FTS5: 561
  прошёл, 1 упал, 101,15 с. Это 551 тест `d33b676` и 11 новых. Падение —
  `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`, ожидаемое на Linux.
  Тот же прогон до коммита, при том же коде и без одного добавленного позже теста, дал
  560 прошедших и то же падение.
- `uv run --locked mypy --platform win32 src tools tests` — ошибок нет в 171 файле.
- `uv run --locked lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `uv run --locked python -m tools.check --deliver` — hygiene, структура отчёта, Ruff
  format (197 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок в 11
  файлах, все про атрибуты только Windows (`msvcrt.locking`/`LK_*`, `ctypes.WinDLL`).
  3.1 эти файлы не меняла.
- `tools.probe_stage6_rpc` из checkout — `passed`, 3 HTTP, schemas 5–6.
- `tools.probe_install_stage6`: `uv build --no-sources`, wheel SHA-256
  `4BD3BDFCB28DD513632021C46D25C12F759EC80B68D733F6B193801A4E010FE3`. Новый venv в
  `/tmp` вне checkout, `source_tree_dirty: false`, `passed`, 3 HTTP. Core, DBOS,
  расширение и повторно открытый процесс загружены из этого venv.
- `tools.probe_stage5_rpc` — `passed`: 2 вызова провайдера, отказ до отправки без HTTP,
  restore в эпоху 2, санация при удалении.
- Все probe шли через обычный Pi 0.87.0 RPC с синтетическим localhost-провайдером.
  Отчёты лежат в игнорируемых `_scratch/stage6-pass3-31-rpc`,
  `_scratch/stage6-pass3-31-installed` и `_scratch/stage6-pass3-31-stage5`.

`git diff d33b676 HEAD -- tools validation.config REVIEW.md pyproject.toml uv.lock
.githooks tests` показывает только новый файл тестов. Gates и прежние тесты не менялись.
Итоговый коммит меняет только Markdown-файлы. Среди них
`src/zaratustra/foundation/AGENTS.md`, который входит в wheel, поэтому SHA-256 wheel
итогового коммита отличается от проверенного; Python-код совпадает с `d08ea38`.

## assumptions

- Имена `close_work`, `WorkClosure`, `upgrade_plan_revision_space` и запись миграции
  `core-v0.1-plan-revision-7` — рабочие решения; спецификация их не предписывает.
- На точке 3.1 schema 7 без DDL, её checksum — SHA-256 пустого набора. Следующие части
  его изменят. Повышенные кодом 3.1 тестовые пространства одноразовые, как решил
  владелец.
- Предпосылки Work: собственные входы. У ребёнка к ним добавляются входы родителя,
  основание текущего плана и листы готовности, у родителя — основание его плана и листы
  условия завершения.
- После санирования плана его основание проверить нельзя, поэтому `stale` по нему
  отказывает, а `failed`/`cancelled` доступны.
- `all` с закрытым членом закрыт; это следует из правила для `any` в плане.
- Stage 6 probe идут путём schema 6 и подтверждают сохранность прохода 2, а не поведение
  3.1 в Pi.

## cuts

- Не начаты части 3.2–3.10, отдельный срез исполнения родителя и обязательства в том же
  Work, а также проход 4.
- Представление исходов в Pi относится к 3.10; Pi-адаптер не менялся.
- Не было реальных модельных вызовов, миграции разработки и PR.
- Windows-прогон в этом контейнере невозможен. `tools.check --deliver` на Linux не
  доходит до конца из-за платформенной разницы mypy; остальные шаги выполнены отдельно.

## cost

Платных сервисов и модельных вызовов нет. Провайдер в probe — синтетический localhost.
Сеть использовалась только для окружения проверок:

- npm-пакеты `@earendil-works/pi-coding-agent@0.87.0` и `@earendil-works/pi-ai@0.87.0` в
  игнорируемый `_scratch/stage6-pass3-pi-runtime`;
- зафиксированные пакеты PyPI через uv для venv установленного probe;
- conda-forge Python и SQLite, загруженные ранее в scratchpad вне репозитория.

## manual-acceptance

Нужен технический review контрольной точки 3.1 владельцем. Он не является приёмкой
schema 7 или прохода 3; следующая часть требует отдельного разрешения. Обязательный
полный gate — независимый Windows-прогон (`tools.check --deliver` и оба Stage 6 probe),
результат которого передаёт владелец. Команды — в `STAGE6-PASS3-CHECKPOINT-3.1.md`.
Ничего здесь не принимает Stage 6 или синтетические Work.

## next

solmax

# Core v0.1 — приёмка Stage 6 pass 2 и план прохода 3

## outcome

Записана приёмка владельцем второго прохода Stage 6 на точном коммите
`d33b676c57b8319cb4a4d2527f64d41fe800c154`: `docs/core-v0.1/STAGE6-PASS2-ACCEPTANCE.md`.
Подготовлен подробный план третьего прохода `docs/core-v0.1/STAGE6-PASS3-PLAN.md`:
десять небольших последовательных частей 3.1–3.10, у каждой — контракт, проверяемый
результат и обслуживание данных. Таблица в начале плана закрепляет за частями каждый
элемент прохода 3 из `STAGE6-PLAN.md`. Отдельно перечислены намеренные изменения
принятого поведения, граница прохода 4, то, что остаётся вне прохода 3, и четыре
решения владельца.

`AGENTS.md` отражает приёмку прохода 2 и то, что для прохода 3 разрешено только
планирование. Код продукта, тесты и probes не менялись; реализация прохода 3 не
начиналась. Это не приёмка Stage 6 и не приёмка синтетических Work.

## evidence

Сессия работала в облачном Linux-контейнере, а не в рабочем дереве владельца на Windows.

- Клон стартовал на `main` (`c73d039`). Ветки `claude/pensive-gates-47w6z7` на remote не
  было; рабочая ветка создана от `d33b676c57b8319cb4a4d2527f64d41fe800c154`, текущего
  HEAD `origin/claude/magical-einstein-4l2fax`. Дерево было чистым, `STOP` и `STEER.md`
  отсутствуют.
- SHA-256 спецификации совпал:
  `1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
- Прочитаны `STAGE6-PLAN.md`, `STAGE6-PASS1-IMPLEMENTATION.md`,
  `STAGE6-PASS2-IMPLEMENTATION.md`, `STAGE6-PASS2-WORKING-PLAN.md`,
  `IMPLEMENTATION-BASELINE.md`, этот `RESULT.md` и разделы спецификации, перечисленные в
  плане. Прочитан код `foundation` (`composition.py`, `work_status.py`, `operations.py`,
  `execution.py`, `storage.py`, `models.py`, `history.py`) и `pi_adapter` (`assigned.py`,
  `bridge.py`), а также тесты проходов 1–2.
- `git diff d33b676 -- src tools tests pyproject.toml uv.lock validation.config REVIEW.md`
  пуст: изменены только `AGENTS.md`, `RESULT.md` и два новых документа.
- `uv build --no-sources` собрал wheel с SHA-256
  `9FE6E3A6268D3AEBDDD495BB657EF1BE6C3ACA27A73778D5339812536EFE2D91` — тем же, что у
  итогового wheel прохода 2 в записи ниже.

Проверки:

- `uv run --locked python -m tools.check --deliver`: hygiene, структура отчёта, Ruff format
  (196 файлов) и Ruff lint прошли. Затем остановка на mypy: 35 ошибок в 11 файлах, все —
  атрибуты только Windows (`msvcrt.locking`/`LK_*`, `ctypes.WinDLL`), как в отчёте
  прохода 2.
- Остальные шаги отдельно: `mypy --platform win32 src tools tests` — ошибок нет в 170
  файлах; `lint-imports --no-cache` — 21 контракт сохранён, 0 нарушено.
- `pytest tests -q` в отдельном venv на conda-forge Python 3.13.7 с SQLite 3.53.3 и FTS5:
  550 прошли, 1 упал, 102,93 с. Единственное падение —
  `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`: этот Python уже
  содержит точный SQLite, как и в отчёте прохода 2. Первый прогон без каталога этого venv
  в `PATH` дал ещё три падения `test_real_boundary_clean_and_reverse_import_rejected`
  (`lint-imports` не найден); полный повтор с `PATH` их снял.
- Документы: `git diff --check` чист; в новых файлах нет хвостовых пробелов и CRLF.

Независимую Windows-проверку `d33b676` (`tools.check --deliver`, 551 тест, оба исходных
Pi-probe, включая установленный wheel) выполнил Codex, а владелец передал её результат;
эта сессия её не наблюдала. (Атрибуция исправлена 2026-09-24: прежняя редакция
приписывала проверку владельцу.)

## assumptions

Спецификация не предписывает имена операций, таблиц и номер схемы; в плане это рабочие
предложения. Рекомендации по четырём решениям владельца — предложения, а не принятые
решения: интеграция собственной Attempt родителя вне прохода 3, одна schema 7 на проход,
существующие действия для прав новых операций, `stale` как явный исход. Результат
Windows-проверки `d33b676`, выполненной Codex, передал владелец. Тесты не читают
документы репозитория, поэтому изменения документов на pytest не влияют.

## cuts

Реализация прохода 3, проход 4, реальные модельные вызовы и миграция разработки не
начинались. Код, тесты и probes не менялись. Pi-probe здесь не запускались: код
совпадает с принятым коммитом, а Pi runtime в этом контейнере отсутствует. Полный
`tools.check --deliver` на Linux не доходит до конца из-за платформенной разницы mypy;
Windows-прогон в этом контейнере невозможен.

Пункты раздела «Вне прохода 3» плана не входят в объём прохода 3 по `STAGE6-PLAN.md`.
Интеграция собственной Attempt родителя вынесена вопросом владельцу, а не отброшена
молча.

## cost

Платных сервисов и модельных вызовов нет. Сеть использовалась только для окружения
проверок: `uv sync --locked` (managed Python 3.13.7 и зафиксированные пакеты PyPI в
игнорируемый `.venv`), а также repodata conda-forge и пакеты
`python-3.13.7-h2b335a9_100_cp313`, `libsqlite-3.53.3-h0c1763c_0`,
`openssl-3.5.8-h781a0a9_0` и `libmpdec-4.0.0-hb03c661_2` в scratchpad вне репозитория.

## manual-acceptance

Владелец принял проход 2 на `d33b676`; решение записано отдельно. План прохода 3 ждёт
проверки владельца, а его реализация требует отдельного явного разрешения. Ничего здесь
не принимает Stage 6 или синтетические Work.

## next

solmax

# Core v0.1 Stage 6 pass 2 — review corrections

## outcome

Fixed both findings from the owner's independent Windows review of
`961e203e0aadf836bf6031b47427fae5fc9bf08c` on `claude/magical-einstein-4l2fax`.
This is an engineering correction for owner review, not acceptance of pass 2 or
of Stage 6. Passes 3–4 were not started.

1. **Parent readiness.** The parent's derived readiness now follows Core's own
   acceptance rules.
   - The parent `AcceptWorkRequest` checks moved from the shared gate into
     `check_parent_acceptance`, with the same order and the same Decision/Grant
     checks.
   - `read_work_status` and `read_execution(...).status` call the same function
     without an actor.
   - `ready:acceptance_pending` therefore appears only when Core would accept
     structurally. Acceptance stays a separate operation, and the operation
     still checks the actor's current rights.
   - A stale prerequisite blocks the parent with its address: basis, input,
     linked output, a completion leaf (Artifact or Decision), or a child result
     needed for an output binding or obligation.
   - Before the output is linked, the parent reads `ready:output_link_pending`,
     with the exact accepted child result as the address.
   - In the same defect class, an unissued child now gets its reasons from the
     checks `IssueChildWorkRequest` makes, including the child's own inputs.
2. **Probe child encoding.** Every isolated Python child in the Stage 6 probes
   now starts as `-I -X utf8`: the reopen process, the restart between
   deletions, and the outer installed-wheel process. `-I` ignores
   `PYTHONIOENCODING`, so the ineffective variable was removed from
   `probe_install_stage6`.

## evidence

Owner's independent Windows review of `961e203`, as reported:
- `tools.check --deliver` passed with 546 tests, 21 contracts, types and build,
  and the wheel SHA-256 matched.
- Both Windows probes passed fully only with a diagnostic `-X utf8` at both levels.

The two new regressions in `tests/zaratustra/foundation/test_child_execution.py`
first failed on the `961e203` code: the parent stayed
`ready:acceptance_pending`.
- `test_parent_blocks_when_its_input_revision_changes`
- `test_parent_blocks_when_its_completion_decision_is_revoked`

After the fix, both `read_work_status` and `read_execution(...).status` return
`blocked` with exactly one reason, `stale_basis`, addressed to that
Artifact/Decision at revision 1. `AcceptWorkRequest` refuses with
`stale_basis`, and the parent stays `proposed`. The existing parent-state test
now shows `output_link_pending` before linking, and premature acceptance is
refused with `output_mismatch`.

`test_unissued_child_and_parent_show_the_issue_refusal_address` revises the
input before issue. Both the unissued child and the parent then read `blocked`
with `stale_basis` at that Artifact revision 1. Issue refuses with the same
code; before this fix the child's derived reason was `stale_input`.

New `tests/tools/test_probe_stage6_rpc.py` (2 tests) runs an isolated child
with each probe's flags and requires exact non-ASCII JSON back.

On Linux the encoding defect was reproduced with an `en_US.CP1252` locale built
by glibc `localedef` and selected through `LOCPATH`/`LC_ALL`.
- The unchanged `961e203` probe failed in `_reopen_in_new_process` with
  `UnicodeEncodeError: 'charmap' codec can't encode characters`
  (`_scratch/stage6-pass2-cp1252-before.log`).
- Under the same locale, the fixed probe passed
  (`_scratch/stage6-pass2-cp1252-after/report.json`).
- So did the installed-wheel probe (`_scratch/stage6-pass2-cp1252-installed/report.json`).

Default-locale runs on the final source also passed:
- `_scratch/stage6-pass2-rpc-d/report.json` (checkout);
- `_scratch/stage6-pass2-installed-d/report.json` (wheel SHA-256
  `9FE6E3A6268D3AEBDDD495BB657EF1BE6C3ACA27A73778D5339812536EFE2D91`, new venv
  in `/tmp` outside the checkout; Core, DBOS, the extension and the reopened
  process all loaded from that venv).

Each passing run made 3 HTTP requests. In each:
- the subject refusal was `stale_basis` with 0 HTTP;
- a redelivered launch left one workflow;
- a duplicate answer returned the same receipt;
- the interactive Pi status made 0 HTTP;
- the technical copies contained no text markers;
- backup/restore reached schema 6 in epoch 2;
- reopen showed the parent and both children `succeeded`.

The timeline now shows the parent as `ready:output_link_pending` after the
obligations are confirmed and before its output is linked.

`uv run --locked python -m tools.check --deliver` passed hygiene, report
structure, Ruff format (196 files) and Ruff lint. It then stopped at mypy on
Linux (see the limitations below). The remaining steps ran individually
through `uv run --locked`:
- `mypy --platform win32 src tools tests`: no issues in 170 source files;
- `lint-imports --no-cache`: 21 contracts kept, 0 broken;
- `pytest tests -q`: 550 passed and 1 failed in 114.07 seconds;
- `uv build --no-sources`: sdist and wheel built.

The 551 tests are 546 plus the 5 new ones. The single failure is the Linux-only
SQLite test listed below.

### Linux check limitations (pre-existing; gates unchanged)

`tools/check.py`, `validation.config`, REVIEW.md and the existing tests were
not changed for Linux.
- `tools.check --deliver` stops at mypy on Linux. There are 35 errors, all
  Windows-only attributes (`msvcrt.locking`/`LK_*`, `ctypes.WinDLL`/
  `get_last_error`), in 11 files unchanged since `29d887d`. The remaining
  steps run individually, and `mypy --platform win32` is clean.
- `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process` fails here
  because conda-forge Python already ships the exact SQLite 3.53.3. It fails
  the same way on `29d887d` and `961e203`.
- A C/POSIX locale already implies UTF-8 mode, so the new encoding test cannot
  catch a missing flag on Linux. On Windows it can. The CP1252 reproduction
  above is manual evidence.
- `.githooks/pre-commit` starts `#!/usr/bin/env python`, which is the
  container's Python 3.11. That version cannot parse the repository's 3.13
  syntax, so the hygiene hook ran with Python 3.13.7 first in `PATH`.
- The ignored `_scratch/stage4-pi-runtime` needs a top-level
  `@earendil-works/pi-ai@0.87.0`.

## assumptions

A completion `any` blocks the parent only when none of its members can still
become true. `dependency_open` counts as open progress, not a stale
prerequisite. Blocking reasons use Core's own codes (`stale_basis`,
`content_unavailable`, …). They are addressed to the Artifact or Decision with
its revision, or to the role's Work. The parent reports a stale prerequisite
before its child phases, because under this plan no child activity can make
the parent acceptable. `-X utf8` is the child's explicit encoding. The parent
already decodes with `encoding="utf-8"`.

## cuts

No acceptance and no passes 3–4. No real model call, development migration or
new dispatcher. No change to `tools/check.py`, `validation.config` or
REVIEW.md. The corrections were not run on Windows here; the owner will repeat
the Windows check.

## cost

No paid service or external model call. The provider is a synthetic localhost
fixture. The CP1252 locale was generated locally in the scratchpad.

## manual-acceptance

Owner review and acceptance are still required. Passing checks does not record
acceptance of Stage 6 pass 2.

## next

solmax

# Core v0.1 Stage 6 pass 2 — composite child Work on the Stage 5 Attempt

## outcome

Implemented only Stage 6 pass 2, from exact commit
`29d887d41f1639dc6ddb354a2039603cb1a30d94`, which the owner accepted as
Stage 6 pass 1. That acceptance is recorded separately in
`docs/core-v0.1/STAGE6-PASS1-ACCEPTANCE.md`, the short working plan is
`docs/core-v0.1/STAGE6-PASS2-WORKING-PLAN.md`, and the implementation is
described in `docs/core-v0.1/STAGE6-PASS2-IMPLEMENTATION.md`.

An issued composite child Work now runs through the existing Stage 5 assigned
Attempt (Core outbox, DBOS queue and ordinary Pi RPC); no dispatcher was added.
Explicit additive schema 6 (`upgrade_child_execution_space`) adds
`execution_plan_pins`: assignment pins the exact parent, role, plan revision
and Method id/version/checksum as addresses. The shared `apply_operation` gate
checks the current plan, issue, dependencies, exact inputs and `method.use`
Decision/Grant authority at assignment. Before every Attempt effect (launch
claim, wait/answer, invocation prepare/admit/send, publish) it also checks the
pin. Stop and sent-call outcome records stay available. Work
`proposed/ready/running/waiting/blocked/succeeded` is derived from the same Core
records in the reading transaction (`read_work_status`,
`ExecutionSnapshot.status/composition`); no second status field and no copy of
the plan exists. Result acceptance remains a separate operation.
An `unknown` outcome keeps the resource held and reads `blocked:outcome_unknown`.
Interactive Pi context and `/zara-status` show current Core state, plan
address and pinned versions. This is an engineering candidate for owner
review, not acceptance of Stage 6 or of the synthetic Work.

## evidence

The session ran in a cloud Linux container, not in the owner's Windows checkout.
Its clone started on `main`. The designated session branch
`claude/magical-einstein-4l2fax` was set to `origin/codex/core-v0.1` =
`29d887d41f1639dc6ddb354a2039603cb1a30d94`, and the tree was clean. The
specification SHA-256 matched `1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
Python 3.13.7 with SQLite 3.53.3/FTS5 came from conda-forge
(`python-3.13.7-h2b335a9_100_cp313`, `libsqlite-3.53.3-h0c1763c_0`). Other
components: Node v22.22.2, Pi 0.87.0 and DBOS 3.0.0.

New `tests/zaratustra/foundation/test_child_execution.py` (6 tests) covers:
- the explicit schema 6 upgrade, with unchanged old routes;
- pins and the child state transitions, including B unblocked only after A's separate acceptance;
- stale-pin and lost-Decision refusals of effects while stop/finish stay available;
- quarantine restore keeping pins, and deletion removing them;
- parent state;
- sequential sanitation across a fresh process.

New `tests/zaratustra/pi_adapter/test_child_assigned.py` (3 tests) covers:
- bridge state and one addressed answer;
- duplicate launch delivery creating one DBOS workflow;
- a subject refusal before launch, with no Pi process and no invocation.

`tools.probe_stage6_rpc` ran a synthetic localhost SSE provider through ordinary
Pi 0.87.0 RPC. Checkout report: `_scratch/stage6-pass2-rpc-c/report.json`, status
`passed`, 3 HTTP in total.
- Early B was refused with `dependency_open`/`child_not_issued`, with 0 Attempts, 0 outbox and 0 HTTP.
- A's issue and assignment replays returned the same receipts; `already_issued`/`stale_attempt` refused the duplicates.
- A redelivered launch left one workflow.
- A separate ordinary Pi showed `waiting`, the question, `Plan …@2`, role and Method with 0 HTTP.
- A duplicate answer returned the same receipt.
- A's result became B's exact input only after A's separate acceptance.
- Premature parent acceptance was refused with `obligation_open`; after confirmations the parent is `succeeded` and the Activity is `ongoing`.
- An independent child whose Decision changed after assignment was refused with `stale_basis`: 0 HTTP, 0 invocations, assignment `stopped`, no Pi home.
- A new `python -I` process read the same results, plan, pins and receipts.
- DBOS/Pi technical copies contain no plan or result text.
- Backup format 2 schema 6 restored into epoch 2.
- Sequential deletion left no markers, kept the independent child and refused the shared Method with `method_in_use`.

`tools.probe_install_stage6` built the wheel (SHA-256
`E486981A25DD9E1D34619BD6D4311FE7A3F7EC0435688CF8744ED76747B31A24`) and installed it into a new venv in `/tmp`,
outside the checkout. It ran the same scenario there with `python -I`. Core,
DBOS, the extension and the reopened process all loaded from that venv. Report:
`_scratch/stage6-pass2-installed-c/report.json`, status `passed`, 3 HTTP.
The Stage 5 path was rechecked with `tools.probe_stage5_rpc` on the final source.
Report: `_scratch/stage5-linux-check-c/report.json`, status `passed`.
- 1 provider call before the answer and 2 in total.
- A parallel Pi showed the question without a model call.
- The pre-send denial made 0 HTTP calls.
- Work `proposed`, assignment `stopped`.
- Restore into epoch 2, deletion sanitized, restored archive deleted.

Stage 4 interactive bridge regressions (`tests/zaratustra/pi_adapter/test_bridge.py`)
and Stage 5 assigned regressions (`test_assigned.py`) pass unchanged.

`uv run --locked python -m tools.check --deliver` passed hygiene, report
structure, Ruff format and Ruff lint. It then stopped at mypy on Linux with 35
errors, all Windows-only attributes (`msvcrt.locking`/`LK_*`,
`ctypes.WinDLL`/`get_last_error`). They are in 11 files byte-identical to
`29d887d`. The remaining steps ran through `uv run --locked` individually:
- `mypy --platform win32 src tools tests`: no issues in 169 source files;
- `lint-imports --no-cache`: 21 contracts kept, 0 broken;
- `pytest tests -q`: 545 passed and 1 failed in 114.41 seconds;
- `uv build --no-sources`: built `zaratustra-0.17.0.tar.gz` and `zaratustra-0.17.0-py3-none-any.whl`.

The only failing test is `test_import_refuses_unfixed_sqlite_runtime_in_a_fresh_process`.
It expects the interpreter's own SQLite to be unsupported. That holds on
Windows, but this conda-forge Python already ships the exact SQLite 3.53.3, so
import is correctly admitted. A detached worktree of `29d887d` failed the same
test in the same way, and `runtime.py` and the test are unchanged. The 546
tests are pass 1's 537 plus the 9 new ones.

## assumptions

`WorkState.status` still stores only the subject outcome `proposed/succeeded`.
Lifecycle is derived. The window between a stop request and the recorded stop
reads `running:stop_requested`: the Attempt still holds its resource and new
effects are refused. A subject refusal of a composite child's launch claim
comes before any Pi process or HTTP, so the adapter records an observed stop.
Technical refusals (`busy`, `storage`, maintenance, corrupt space, pending
deletion) leave the assignment for maintenance. Parent execution and
interactive Stage 4 Attempts on composite Work are still refused with
`unsupported_composite_execution`. The parent's outputs still link to exact
accepted child results. The stale-pin regression uses explicit SQLite fault
injection because this pass has no public plan revision after issue. On Linux,
Core admits the runtime by version and FTS5 (existing `runtime.py`), without the
Windows DLL. Pi 0.87.0 ships `npm-shrinkwrap.json`, so the extension's
`@earendil-works/pi-ai@0.87.0` import was installed at top level in the ignored
`_scratch/stage4-pi-runtime`. That is environment, not product.

## cuts

Not started: real model calls, development migration, passes 3–4 and a new
dispatcher. Not implemented:
- plan revision during execution;
- Method version transition;
- waiver, inactive and unresolved;
- parallel children and nested composite Work;
- `failed/cancelled/stale` outcomes.

Not verified for composite children:
- the commit/checkpoint/Pi-death crash matrix;
- rights revocation during a running turn (the Stage 5 stop monitor rechecks the same Core control every 0.5 s);
- real providers and modifying tools.

Not run on Windows. `tools.check --deliver` could not complete on Linux because
of the mypy platform difference above. `C:\projects\zaratustra` and
`C:\my_global_workflow\core-v0-1\zaratustra` do not exist in this container.
Trial writes stayed in the session clone, its ignored `_scratch`, the
scratchpad and disposable `/tmp` trial venvs; package managers used their
normal caches.

## cost

No paid service and no external model call. The provider was a synthetic
localhost fixture (3 HTTP per scenario). Network use was limited to installing
pinned environment packages: conda-forge Python/SQLite, the locked PyPI set
through uv, and npm `@earendil-works/pi-ai@0.87.0`.

## manual-acceptance

The owner authorized only the pass-2 implementation, checks and a local commit.
Nothing here records acceptance of Stage 6 pass 2, of Stage 6 or of the
synthetic Work. Owner review should repeat `tools.check --deliver` and both
probes on Windows with the configured SQLite DLL (commands in
`docs/core-v0.1/STAGE6-PASS2-IMPLEMENTATION.md`).

## next

solmax

# Core v0.1 Stage 6 pass 1 — sequential sanitation correction

## outcome

Corrected the independent review's sequential deletion defect from exact HEAD
`6e7a5f62f6e64bb295425940907afe9b17986224` on `codex/core-v0.1`.
Sanitized plans now retain validated content-free dependency addresses, so a
later Artifact deletion can retire copied bases and receipts without erasing
independent confirmations or the parent's declared obligations. This report
records engineering evidence only, not owner acceptance of Stage 6 pass 1.

## evidence

The unchanged `additional_review.py` was copied and run before edits in new
TEMP `C:\Users\Anton\AppData\Local\Temp\zaratustra-stage6-sequential-repro-0a056f2efb0f4d2a9f607f0526aa12f0`.
After A deletion its independent B confirmation remained satisfied, as
expected. After X deletion its basis and receipt remained readable, replay
returned the old receipt, and the unique marker remained in closed SQLite and
a new managed backup. The old backup was purged; the control without A deletion
passed. All content was synthetic; no HTTP calls occurred.

The new `test_sequential_deletion_keeps_dependency_addresses_after_restart`
uses three declared obligations, independent B and C, two plan revisions and
a fresh `-S` process between A and X deletion. It checks exact unavailable plan
and confirmation history, an `open` affected obligation, intact independent
confirmation and replay, removed affected receipt, refused affected replay,
old backup removal, closed live SQLite and clean new backup. The existing
composition regressions also pass. Full
`uv run --locked python -m tools.check --deliver` passed: 190 files formatted,
Ruff clean, mypy clean across 164 source files, 21 import contracts kept, 537
tests passed in 291.35 seconds, and sdist/wheel built.

The independent script was then copied to a second new TEMP, with only its
installed-wheel path updated to the newly built local wheel:
`C:\Users\Anton\AppData\Local\Temp\zaratustra-stage6-sequential-fixed-4c52d792a8cf45dda8ed03672114cb88`.
Its 54 packaged Python files matched the current source and installed copy;
isolated import loaded SQLite 3.53.3 without sitecustomize or preload. Wheel
SHA-256 was `A27044E8D31624A04A22B1514461E22790EAC2DCC27C6B2DD67902A3CBEEAF65`.
After sequential deletion the script reported `open`, no copied marker basis,
`not_found` for the old receipt, `history_unavailable` for replay, old backup
removed, and no marker in closed live SQLite or the new backup.

## assumptions

The retained index contains only role IDs, fixed dependency edges and Artifact
UUIDs captured at sanitation. Public exact plan reads remain
`content_unavailable`. Older already sanitized payloads without the index
cannot recover their lost graph and use conservative obligation sanitation.
No schema 5 migration was introduced.

## cuts

No Stage 6 pass 2, Pi/DBOS extension, real model call, modifying external tool
or legacy migration. `C:\projects\zaratustra` was not modified.

## cost

No paid service or external model call. The reproduction and tests use local
synthetic data, the existing exact SQLite DLL and new TEMP directories.

## manual-acceptance

The owner authorized these corrections, full checks and a local commit.
Passing checks does not record acceptance of Stage 6 pass 1.

## next

solmax

# Core v0.1 Stage 6 pass 1 — independent-review corrections

## outcome

Corrected two independently reproduced Stage 6 pass-1 defects on
`codex/core-v0.1` from exact HEAD `4920341899bc887b2ee165a169bde2a4b4c575eb`.
Normal Core import now admits the configured SQLite DLL before `sqlite3` loads.
Partial child Work and Artifact deletion now sanitize their dependent schema 5
content, historical copies, confirmations, receipts and managed backups while
retaining the parent's declared obligations. This is an engineering correction
for owner review, not acceptance of Stage 6 pass 1.

## evidence

The unchanged independent `review_stage6.py` was copied and run in new TEMP
`C:\Users\Anton\AppData\Local\Temp\zaratustra-stage6-repro-808b33d0fa5d4ff18060a8ec2b49834b`.
Before correction it reproduced the failing normal import (candidate SQLite
3.50.4), child goal in both plan revisions and a new backup, and Artifact
confirmation basis in the live store and new backup. The control baseline
imported SQLite 3.53.3; all probes used fictional local content and zero HTTP.

After correction, `tests/zaratustra/foundation/test_composition.py` covers the
fresh process without sitecustomize, both partial deletions through public Core
operations, exact unaffected history, addressably unavailable sanitized
history, reopened obligations, blocked dependent actions, receipt removal,
blocked replay, an independent confirmed role retained, closed SQLite bytes,
old backup purge and clean new backup.
The installed `dist/zaratustra-0.17.0-py3-none-any.whl` was installed with its
dependencies into new TEMP
`C:\Users\Anton\AppData\Local\Temp\zaratustra-stage6-final-wheel-efaf153d62bb4823bf8eb45b98cb8e9c`.
An isolated `-I -B` process, with only the normal configured DLL and no
sitecustomize or preload, imported Core from that wheel's `site-packages` and
reported SQLite 3.53.3. The final wheel SHA-256 was
`B7541142C3FBD194A24912500FDFA80C8262B28EC3AE861D91DE484F48C6B438`.
Full `uv run --locked python -m tools.check --deliver`
passed: 190 files formatted, Ruff and mypy clean, 21 import contracts kept,
536 tests passed in 267.93 seconds, and sdist/wheel built.

## assumptions

The repository specification remains the permanent copy with SHA-256
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
When a plan revision contains a terminally deleted dependency, its exact
address remains but a read returns `content_unavailable`; Core does not invent
a replacement historical meaning. A lost confirmation creates a new `open`
obligation revision. See
`docs/core-v0.1/STAGE6-PASS1-IMPLEMENTATION.md`.

## cuts

A parent whose current plan was sanitized cannot continue under that plan in
pass 1; no repair operation is added. No Stage 6 pass 2, Pi/DBOS composition,
real model call, modifying external tool or legacy migration was started.
`C:\projects\zaratustra` was not modified.

## cost

No paid service or external model call. Only the existing local SQLite DLL,
uv cache, synthetic data and new disposable TEMP workspaces were used.

## manual-acceptance

The owner's instruction authorized correction, checks and a local commit.
Neither passing checks nor this report records owner acceptance of Stage 6.

## next

solmax

# Core v0.1 Stage 6 pass 1 — engineering delivery

## outcome

Implemented the owner-authorized first model-independent Stage 6 pass on
`codex/core-v0.1` from exact HEAD `3ca96c8719a68acb6250d56731b457f0e12c4f6a`.
The candidate provides schema 5 Method versions, composite Work plans, atomic
obligations, dependent child issue, exact evidence confirmation and separate
parent acceptance. It is ready for owner review; this report does not record
owner acceptance of Stage 6 or a synthetic Work.

## evidence

The repository specification SHA-256 is
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
The branch and starting HEAD matched the request. The implementation and
public API are described in
`docs/core-v0.1/STAGE6-PASS1-IMPLEMENTATION.md`. The model-free
`tests/zaratustra/foundation/test_composition.py` passes seven scenarios,
including restart in another process, early B refusal without Attempt/outbox,
cycle, missing child with persistent obligation, stale plan/Artifact,
revoked Grant, version pin, exact parent output, replay/conflict, atomic
rollback, backup, quarantine restore, child/parent/Method deletion and
sanitation. The final `uv run --locked python -m tools.check --deliver`
passed with the exact SQLite 3.53.3 runtime: 190 files formatted, Ruff clean,
mypy clean across 164 source files, 21 import contracts kept, 531 tests passed
in 251.24 seconds, and sdist/wheel built. The report structure gate passed.

## assumptions

The first pass uses a direct Core `IssueChildWorkRequest` to represent subject
issue. It creates no external Attempt. A parent result is bound to an exact
accepted child output in this pass. Method `always` applicability is the
only supported applicability value; unsupported structural conditions are
rejected. Trusted local authority is established outside model/request text.
The existing exact SQLite 3.53.3 DLL was selected only for local checks.

## cuts

No Stage 6 pass 2–4 implementation, Pi/DBOS composition, real model call,
modifying external tool, legacy `.zara` migration, waiver, running-plan
revision, nested composite Work, Method transition, or Activity completion.
`C:\projects\zaratustra` was not modified.

## cost

No paid service or external model call. The changes use the existing SQLite,
Pydantic, uv and test tooling; only synthetic local test data and managed
temporary workspaces were used.

## manual-acceptance

The owner's request authorized implementation, local checks and a local
commit for pass 1. Automated success and this report are engineering evidence,
not owner acceptance. Owner review is required before treating Stage 6 pass 1
as accepted or starting a later pass.

## next

solmax

# Core v0.1 — приёмка Stage 5 и план Stage 6

## outcome

Записана приёмка владельцем четвёртого прохода и всего Stage 5 в объёме STAGE5-PLAN.md на точном коммите 245e7288c08eb4d49c84edd89f01e746a45c37a5. Отдельное решение о синтетическом Work не принималось. Подготовлен STAGE6-PLAN.md с первым модельно-независимым сквозным проходом и обслуживанием новых данных в каждом проходе. Код Stage 6 не менялся.

## evidence

Работа велась в ветке codex/core-v0.1 рабочего дерева C:\my_global_workflow\core-v0-1\zaratustra, исходный HEAD 245e7288c08eb4d49c84edd89f01e746a45c37a5. Прочитаны AGENTS.md, IMPLEMENTATION-BASELINE.md, STAGE5-PLAN.md, STAGE5-IMPLEMENTATION.md и публичные модели foundation. Спецификация найдена по уточнённому владельцем пути в docs/core-v0.1; SHA-256 точно совпал с заданным. Сверены её предметные разделы Work/Method, AD01/Q01, состав V1 и начало собственной разработки; по этой сверке скорректирован план.

Первый tools.check --deliver остановился при сборе тестов: системный SQLite 3.50.4 вместо закреплённого 3.53.3. Существующий проектный sqlite3.dll проверен по SHA-256 79FD9EC89DBA3F8BD64529A2CA8E9DDE6AE6EDC486C55A1D3F1CE77975A8375C, совпадающему с runtime.py. Повторный полный uv run --locked python -m tools.check --deliver с ZARATUSTRA_SQLITE_DLL прошёл: 188 файлов форматированы, Ruff и mypy чистые, 21 контракт сохранён, 524 теста прошли за 231.44 секунды, sdist и wheel собраны. После полного gate менялись только документы по прямой сверке спецификации; staged diff проверен git diff --cached --check, полный gate после этих редакторских правок не повторялся.

## assumptions

Исходная спецификация проверена по точному SHA-256. Номер будущей добавочной схемы и последовательность проходов — инженерная рекомендация, поскольку спецификация не предписывает wire-схему или порядок разработки. Путь из запроса с экранированными подчёркиваниями соответствует существующему C:\my_global_workflow\core-v0-1\zaratustra.

## cuts

Не было реализации Stage 6, миграции данных, настоящих модельных вызовов, доступа к личному рабочему пространству или изменений C:\projects\zaratustra. Проверка качества не доказывает семантику будущего Stage 6; его критерии сейчас являются планом.

## cost

Ноль платных вызовов. Использованы существующий локальный DLL и локальный PROBA gate.

## manual-acceptance

Владелец явно принял Stage 5 pass 4 и Stage 5 целиком на указанном коммите. Синтетический Stage 5 Work этим решением не принят и остаётся proposed до отдельной предметной операции. Stage 6 пока имеет только право на планирование.

## next

solmax

# Core v0.1 Stage 5 pass 4 — restored unknown stop correction

## outcome

The independent review of `371738507cd12f93d3fbbafec635fc657fdd19e3`
was reproduced in a new temp directory. A current recovery owner can now
confirm that the exact restored `unknown` Pi assignment stopped, then create
a linked new Attempt. The old Attempt and authority stay fenced; an unknown
model invocation and its reserve remain unknown and held. This is a pass-4
correction for owner review, not owner acceptance of pass 4 or Stage 5.

## evidence

The unchanged review reproduction in
`C:\Users\Anton\AppData\Local\Temp\zaratustra-pass4-repro-7fdf3e842b234c219faa1058e7d1f965`
returned `stale_attempt` for restored stop confirmation and `resource_busy`
for a linked Attempt before the fix; its evidence is also in
`_scratch/stage5-pass4-correction-repro-evidence.json`. The focused regression
now passes with
current owner and revoked-right checks, exact revision, audit, receipt, replay,
held reserve and late old-Attempt denials. The Core/Pi regression subset passed
33 tests. The installed outside-checkout wheel trial in
`_scratch/stage5-pass4-correction-installed-a/report.json` passed with Core,
DBOS and Pi imports from temporary `site-packages`, restored `unknown`, old
authority refusal, confirmed `stopped`, linked next Attempt and no extra HTTP.
The final `uv run --locked python -m tools.check --deliver` passed: 188 files
formatted, Ruff clean, mypy clean across 162 source files, 21 import
contracts kept, 524 tests passed in 230.53 seconds, sdist and wheel built.

## assumptions

The trusted local adapter establishes the fresh recovery owner; Core checks
the latest Recover actor and current `work.execute` Grant. Stop confirmation
means observed Pi termination. It does not decide the external model outcome.
The trial uses synthetic records and localhost provider.

## cuts

The external outcome and 20-unit reserve in the focused reproduction remain
unknown and held after Pi stop confirmation. No real provider outcome was
reconciled. The previous pass-4 limits on arbitrary third-party SQLite writes,
external copies and untested interleavings remain. No next stage or migration
began; `C:\projects\zaratustra` was not changed.

## cost

Zero paid or real model calls. Reused the existing Core operation, current
Grant evaluation, audit/receipt transaction and installed Core/DBOS/Pi trial.

## manual-acceptance

The owner accepted passes 1–3 separately. Pass 4 and Stage 5 as a whole still
await owner decisions. The synthetic Work remains `proposed`.

## next

solmax

# Core v0.1 Stage 5 pass 4 — shared maintenance boundary

## outcome

The owner accepted pass 3 at exact commit
`8a34c09f342059e4cedc1011e81b83a4272fd289` separately; the record is
`docs/core-v0.1/STAGE5-PASS3-ACCEPTANCE.md`. The authorized fourth pass now
holds one managed maintenance boundary around DBOS delivery/host, active Pi,
backup and deletion. The format-2 backup records both SQLite snapshots, managed
Pi files, hashes, versions and an end-of-publication marker. Restore remains
quarantined in a new epoch; `unknown` retains exclusive resource ownership.
This is engineering delivery for owner review, not acceptance of all Stage 5.

## evidence

`docs/core-v0.1/STAGE5-IMPLEMENTATION.md` records the exact changes,
observations and proof limits. Focused tests covered active lock refusal, DBOS
writer refusal with payload retained, incomplete package refusal after inventory
commit, a killed pre-marker backup followed by a successful new run, restored
question/remainder/usage/unknown and old ownership fencing.
`_scratch/stage5-pass4-rpc-a/report.json` passed the ordinary localhost Pi RPC,
technical backup, quarantine restore and managed deletion. The seven crash and
replay groups passed twice in `_scratch/stage5-pass4-faults-a/report.json` and
`_scratch/stage5-pass4-faults-b/report.json`. Stop cases passed in
`_scratch/stage5-pass4-stop-a/report.json`, preserving active transmitted HTTP
as `unknown` with 1000 held units. The installed wheel probe in
`_scratch/stage5-pass4-installed-e/report.json` verified Core/DBOS/extension
imports from outside checkout, technical backup, epoch 2 restore, deletion of a
managed backup and restored `unknown` with `resource_busy`. Wheel SHA-256:
`c064f48f993c443e74472f19075bb8b98d687691a8be3a10a081dd8d10a80b60`.
The final full `uv run --locked python -m tools.check --deliver` passed:
188 files formatted, Ruff clean, mypy clean across 162 source files,
21 import contracts kept, 523 tests passed in 270.62 seconds, and sdist/wheel
built.

## assumptions

All product-managed assigned and interactive Pi paths use the shared lock.
Direct third-party DBOS SQLite writes outside this lock are not coordinated;
the bounded SQLite writer/checkpoint probe refuses an already busy database.
The installed probe uses pinned SQLite 3.53.3, DBOS 3.0.0, ordinary Pi 0.87.0
and localhost synthetic HTTP/SSE.

## cuts

The older fault runs `l/m` recorded `disk I/O error`/`database is locked`, but
their saved host logs do not identify a unique parent-side stack. The two new
passing runs and handle/lock fixes do not prove every Windows interleaving.
External copies, provider retention, OS snapshots, real credentials, paid model
calls and mutating tools were not exercised. A copied technical format-1 backup
without the new common boundary is refused. No next stage or migration began.

## cost

Synthetic localhost only; zero paid or real model calls. Reused Core operation,
SQLite Backup API, existing DBOS queue/wait/Client and ordinary Pi RPC.

## manual-acceptance

The owner accepted Stage 5 passes 1–3 separately. Pass 4 and Stage 5 as a whole
await separate owner decisions. The synthetic Work remains `proposed`.

## next

solmax

# Core v0.1 Stage 5 pass 3 — independent review corrections

## outcome

The two remaining findings at HEAD `0a34f5a` were reproduced and fixed within
the authorized third pass. A stopped or revoked assigned Attempt now interrupts
the live ordinary Pi RPC while waiting or reading an active turn. Core records
`stopped` only after observed child exit; an uncertain transmitted model request
remains `unknown` with its reserve and exclusive resource. A new outside-checkout
wheel probe exercises installed Core → DBOS → ordinary Pi. Pass 3 awaits owner
acceptance. Stage 5 and Work are not accepted; pass 4 was not started.

## evidence

`docs/core-v0.1/STAGE5-IMPLEMENTATION.md` gives the per-scenario observation
and proof limit. Before the fix, `_scratch/stage5-pass3-stop-repro-a/evidence.json`
showed `stop_requested` with both host and Pi still live five seconds later;
only an artificial DBOS answer woke the waiter. The old installed probe covered
only schema-1 CLI. The corrected `_scratch/stage5-pass3-stop-c/report.json`
shows waiting stop → `stopped`, child exited, one HTTP and preserved
question/remainder/partial text; active stop → child exited but `unknown`, one
transmitted HTTP and 1000 held units; revoked runner → child exited, owner
separately reconciled `stopped`. Exact stop replay kept one receipt. Four new
focused regressions cover control rights, active read, failed child stop and
ordering before DBOS wake.

`_scratch/stage5-pass3-faults-n/report.json` passed the seven earlier
crash/replay groups again: checkpoint restart, post-claim crash, live host death
with late output rejection, host overlap, incompatible version, revoked resume,
and lost HTTP response. Two earlier repetitions hit concurrent SQLite errors;
the monitor now uses a narrow Core read transaction. The installed wheel trace
`_scratch/stage5-pass3-installed-clean/report.json` confirms `python -I`
outside checkout, package/DBOS/extension modules under temp `site-packages`,
ordinary Pi 0.87.0 and DBOS 3.0.0, two addressed answered HTTP turns, exact
output, 280 committed/zero held units, Work `proposed`, one duplicate-answer
receipt, and post-claim restart `unknown` without another HTTP. Wheel SHA-256:
`5cd865231a30c6bf0cfec483c8d807dede7a677be6af513b6a8b440a88a46963`.

The final full `uv run --locked python -m tools.check --deliver` passed:
188 files formatted, Ruff clean, mypy clean across 162 source files,
21 import contracts, 516 tests in 289.15 seconds, sdist and wheel built.

## assumptions

The control monitor observes one claimed Attempt and current Core rights. It
can stop a child whose host is live; a dead host's orphaned Pi identity and
provider-side effect remain unknown. A revoked runner cannot record a stop;
the owner must make a current-rights, address-specific reconciliation after
observing process exit. The installed probe uses pinned SQLite 3.53.3 and
localhost synthetic HTTP/SSE.

## cuts

The observations cover concrete Windows schedules, not every crash
interleaving or a real provider's processing after a lost response. Earlier
fault repetitions saw SQLite lock/I/O transients; one passing rerun does not
prove those impossible. The installed probe confirms the shipped composition,
not all recovery windows. No real model call, personal data, mutating tool,
two-SQLite global maintenance of pass 4, dispatcher, or change to
`C:\projects\zaratustra` was made.

## cost

Synthetic localhost HTTP only; zero paid or real model calls. Reused Core
operation/receipt, DBOS queue/wait and ordinary Pi RPC.

## manual-acceptance

The owner accepted passes 1 and 2 separately. The third pass and Stage 5 as a
whole still await separate decisions; synthetic Work remains `proposed`.

## next

solmax

# Historical Core v0.1 Stage 5 pass 3 — initial fault pass

## outcome

The owner accepted Stage 5 pass 2 at
`b0b923bfb4c4b42e448b2a5a61dcc39dd3937c3e` separately; the exact record
is `docs/core-v0.1/STAGE5-PASS2-ACCEPTANCE.md`. The authorized pass 3 tested
the real local Core → pinned DBOS 3.0.0 → ordinary Pi 0.87.0 RPC composition
under synthetic crash/replay, lost-response, revocation, late-result, resource
and version cases. A Core launch claim now fences recovery before a child can
start. Unknown provider outcome retains reserve and exclusive resource.
An incompatible Pi package refuses before DBOS launch. This is engineering
completion for owner review, not acceptance of pass 3 or all Stage 5.

## evidence

`docs/core-v0.1/STAGE5-IMPLEMENTATION.md` records the working plan, changes,
per-scenario observed outcome and proof limit. The reproducible machine trace
is `_scratch/stage5-pass3-faults-j/report.json` with host logs. Its checkpoint
restart yielded one Work with two answered HTTP invocations and one answer
receipt; post-claim crash yielded `unknown` and zero HTTP; live host death
left one HTTP, saved answer/remainder and rejected the late output; dropped
HTTP response left one `unknown` invocation, 1000 held units and no second
send on restart. Conflict on the same directory was refused while an
independent directory was assignable. Revocation before resume sent no second
HTTP. A second host entered while the first Pi was live and waiting;
the first model turn was not duplicated and one addressed answer produced one
continuation. Both incompatible executor assignment and mismatched Pi package
refused before provider use. `_scratch/stage5-pass3-baseline-live/report.json` retained
the ordinary two-HTTP positive path and managed deletion/restore checks.

The full `uv run --locked python -m tools.check --deliver` passed: 185 files
formatted, Ruff clean, mypy clean across 159 source files, 21 import contracts
kept, 512 tests passed in 239.94 seconds, and sdist/wheel builds succeeded.
The installed `uv run --locked python -m tools.probe_install` passed outside
the checkout with wheel SHA-256
`1d355aaf6de15c31c535c5fdb8e4c09d139e54d7d687419aa8ca0d4a88622632`.

Повторная проверка на том же коммите
`cec0040423d3627397b107c1206fe1edbc4c6a9c` сохранила новую локальную
трассу `_scratch/stage5-pass3-faults-k/report.json`. Все семь групп
наблюдений совпали с предыдущей трассой после исключения случайных UUID
квитанции и независимой Attempt. Повторный полный `tools.check --deliver`
прошёл: 512 tests за 252.47s, 21 import contracts, Ruff/mypy и обе сборки.
Повторный outside-checkout `tools.probe_install` прошёл с тем же SHA-256 wheel;
он по-прежнему доказывает только установленную базовую CLI/упаковку.

## assumptions

The test uses one Windows host, one pinned DBOS application version, ordinary
upstream Pi RPC and a local HTTP/SSE fixture. The Core claim receipt uses a
stable Attempt operation id and a fresh nonce on each actual DBOS workflow
entry. An earlier claim is treated conservatively as unknown even if the
host died before the child existed. Unknown is resolved only by a later
addressed observation or owner action.

## cuts

The probe does not prove every crash interleaving, liveness of an orphaned
Pi after host death, a real provider's processing outcome, credentials, paid
model use, mutating tools, or upgrade of a live binary. When the runner Grant
is revoked, its technical stop cannot be recorded under that revoked
identity: assignment stays `ready`, its resource stays held, and a trusted
owner action is required. The two-SQLite global maintenance boundary belongs
to separately authorized pass 4 and was not begun. The outside-checkout probe
checks installed base CLI behavior, not Pi/DBOS recovery.

## cost

Seven synthetic localhost model HTTP requests in the final fault probe; zero
real model or paid calls. No real personal data, old workspace, Direction OS,
remote publication, or `C:\projects\zaratustra` was changed. The code
reuses Core operation/receipt, DBOS queue/wait and ordinary Pi RPC surfaces;
no dispatcher or model loop was added.

## manual-acceptance

The owner accepted passes 1 and 2 separately. Pass 3 awaits owner review;
Stage 5 as a whole and every synthetic Work remain unaccepted.

## next

solmax

# Core v0.1 Stage 5 pass 2 — independent review corrections

## outcome

At reviewed HEAD `c5ec9de25a4f8b1782c1bbe8fc073e647468dc6b`, four
reproducible pass-2 defects were reported and independently reproduced in a
fresh temporary synthetic space. Current `maintenance.delete` admission now
precedes technical cleanup; one captured batch controls Core jobs, DBOS/Pi
targets and contaminated backups; Artifact/Work deletion preserves payload-free
Attempt addresses before erasing their references; and a deleted unrelated Work
no longer blocks a live assigned Attempt. A related technical backup gap found
during this correction was closed: no new DBOS/Pi backup can copy payload while
any Core deletion is pending. This is engineering completion for owner review,
not acceptance of pass 2 or Stage 5.

## evidence

The independent `READONLY-REVIEW.md`, `review_probes.py` and `evidence.json`
were read from the owner-specified temporary directory. A fresh copy of that
script in a new empty temporary directory reproduced all four original
failures, including one unauthorized DBOS workflow/home removal, a second Work
finalized without technical cleanup, retained partial payload from a stopped
assignment and a live launch blocked by an unrelated tombstone.

Behavioral regressions now cover missing and revoked maintenance Grants,
interleaved deletion B staying pending until its own cleanup, the stopped
partial Artifact with unrelated DBOS/Pi data retained, and a live assigned
Attempt resolved past a deleted Work. The technical backup test first failed
against the implementation, then passed after a pre-backup pending check. The
accepted Stage 2 interleaved-backup test still passes. The synthetic localhost
Pi RPC replay in `_scratch/stage5-pass2-correction-live/report.json` passed:
one HTTP before the saved question, none for the parallel interactive display,
one after the addressed answer, zero HTTP for the pre-send denial, final Work
`proposed`, observed assignment `stopped`, and clean managed deletion/restore.
The original reviewer script was copied again into a new empty temp after the
fixes. Its denied call left one workflow and Pi copy intact; B stayed pending
until the next run; the stopped partial left no workflow or managed payload.
The valid Attempt reached Pi RPC past the unrelated tombstone. Its deliberately
unavailable localhost provider produced `rpc_extension`. That run revealed a
DBOS error deserialization failure, now fixed by making `FoundationError`
round-trip with its original code and text; a regression covers the same
serialization. The final full `uv run --locked python -m tools.check --deliver`
passed: 184 files formatted, Ruff clean, mypy clean across 158 source files,
21 import contracts kept, 510 tests passed in 293.59 seconds, and source/wheel
builds succeeded.

## assumptions

Only one exact set of pending operation ids and contaminated backup ids is
selected under the maintenance lock. Core operations may still arrive after
the snapshot; they remain pending. The payload-free address entries use the
existing schema-4 Core maintenance-event table and are removed when their
jobs complete. DBOS remains a technical queue and workflow store.

## cuts

The pass-3 crash, restart, unknown-process, late-result and version matrix is
still unverified and was not begun. Pending schema-4 deletions written by the
earlier implementation without a preserved technical address fail closed as
`technical_state` when DBOS cleanup is required. No real provider, paid model,
credentials, personal data, old `.zara` migration, Direction OS or
`C:\projects\zaratustra` was used.

## cost

Two localhost synthetic model HTTP requests in the live replay; zero real
model or paid API calls. Changes reuse the existing Core operation, maintenance
event, DBOS Client and Pi RPC surfaces, plus behavioral regression tests.

## manual-acceptance

The owner accepted Stage 5 pass 1 separately. Pass 2 remains unaccepted and
awaits review of this correction. No synthetic Work was accepted. Stage 5 is
not declared complete.

## next

solmax

# Core v0.1 Stage 5 pass 2 — one assigned ordinary Pi RPC

## outcome

The owner accepted Stage 5 pass 1 at
`03a3887f7d50aa1dccf8c761ce5bc933a57f86c9` with the words
“Ну, если можно принимать, то я принимаю”. That acceptance is recorded
separately in `docs/core-v0.1/STAGE5-PASS1-ACCEPTANCE.md`; it is not acceptance
of all Stage 5. The owner then authorized pass 2. This pass connects one
Core-assigned Work and committed outbox to pinned DBOS 3.0.0 queue/wait/send
and a separate ordinary upstream Pi 0.87.0 RPC. The interactive Pi remains
available for another conversation and can display/answer the saved question.
Core remains the sole subject original. The Work result is an exact Artifact
and stays `proposed` for separate acceptance. No custom dispatcher was added.

## evidence

The source specification still has SHA-256
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
`docs/core-v0.1/STAGE5-IMPLEMENTATION.md` records the implemented boundary.
The real Pi RPC/localhost SSE trace in ignored
`_scratch/stage5-rpc-live-f/report.json` observed one model HTTP before the
question and one after its addressed answer. A second ordinary Pi RPC displayed
the Core question without a model HTTP. Repeated DBOS enqueue and repeated
answer caused no extra provider send. Both HTTP body digests matched Core
invocations; 280 reported units were committed with zero held. The final
Artifact was exact, Work stayed proposed and the child exit was observed
before Core recorded `stopped`. A separate over-budget assigned Attempt kept
one `prepared` invocation and sent zero HTTP. Its expected DBOS workflow error
is not counted as a successful Work.

The trace also verified a hashed Core+DBOS+Pi-home backup, quarantine restore
with epoch 2 and inert old executor archive, targeted DBOS workflow deletion
through its Client API, removal of the affected Pi home and backup, Core/DBOS
SQLite integrity after sanitation, and deletion of restored technical archives.
Core-only deletion explicitly refused when the DBOS cleanup adapter was absent.
Focused Core/bridge/assigned regression tests passed (21 tests). The full
`uv run --locked python -m tools.check --deliver` passed: 184 files formatted,
Ruff clean, mypy clean across 158 source files, 21 import contracts kept,
503 tests passed in 285.34 seconds, and source/wheel builds succeeded.

## assumptions

DBOS carries addresses, queue state and wake signals; Work prose, answer and
result remain in Core and the controlled Pi process. The synthetic provider
uses ordinary HTTP/SSE with the Stage 4 guarded Provider wrapper. `stopped`
is written only after observing process exit; an unobserved stop writes
`unknown` and retains the Core resource. The tested backup is cold with no
active Pi process. SQLite is the selected local single-host DBOS backend for
this limited composition, not a whole-system suitability decision.

## cuts

The pass 3 crash matrix remains unverified: host death around Core commit,
DBOS checkpoint and child launch, HTTP response loss, DBOS/Pi restart,
mid-turn Grant revocation, late result, competing resources, unkillable child,
and incompatible workflow version. The pass 4 coordinated hot-maintenance
matrix and outside-checkout probe remain outstanding. A real provider/auth,
paid model, modifying tool, personal data, old `.zara` migration, Direction OS
or `C:\projects\zaratustra` was not used. The tested localhost/offline profile
is a verification choice; provider/model/limits are configurable. Assigned
tools currently admit none or explicitly selected read-only Pi tools until
durable modifying-effect control is implemented.

## cost

Two synthetic localhost model HTTP requests and one pre-send refusal with
zero HTTP. Zero real model calls, paid API calls or user credentials.
The installed addition is pinned DBOS 3.0.0, a bounded Pi RPC client/bridge,
technical backup/deletion coverage, one probe and focused tests.

## manual-acceptance

The owner already accepted pass 1, independently of this result. Engineering
evidence for pass 2 awaits the owner's review; no Work in the synthetic trace
was accepted. Reproduce with the command in `STAGE5-IMPLEMENTATION.md`, inspect
the Core Artifact, invocation ledger, question/answer/outbox, backup/restore
and deletion report, then decide whether to accept pass 2. This result does
not declare Stage 5 complete.

## next

solmax

# Core v0.1 Stage 5 pass 1 — deletion and acceptance corrections

## outcome

The owner requested two corrections within the already authorized first pass,
starting from `d04c87534a279e4e666075c8e663edf62dce4f47`. Deleting a dependent
Artifact now clears `stop_reason` from every affected assignment, including
`stopped` and `interrupted`, while retaining its known status. Accepting Work
does not assert that an independently assigned executor has stopped. An
`unknown` assignment and its active Attempt retain the exclusive resource;
addressed `RecordAttemptStopRequest(stopped)` remains available after acceptance
and releases it. Acceptance closes pending delivery and requests stop for other
active assignments. The interactive Stage 4 acceptance path still interrupts
its non-assigned Attempt.

## evidence

Both supplied synthetic probes were reproduced on the clean base before editing:
the stopped assignment exposed `stop_reason` after `complete_deletions`, and
acceptance released a resource despite `unknown` and prevented later stop
confirmation. New regressions failed on those exact assertions before the fix.
Twelve focused synthetic tests now pass. They cover both terminal statuses,
receipt replay removal, API and closed SQLite bytes after deletion, purging an
affected managed backup, the `unknown → resource_busy → accept → resource_busy
→ stopped → assign` sequence, stop requested by acceptance, and unchanged
interactive Stage 4 acceptance. Full
`uv run --locked python -m tools.check --deliver` passed: 181 formatted files,
clean Ruff, mypy on 155 source files, 21 kept import contracts, 500 tests,
source/wheel build and report structure. `git diff --check` passed.

## assumptions

An accepted Work result is distinct from proof that its assigned process died.
With no Pi RPC in this pass, `RecordAttemptStopRequest(stopped)` is a trusted
subject assertion; a real process outcome gate remains for a later pass.

## cuts

No Pi RPC, DBOS dependency, dispatcher, real model call, personal data, old
`.zara` migration, Direction OS edit or `C:\projects\zaratustra` edit. No Stage 5
second-pass work was started.

## cost

Local synthetic tests and documentation only; zero model/service calls and
zero paid cost.

## manual-acceptance

The owner authorized the corrective implementation and local commit. The
engineering checks do not constitute owner acceptance of Stage 5 pass 1.
Stage 4 acceptance remains recorded separately.

## next

solmax

# Core v0.1 Stage 5 pass 1 — durable subject continuation

## outcome

The owner approved pass 1 of `docs/core-v0.1/STAGE5-PLAN.md` from exact commit
`e29bc0450ce4ee6c67e5e28859c8e410a82f1771`. An explicit additive schema 4
now stores addressed independent Attempt assignments, questions/answers,
remainders, stop state and pending/cancelled outbox entries in the independent
foundation. They use the existing operation/audit/receipt transaction and current
rights. No Pi RPC, DBOS runner or dispatcher was introduced. The accepted
interactive Stage 4 bridge and direct execution API also admit schema 4, while
schema 3 spaces remain readable and usable without automatic upgrade.

## evidence

`docs/core-v0.1/STAGE5-IMPLEMENTATION.md` records the public surface and
maintenance boundary. Seven focused synthetic tests pass. One test reopens the
space in a new Python process, reads the durable question and remainder, saves
the answer and proves one `resume` outbox entry; exact replay does not create a
second. The others exercise explicit upgrade and Stage 4 bridge compatibility,
replay rights, revoked answer rights, stop/unknown reserve, stale Attempt fencing,
backup/quarantine/epoch and Work/Artifact deletion. The full
`uv run --locked python -m tools.check --deliver` passed with the verified SQLite
runtime: 181 format-clean files, clean Ruff, strict mypy on 155 source files,
21 kept import contracts, 495 passed tests, source/wheel build and report
structure. Independent review identified that a responder could create an
answer without the `receipt.read` right needed to replay its receipt after a
lost acknowledgement. Admission now requires this current right; a focused
test covers refusal without it and exact retry with it. This check is
engineering evidence, not owner acceptance.

## assumptions

The first pass records a pinned future executor version but never starts that
executor. An assignment and launch outbox item are durable intentions, not proof
of a live Pi child or external effect. The current Work stays `proposed` until
separate acceptance; actual `ready/running/waiting` Work transitions require
the next pass with observable process outcomes.

## cuts

No real model call, personal data, Pi RPC, DBOS production dependency, custom
dispatcher, old `.zara` migration, Direction OS change or
`C:\projects\zaratustra` change. Outbox delivery, live process ownership and
two-database maintenance remain for separately authorized later passes.

## cost

Local source, synthetic tests and documentation only; zero model/service calls
and zero paid cost.

## manual-acceptance

The owner authorized implementation and local delivery of pass 1. Focused and
full engineering checks are evidence, not owner acceptance of the completed
pass. The earlier Stage 4 owner acceptance remains separate.

## next

solmax

# Core v0.1 Stage 5 plan and Stage 4 owner acceptance

## outcome

The owner explicitly accepted the whole Stage 4 implementation at
`afa367c10ad949b1610810d4f2e6f106c1a7b7cc`, independently of the earlier
acceptance of one synthetic Work. `docs/core-v0.1/STAGE4-ACCEPTANCE.md` records
the exact words and scope. `docs/core-v0.1/STAGE5-PLAN.md` defines the next
minimal path: a separately assigned ordinary Pi RPC Attempt with a durable
question/answer, Core-owned state, DBOS-backed technical continuation subject
to an explicit backend decision, and recovery/maintenance gates. Stage 5 was
not implemented or executed in this planning pass.

## evidence

The specification SHA-256 was rechecked as
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
The Stage 4 HEAD was verified as
`afa367c10ad949b1610810d4f2e6f106c1a7b7cc` on `codex/core-v0.1`.
The plan was compared with the implementation/technical baselines, Stage 4
plan and implementation, scenario 9 machine report, RESULT history, and the
current public `foundation`/`pi_adapter` surfaces. `git diff --check` passed;
all referenced local files exist. The full
`uv run --locked python -m tools.check --deliver` passed with an isolated uv
cache and the previously verified SQLite runtime: 180 format-clean files,
clean Ruff, strict mypy on 154 files, 21 import contracts, 488 tests, source
and wheel build, and report structure. The default and reused uv caches were
inaccessible in this sandbox; the isolated cache resolved that setup error.

## assumptions

The first Stage 5 scenario uses one existing method-less Work and a fictional
read-only resource. DBOS 3.0.0 is the recommended conditional integration
candidate after its positive synthetic scenario 9; production suitability
still needs the real composition checks and an owner decision.

## cuts

No production code, DBOS production dependency, independent execution,
model call, personal data, old `.zara` migration, Direction OS edit, or
`C:\projects\zaratustra` edit is part of this pass. The full accepted Core
composition remains future work as detailed in the plan.

## cost

Documentation and local checks only; zero model/service calls and no paid cost.

## manual-acceptance

Stage 4 is accepted by the owner's explicit words; the synthetic Work was
accepted separately. This planning pass does not accept Stage 5 or approve its
production backend, model, resource or real data.

## next

solmax

# Core v0.1 Stage 4 proposed-output regeneration correction

## outcome

A new explicitly started Attempt on the current revision of a proposed Work
can replace an occupied output slot after an answered invocation. Core still
checks the exact Attempt basis and writes the Artifact and Work link in one
transaction. Earlier Artifacts and Work revisions remain readable. A Work
changed after Attempt start rejects late publication; `succeeded` remains
closed to new Attempts.

## evidence

The new synthetic regression failed before the fix at the unconditional
occupied-slot `stale_work` check. After the fix, it published a second answer
at Work@3, retained Work@2 and both exact Artifact payloads, kept one link for
the slot, replayed both publications exactly and explicitly accepted only the
new revision. The existing competing-link, deletion and acceptance checks
also passed in the focused 15-test run. The first full delivery run
reported 487 passed and one unrelated intermittent catalog concurrency failure
(`test_relocation_preserves_concurrently_added_independent_row`); that test
passed alone. The repeated full `tools.check --deliver` passed: 488 tests,
180 format-clean files, clean Ruff, strict mypy on 154 files, 21 import
contracts, source/wheel build and report structure.

## assumptions

A revised result requires an explicit new Attempt begun at the current
`proposed` Work revision. Operation replay uses the saved receipt before any
current-state validation, so a prior successful publication remains replayable.

## cuts

The correction changes only interactive Stage 4 publication. It adds no
executor, DBOS integration or model/provider contract.

## cost

Synthetic local tests only; zero real model calls and zero paid API calls in
this correction. The earlier owner-run synthetic Work used 953 reported units,
zero held after its answered call.

## manual-acceptance

The owner already accepted the earlier synthetic Work: revision 3 is
`succeeded`, with its explicit basis, original Artifact, 953 committed units,
zero held and no active Attempt. This does not constitute owner acceptance of
the full Stage 4 implementation. No repeat acceptance is requested.

## next

solmax

# Core v0.1 Stage 4 restart and acceptance correction

## outcome

The owner's synthetic manual test confirmed the saved Artifact, current rights
and 953 units in a fresh Pi session. That session also exposed an unwanted
empty active Attempt on a proposed Work with a linked output. The adapter now
opens that Work for reading without starting an Attempt. Explicit acceptance
retires any active Attempt in the same Core transaction under `work.accept`.
The owner's Work
remains `proposed`; no acceptance was performed by engineering.

## evidence

The fresh status showed the same Work@2, output Artifact, one answered
`openai-codex/gpt-5.6-luna` SSE invocation, 953 committed units and zero held.
It also showed a second active Attempt with no invocations. That exact empty
synthetic Attempt was interrupted through the public Core operation; the Work,
output, invocation count and accounting remained intact. Focused regression
checks cover acceptance with an active Attempt by an actor without
`work.execute` and its retirement. The full
`tools.check --deliver` passed: 487 tests, 21 import contracts, strict typing,
hygiene and source/wheel build.

## assumptions

An already linked proposed result is available for review after restart.
Starting another Attempt for revision remains an explicit `/zara-work` action.

## cuts

The ordinary Pi model loop and provider admission boundary are unchanged.
No next-stage executor or DBOS work was started.

## cost

The owner sent one additional subscribed Codex request against the synthetic
note: 953 reported units, zero held after its answer. Engineering sent no
model request in this follow-up. Total real subscribed sends across Stage 4
are now three; no paid API was used.

## manual-acceptance

The synthetic result is ready for the owner's own review and optional explicit
`/zara-accept`. Engineering did not accept it. After acceptance, inspect
`/zara-status` for `succeeded`, a basis and no active Attempt.

## next

solmax

# Core v0.1 Stage 4 correction

## outcome

Three reproduced Stage 4 defects are corrected in the local interactive path.
An unsupported Pi provider now stops before HTTP while a Work is selected.
Attempt publication checks its basis and links its Artifact in one Core
transaction, preserving a newer output. Adapter-created Pi session histories
are now managed in the selected Core space and retired during deletion
maintenance. The Work's successful result still requires a separate explicit
acceptance. No DBOS or independent RPC executor was added.

## evidence

Before editing, synthetic reproductions showed one unreserved HTTP request
after Pi switched to a second localhost provider; a late Attempt replaced a
different Work@2 output; and a Pi JSONL copy retained `fictional note` after
Work deletion. After the changes, the same ordinary Pi `set_model` switch made
zero provider requests and exposed no Core input. Focused tests prove a late
Attempt gets `stale_work`, leaves Work@2 and its exact bytes intact, creates no
late Artifact, and preserves exact publication replay. A synthetic managed
Pi JSONL file is removed by `complete_deletions`; maintenance refuses to run
while Pi owns the space. A supported localhost Pi run still made one observed
and admitted call, saved the output, and left Work proposed before its
synthetic-only acceptance step. The manual setup helper created a fresh
proposed synthetic Work in ignored `_scratch`.

The full `uv run --locked python -m tools.check --deliver` passed after the
core fixes. The final run checked 180 formatted files, clean Ruff, strict
mypy on 154 files, 21 import contracts, 486 tests, source/wheel build and
report structure.

## assumptions

The host has exclusive local ownership of the Core space while Pi runs.
Every host-created session is stored below `<space>/.zara-core/pi-sessions`;
deletion retires all such sessions because one transcript may mix material
from several Works. The same lock prevents concurrent Pi writes and cleanup.
Provider coverage is the ordinary Pi provider stream path used by this adapter.

## cuts

Historical Pi sessions created before this correction in arbitrary directories
cannot be discovered from Core because their paths were never recorded. They
need separate operator review and removal. Provider-side copies, manually
exported chats and filesystem snapshots are outside local Core maintenance.
Automatic publication remains limited to one textual output slot. No new
model/provider allowlist or fixed product budget was introduced.

## cost

Zero real model calls and zero paid API calls in this correction. All model
transport checks used synthetic localhost providers. The prior Stage 4 trial
spent two subscribed Codex sends: one unknown with 8,000 units reserved in
its separate scratch space, and one answered with 868 reported tokens.

## manual-acceptance

The exact PowerShell setup and launch commands are in
`docs/core-v0.1/STAGE4-IMPLEMENTATION.md`. They create a new synthetic space,
working directory, input Artifact, Activity and proposed Work inside ignored
`_scratch`, then start ordinary Pi. Review `/zara-status` before and after a
fresh Pi session. Check the linked Artifact, rights, current reserve and
proposed status. Only the owner may invoke `/zara-accept` for the reviewed
result; this correction has not accepted the owner's Work.

## next

solmax

# Core v0.1 Stage 4 interactive Pi implementation

## outcome

The owner accepted preparatory commit `adc725866bd8c3a7f1fc73e6f5c836febc0652a1`
and authorized the interactive Stage 4 path on `codex/core-v0.1` in
`C:\my_global_workflow\core-v0-1\zaratustra`. Ordinary upstream Pi 0.87.0 now
connects to independent Core through the installed Zaratustra extension and
loopback bridge. A selected Work receives exact current inputs, a persistent
Attempt and pre-send model reserve. A completed answer becomes an Artifact and
separately linked Work output. Work remains `proposed` until explicit acceptance.
No standalone RPC executor, production DBOS or dispatcher was added.

## evidence

The source specification still has SHA-256
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
`docs/core-v0.1/STAGE4-IMPLEMENTATION.md` describes the implemented boundary.
Focused tests cover current execution/model rights, stale inputs before send,
unknown reserve after a fresh session, explicit acceptance, and deletion.
The synthetic localhost Pi run used `/zara-work` to choose Activity/Work, matched
the SHA-256 of the final provider HTTP body to Core's pre-send invocation,
saved an answered result and left Work `proposed`. A separate `/zara-accept`
input and confirmation changed it to `succeeded` with a recorded basis. A
budget-refused run persisted `prepared` and sent zero provider HTTP requests.
The wheel includes `pi_adapter/extension.ts`.

The first subscribed Pi request used provider `openai-codex`, model
`gpt-5.6-luna`, SSE transport and low reasoning. Pi reported an invalidated
authentication token after send; Core kept that invocation `unknown` with its
8,000-unit reserve. The owner completed Pi's standard ChatGPT Plus/Pro (Codex)
login. A second subscribed request through the same provider/model/transport
answered the synthetic text with 868 reported tokens. Core saved `The note is
fictional.` as an exact Artifact and Work link. A fresh local authority read
the same `proposed` result, current Work rights and 868 committed / zero held
units from SQLite.
Only fictional scratch workspaces and the subscribed Codex path were used.

The final full `uv run --locked python -m tools.check --deliver` passed:
178 formatted files, clean Ruff, strict mypy on 152 files, 21 import contracts,
484 tests, source/wheel build and report structure. The final check includes
the current-rights snapshot and Pi's no-tools default.

## assumptions

The actor is established from an explicitly confirmed local console and OS
account, then bound to space id and execution epoch. The Codex Provider is forced
to SSE because Pi's default WebSocket path is not observable through the
available final-request `fetch` hook. A positive Pi auth readiness check can
still precede a server-side token rejection. Token units are the provider's
reported usage; a response above its reserve is still charged and prevents
further admission at an exhausted limit.

## cuts

Automatic publication covers one text output slot. Multi-slot and non-text
outputs need an explicit future output path. Unknown model outcomes retain
reserve and are not auto-refunded. This stage does not implement a standalone
RPC executor, DBOS connection, automatic resumption, provider-side data
retention, or Core-managed deletion of Pi's external session JSONL. It does not
read real user documents or modify `C:\projects\zaratustra`.

## cost

Two actual subscribed model HTTP sends out of the owner's ten-call trial cap:
one rejected after send with unknown usage and 8,000 units held in its separate
scratch Core space; one answered with 868 reported tokens and zero held in its
own scratch Core space. No service or retry calls were sent in the real trial.
Local synthetic provider runs are outside the actual model-call count. No paid
API key or paid API endpoint was used. Model/provider/budget paths and rights
remain configurable product inputs; trial values are not hard coded.

## manual-acceptance

In a fresh space authorized for the local console actor, choose a Work through
`/zara-work`, run one synthetic or otherwise authorized text summary, then
reopen Pi and inspect `/zara-status`. Check the saved exact Artifact, output
link, current rights, reserve and `proposed` Work. Use `/zara-accept` only after
reviewing the text, enter a basis and confirm the displayed Work revision and
output. Confirm `succeeded`, its receipt and an `ongoing` Activity. The
subscribed trial Work remains `proposed`; engineering evidence does not accept
that result or the Stage 4 implementation on the owner's behalf.

## next

solmax

# Historical Core v0.1 Stage 4 plan and scenario 9 check

## outcome

The owner accepted Stage 3 at `fa4c1773b0b4a8b75a03702be1e524cf8d5ea7a9`.
`docs/core-v0.1/STAGE4-PLAN.md` defines the first ordinary-Pi-to-independent-Core
user path and its implementation boundary. One new development-only Windows run
of DBOS scenario 9 was positive. No production Pi/DBOS integration or real model
call was started.

## evidence

The source specification SHA-256 matches
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
The single scenario 9 run used pinned Python 3.13.7, SQLite 3.53.3, DBOS 3.0.0
and the preserved synthetic executor migration 114. Its ignored output is
`_scratch/dbos-scenario9-windows-20260923-a`; the tracked report is
`docs/core-v0.1/STAGE4-DBOS-SCENARIO9-EVIDENCE.json` with SHA-256
`3404D652208A9AC7C2FC255F012D70BF435A290302E7B775DCA7766EE5DE6315`.
The payload was present before deletion. DBOS child exit/delete succeeded;
exclusive Windows opens succeeded before and after sanitation; both databases
reported integrity `ok` and zero freelist; managed payload rows were zero and
two closed-file scans found no sentinel. No targeted repeat or other scenario run
was made. The full `uv run --locked python -m tools.check --deliver` passed:
170 formatted files, clean Ruff, strict mypy on 146 source files, 20 import
contracts, 479 tests, source/wheel build and report structure. The report's
probe hash identifies the executed pre-formatting bytes; only formatting and
import order changed in the final stand file afterward.

## assumptions

The previous `database is locked` is consistent with stand code that committed
SQLite transactions without explicitly closing connections, followed by an
exclusive journal-mode switch. The old reports did not capture the precise
blocking handle or SQL stage, so that historical attribution is not claimed as
proven. The repaired stand closes connections explicitly and reports the stage
of any future SQLite lock. Stage 4's first user scenario is an interactive Pi
path; standalone durable RPC execution is a separate DBOS-dependent boundary.

## cuts

The check covers a single synthetic Windows fixture and managed live SQLite/log
files. It does not prove production runner integration, crash during sanitation,
unmanaged copies, SSD-level erasure or a real provider. No old `.zara`, personal
data, model account, provider call, custom dispatcher or `C:\projects\zaratustra`
change was used. The full accepted Core composition remains in scope beyond the
first scenario.

## cost

One new scenario 9 run, no repeat, no external service cost. Changes are a
development-only stand repair, its focused test, a tracked machine report,
Stage 4 plan and this report.

## manual-acceptance

The owner accepted Stage 3 and authorized this planning/check/commit pass.
The positive technical check does not constitute owner acceptance of a future
production Stage 4 implementation or selection of a real provider/resource.

## next

solmax

# Historical Stage 3 result

## outcome

Stage 3 is implemented on
`codex/core-v0.1` from the owner-accepted Stage 2 commit
`62a173c7d9a875e6709bf2e0dbe0d8d7e7fa31df`. It adds a minimal,
model-free Activity/Work contract in `zaratustra.foundation`. One Work can be
accepted explicitly without completing its continuing Activity. Subject
deletion follows the owner's explicit Stage 2 replay decision.

## evidence

The source specification hash is
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
`docs/core-v0.1/STAGE3-PLAN.md` records the plan and
`docs/core-v0.1/STAGE3-IMPLEMENTATION.md` records the actual boundary.

The synthetic `tools.probe_stage3` run in
`_scratch/stage3-activity-work-probe-final` created only fictional data. A new
process read Activity `ongoing`, Work `succeeded` at revision 3, exact input
and output Artifact references, the acceptance basis, its source, audit and receipt.
Both Artifact presence and output linking left the Work `proposed` until the
separate acceptance operation.

Six focused tests pass. They cover exact history, incomplete and mismatched
outputs, stale revisions, distinct read/write/accept rights, pre-existing
Stage 2 Grants, backup contamination, quarantined restore, deletion,
`history_unavailable` on old replay, `not_found` on old receipt reads,
exact deletion replay, retained audit, unaffected receipts, current rights
and unavailable result references. The full `uv run --locked python -m
tools.check --deliver` passed after the deletion-contract changes: 170 files
were format-clean, Ruff was clean, strict mypy passed across 146 source
files, all 20 import contracts held, 479 tests passed, source and wheel
artifacts built, and report structure passed.

## assumptions

Trusted local authority is established outside request content, as in Stage 2.
The Work acceptance basis is the authorized actor's statement. The program
checks exact references, media type, revisions, rights and declared slots; it
does not infer semantic success from a file. Deleting an Activity/Work removes
prior revision-forming receipts and content-derived fingerprints, keeps
minimal operation identity and permitted audit, and retains the deletion
operation's own content-free receipt.

## cuts

No Pi, DBOS, scheduler, dispatcher, automatic execution, Attempt, Method
definition, memory, Sleep, old `.zara` migration or real personal Activity was
introduced. Stage 3 Work states are `proposed` and `succeeded` only.

## cost

One additive strict SQLite schema upgrade, Activity/Work records and content,
public read/mutation contracts, six focused tests, one synthetic development
probe and documentation. Probe output remains in ignored local scratch.
No external service or model cost was incurred.

## manual-acceptance

The owner accepted Stage 2, authorized this Stage 3 and explicitly chose
Stage 2 deletion/replay semantics for Activity/Work. Engineering checks do
not establish owner acceptance of the completed Stage 3 implementation.

## next

solmax

## recommendation

The next separately authorized boundary is a real first-use adapter: select
an owner-approved new space, establish actual trusted-local identity and
confirmation, then expose creation, reading and explicit acceptance of these
records in the chosen developer workflow. Work execution, Attempts and any
Pi/DBOS runner integration require their own technical decision and are not
part of Stage 3.

# Historical Stage 2 result

## stage2-outcome

The owner-authorized Stage 2 implementation is ready for manual acceptance on
`codex/core-v0.1`, based on exact commit
`7ec6033d234b0e2e870f53796a9dad0711d3b468`. It implements an independent
`zaratustra.foundation` package and stops at the requested subject foundation. It
does not read, migrate or write the old `.zara` workspace and does not modify the
separate `C:\projects\zaratustra` checkout.

The foundation provides a new empty space identity and schema; exact immutable
Artifact revisions and provenance; versioned Decision and Grant records; current
Decision/Grant admission; one atomic operation/audit/receipt path; exact replay,
conflict and stale-revision behavior; separately authorized inspection and receipt
reads; verified backup; quarantined restore with execution-epoch rotation; fresh
recovery; terminal logical deletion; and physical cleanup of managed live and backup
payloads. No successor Activity/Work execution layer was admitted.

Implementation commits are `9df390a` (foundation), `f5159fb` (terminal deletion and
backup/delete race closure), `8a62bdd` (crash-consistent backup publication), and
`e3fd379` (exact-batch concurrent deletion cleanup).

## stage2-evidence

The implementation is grounded in specification file
`Zaratustra_Core_Specification_v0.1.md`, SHA-256
`1DDCBF9DBFD58426781F61A67A170D3CE85FCD2BA27B17549773ED3356F9428D`.
`docs/core-v0.1/STAGE2-PLAN.md` records the admitted plan and
`docs/core-v0.1/STAGE2-IMPLEMENTATION.md` records the actual contracts, operations,
storage and reproduction boundary.

The runtime gate accepts Python `3.13.7`, exact SQLite `3.53.3` and FTS5. The
development run used the official Stage 1 archive identity
`3a494861ce24d1f330efbc6c3fb58ce4972f2cf8df4e43122246ed987109dc8a`
(SHA3-256) and extracted DLL identity
`79FD9EC89DBA3F8BD64529A2CA8E9DDE6AE6EDC486C55A1D3F1CE77975A8375C`
(SHA-256). The DLL is admitted per process through `ZARATUSTRA_SQLITE_DLL`; it is
not vendored, downloaded by product code or installed globally.

The model-free synthetic probe at `_scratch/stage2-foundation-probe-b` passed on
SQLite `3.53.3`. It demonstrated exact replay, exact historical/current bytes,
receipt recovery, a verified backup, quarantine-only restore, epoch rotation from 1
to 2, fresh recovery and exact restored bytes. It made no model, provider, network or
old-workspace call.

Twenty-three focused foundation tests pass. They cover new/reopened spaces, unsupported
runtime/schema refusal, exact binary/text revisions, provenance, replay/conflict,
stale writes, current Decision/Grant behavior, atomic rollback, response recovery,
concurrency, tamper refusal, quarantine/recovery, terminal deletion, backup/delete
interleaving and both backup publication crash windows. In particular, a renamed
package remains non-restorable before inventory commit and becomes verifiably
complete immediately after commit. The corrective deletion tests reproduce the
confirmed cleanup/finalization race, require concurrent work to remain pending for a
second run, and prove retry after interruptions on both sides of status finalization.

The final full `uv run --locked python -m tools.check --deliver` run passed: 168
files were format-clean, Ruff was clean, strict mypy passed across 144 source files,
all 20 import contracts were kept, all 473 tests passed, source and wheel artifacts
built, and delivery-report structure passed. Wheel inspection confirms the
foundation package is installed while development tools remain excluded.

The required bounded setup evaluator found no setup blocker. Independent adversarial
review first found two Critical defects (deleted-record resurrection and a
backup/delete publication race) and then one Important inventory/publication crash
window. Commits `f5159fb` and `8a62bdd` fixed them with deterministic regression
tests. Final bounded re-review found no Critical or Important issue and returned
ready. This review is engineering evidence, not owner acceptance or a Direction G5
artifact.

On September 23 the owner supplied a deterministic reproducer for a later-confirmed
race in `complete_deletions`: a broad final status update could mark a concurrently
created deletion job and contaminated backup complete/purged without removing that
backup. The reproducer passed on `09122dd` with the defect present. Commit `e3fd379`
replaces broad finalization with exact captured ids, rereads actual remaining work,
and keeps concurrent work retryable. The original defect assertions no longer hold;
the second cleanup now removes the concurrently contaminated package.

## stage2-assumptions

Trusted-local authority is established by an adapter outside model/request content,
then bound to the resolved space path, space id and execution epoch. This stage
implements and tests that seam but does not choose the future UI, OS identity or
interactive confirmation adapter.

SQLite `3.53.3` remains an exact build requirement for WAL-backed foundation spaces.
The ordinary managed Python runtime's SQLite `3.50.4` is intentionally refused; a
deployment must supply the already verified build or another owner-approved exact
distribution mechanism without weakening runtime admission.

The deletion claim covers files managed by this stage: the live Core database and
its WAL/SHM plus registered managed backup packages. SSD remapping, OS snapshots,
manually copied packages and external systems are outside the claim. Audit keeps
permitted identifiers and outcome references, but managed payload, payload digest,
provenance and replayable payload-derived receipt intent are removed.

## stage2-cuts

There is no Activity/Work runtime, Attempt, scheduler, dispatcher, outbox, memory,
Sleep, Pi integration, DBOS dependency, model router, CLI workflow, installer,
updater, migration from old `.zara`, remote publication, CI/CD, GitHub Action or push
notification. No real personal data, paid service or new external right was used.

DBOS remains an open technical question rather than a selected dependency. Its Stage
1 Windows closed-handle sanitation evidence is still inconclusive; this stage neither
silently narrows deletion semantics nor introduces a custom dispatcher.

## stage2-cost

The installed change is one independent Python package, one strict SQLite schema and
its tests/docs/probe. New workspace state exists only under a user-selected empty
directory. Test/probe outputs and the verified SQLite runtime remain in ignored local
scratch space. No external service cost was incurred.

## stage2-manual-acceptance

The owner explicitly authorized Stage 2 planning, implementation, local commits and
the foundation-only boundary. Automated checks and review establish the engineering
evidence above; they do not declare owner acceptance. The owner should decide whether
this Stage 2 foundation is accepted before authorizing any successor stage.

## stage2-recommendation

Keep `zaratustra.foundation` as the sole subject-state authority and preserve its
public module boundary. For a successor, first obtain a separate owner CALL that
defines the smallest Activity/Work contract consuming this foundation. Do not bind
that work to DBOS until the open Windows deletion/handle gate is resolved, and do not
add a replacement dispatcher merely to bypass that gate.

## stage2-next

solmax

END_OF_FILE: RESULT.md
