# Public onboarding R2 T3 engineering handback

## outcome

Public onboarding R2 T3 is implemented by commit
`01076ed9470202aef51f84d0561b76fa1b1c4382`, whose exact parent is the accepted
R2 T2 baseline `daaafe55653c38eaefe36e468858aa68a21429d0`. The report commit containing
this file has that implementation commit as its exact parent; its exact identity is
returned in the executor handback because a commit cannot truthfully name itself.

The installed `zara entry` surface now accepts ordinary prose for process creation,
shows readable persisted onboarding stages, preserves explicit manual research,
activates an exactly confirmed constructed Process, and resumes through a separately
authorized exact Process-state read showing current/no-current Work, Process material
metadata and Results.

Safe Process change remains on the existing preview/decision/Result authority. A
later ordinary Work uses one retained, non-authoritative exact plan, exact constructed
definition Pack binding, separate confirmation and the existing T2 Work-creation
event. Exact retries recover the receipt; changed or stale intent never rebases.

Implementation paths changed are `AGENTS.md`,
`docs/public-onboarding-r2-t3/PLAN.md`, `pyproject.toml`,
`src/zaratustra/AGENTS.md`, `src/zaratustra/cli/AGENTS.md`,
`src/zaratustra/cli/__init__.py`, `src/zaratustra/entry/__init__.py`,
`src/zaratustra/local/__init__.py`, `src/zaratustra/onboarding/AGENTS.md`,
`src/zaratustra/onboarding/__init__.py`,
`src/zaratustra/process_creation/__init__.py`,
`tests/tools/test_public_onboarding_r2_t3.py`,
`tests/zaratustra/onboarding/test_onboarding.py`,
`tests/zaratustra/process_change/test_process_change.py`, and
`tools/probe_public_onboarding_r2_t3.py`. This report additionally changes root
`RESULT.md`.

## evidence

- PLAN W21: ordinary strings flow through the existing `CreationDraft` and the
  installed capability catalog. Both a simple case and a three-outcome case reach
  activation; no product code authors domain content.
- PLAN W22: unfinished states render their persisted stage and next manual action.
  Activated resume uses an exact, separately authorized `ProcessStateQuery` by
  catalog rather than a historical Work selection.
- W26/W28/A05: Process material metadata is rendered only from verified Core reads.
  W27/W29-W32/A06-A09: no-current is normal; later Work is explicit, Pack-bound and
  confirmed; an exact retry returns the same receipt. Wrong binding, changed intent,
  missing confirmation and intervening Process material all refuse.
- Safe change binds the existing current-context, preview, decision and Result-effect
  APIs. The existing full safe-change probe retains reject/approve/apply/restart and
  exact-receipt evidence.
- Focused onboarding tests passed `7 passed in 3.12s`. Expanded creation/change/Work
  creation tests passed `41 passed in 31.35s`. Probe-wrapper and CLI safe-change
  binding tests passed `2 passed in 3.68s`.
- The exact post-commit reproduction
  `uv run --locked python -m tools.probe_public_onboarding_r2_t3 --output
  _scratch/public-onboarding-r2-t3-02` reported product 0.17.0, no provider contact,
  no user JSON requirement, simple and complex `current_work`, three complex
  outcomes, persisted manual research, one Process material after terminal,
  terminal `no_current_work`, exact later-Work replay, fresh later `current_work`,
  applied safe change and exact safe-change replay. Its simulated confirmations are
  explicitly not owner acceptance.
- Preliminary full `uv run --locked python -m tools.check` passed 132 formatted
  files, Ruff, strict mypy over 115 files, 18/18 import contracts,
  `395 passed in 166.01s`, and both 0.17.0 distributions.
- Exact implementation-commit
  `uv run --locked python -m tools.check --deliver` passed 132 formatted files,
  Ruff, strict mypy over 115 files, 18/18 contracts, `395 passed in 164.17s`, both
  0.17.0 distributions, and report structure.
- A bounded read-only setup evaluator confirmed the exact clean baseline, contract-36
  PROBA and no applicable STOP/STEER. A read-only review found one false definition
  binding risk; the implementation added the exact digest-derived Pack and
  process-type check plus a negative test, and the reviewer confirmed no remaining
  Critical or Important finding. Neither check is binding fresh Direction G5.
- Execution receipt: parent Codex task
  `01a09f18-b6a0-7ad2-92c4-176015521998`; executor task
  `01a09f6f-0b97-77b0-9724-0e2e1e6b013e`; worktree
  `C:/my_global_workflow/3e41/zaratustra`; branch
  `codex/public-onboarding-r2-t3`.

Done_when 1: PASS in product evidence. Ordinary prose creation, persisted manual
staging, exact confirmation, activation and readable authorized resume are covered.

Done_when 2: PASS in product evidence. No-current, verified Process material/Result
reads, exact Pack-bound later Work and existing safe Process change are covered with
positive, refusal, stale-state and replay evidence.

Done_when 3: implementation-commit full delivery evidence passes as above. The exact
report-commit full gate and clean status are returned in the executor handback;
separate fresh Direction review remains pending and is intentionally not claimed.

W18-W20, W24-W25 and A03 remain assigned to later T4-T7 work. Accepted
W01-W17/K01-K11 behavior is unchanged.

## assumptions

The constructed definition Pack identity remains exactly
`zaratustra.definition.<definition_sha256>`, with `process_type` equal to the
constructed definition id. The caller or collaborating agent authors proposal and
later-Work prose; the product validates and persists it but does not infer domain
semantics.

## cuts

The new prose command requires no user JSON, while the assistant-authored generic
definition and retained later-Work plan remain strict internal files. There is no
provider call, automatic research, product domain author, hidden Work, automatic
next Work, historical/reopened Work authority, second mutation authority,
lifecycle/planner replacement or Solmax Core.

No T4 assets, README/install/updater/release work, private or remote access, push,
Direction mutation, owner acceptance or Direction close is claimed.

## cost

The slice adds one onboarding coordinator, catalog Process-state query preparation,
installed CLI groups, a local exact decision reader, one import boundary, tests,
probe and plan. It adds no dependency or schema. Activation explicitly migrates only
the selected fresh target through released schema 9 so T1/T2 state and later-Work
authority exist.

The existing uv cache required approved local access. The file-scoped feedback gate
used its three allowed attempts (format, import order, then mypy findings) and is not
claimed green; each cause was corrected and direct Ruff/mypy plus both full gates
passed. The first full gate stopped on three missing helper return annotations; they
were corrected and the next full gate passed. No destructive reset/clean, push,
release, paid service or external workspace access occurred.

## manual-acceptance

pending. Automated evidence, setup smoke and review do not constitute owner
acceptance, integration, release or binding fresh Direction G5.

## next

solmax

END_OF_FILE: RESULT.md
