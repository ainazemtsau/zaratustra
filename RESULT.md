# T6 — два замечания G5 исправлены, полный gate ждёт повтора

## outcome

CALL c-solmax-zaratustra-m1-overview-20260910-exec, PROBA36, basis
c4b8aea4db38fea4c39a473c47d0d8b82d8552a0. Worktree
C:/projects/zaratustra/_scratch/m1-overview-20260910, branch
codex/m1-overview-20260910. STOP/STEER отсутствуют; validation.config
сверен: synced_contract_version 36, default_mode PROBA.

Свежая G5 по source 87db23ae вернула два замечания. Оба приняты полностью.

G5-T6-01: обязательный deliver падал на `ruff check`, порядок импортов в
tools/probe_overview.py. Лишний алиас убран, два импорта из одного модуля
заменены одним отсортированным.

G5-T6-02, содержательный дефект: `current_status` приходит от пакета свободным
текстом, где перенос строки разрешён, поэтому человекочитаемый вид получал
лишнюю строку, читаемую как настоящая строка обзора, вместе с поддельными
counts и заявлением, противоречащим envelope. Безобидный двухстрочный статус
давал то же самое. Исправлено в `overview_lines`: каждое прочитанное из
документа значение проходит через `_flat`, который заменяет непечатаемые
символы пробелом и схлопывает пробельные последовательности. Инвариант: один
возвращённый элемент — ровно одна печатаемая строка, поэтому текст пакета не
может добавить строку, счётчик или заявление. Отступы и подписи остаются
нашими константами.

Сам JSON-документ не менялся. Он по-прежнему дословно вкладывает ответ T3
вместе с точным значением статуса, включая переносы; байтовое равенство строки
собственному `read_capabilities` сохранено. Рецензент отдельно подтвердил, что
документ честен, и дефект относился только к виду.

Решение W19 и остальная конструкция не пересматривались: обзор остаётся
производным потребителем одного публичного контракта, строки независимы,
общей revision и межбазовой транзакции не обещается, новых поверхностей нет.

Это снова кандидат. Тесты, native gates и сценарии здесь не запускались.

## evidence

Разбор замечаний и границы правки: docs/m1-overview/FIX-G5-20260910.md.
Прежние документы без перезаписи: PLAN.md, CALL.md, baseline-ids.json,
REPRODUCE.md; прежний handoff CLAUDE-CHECK.md относится к source 87db23ae.
Новый узкий handoff: docs/m1-overview/CLAUDE-RECHECK.md.

Commits: 05398cd PLAN/CALL/baseline, 87db23a первый source,
a457a18 первый handoff, 4dad08eeaa9eaa2a3064947e0f1ffc2707476006 текущий source.

Свежая G5 на 87db23ae: docs/g5/m1-overview-20260910/G5-REPORT.md, commit
9d46e2e8b3af2924d19c17835c0e5647eedd8ff6, branch codex/g5-m1-overview-20260910.
Её verdict: deliver FAIL на lint; done_when 1 и 3 выполнены; done_when 2 не
выполнено; неизменность Core и обоих правил PASS, восемь идентификаторов
baseline совпали. Сообщённые ею mypy, 11 контрактов, 250 tests и сборка
относятся к прежнему source, шли после упавшего gate и не являются пройденным
deliver. Здесь эти результаты не подставляются вместо нового вывода.

Дельта 87db23a → 4dad08e: `_flat` и его применение в `overview_lines`, одна
строка импорта в probe и её вызовы, воспроизводимый контроль NOISY в
tools.probe_overview, новый тест
`test_pack_text_cannot_forge_a_row_a_count_or_an_envelope_claim`, строки в
src/zaratustra/process_packs/AGENTS.md. Контроль и тест используют один и тот
же статус с переносами, поддельной третьей строкой, поддельными counts,
противоречащим заявлением и ESC-последовательностью.

Неизменность по docs/m1-overview/baseline-ids.json сохраняется: tree
src/zaratustra/core a93fdbb567c69c14dc1c645bede50157d3c3fc00, tests/fixtures
c8b4594826173680343566049945cac14fad082b, fictional_lot
562949e5984875f50d58c22848a6f279e19821ef, fictional_signal
29708288ddaaadaba45184da8e04385847dca551; blobs capabilities.py 2579131,
capability_models.py f1b2c66, lifecycle.py f5c3c62, runner.py 5ed0d7d.
Это байтовая идентичность, а не семантический PASS.

Здесь выполнено только чтение исходников, Git и byte inspection и обычная
commit hygiene: `.githooks/pre-commit` прошёл на итоговом дереве, exit 0.
Полный `tools.check --deliver` и `tools.probe_overview` назначены свежему
Claude Code; повторная G5 — отдельная физическая сессия.

## assumptions

Распределение владельца «Реализация здесь, проверки в Claude Code»
сохраняется, поэтому исправление предъявляется непроверенным.

Нормализация вида означает, что человекочитаемый обзор не воспроизводит
форматирование пакета дословно. Авторитетный источник — документ; вид
остаётся производным и теперь не управляется текстом пакета.

Done_when 1 и 3 подтверждены прошлой G5 на 87db23a и могут быть
переиспользованы после сверки узкой дельты. Done_when 2 проверяется заново.

Приёмка T5 не является приёмкой T6. Предыдущая G5 не является G5 этой версии.

## cuts

Новых cuts нет. Не вводились: новый frontend, assistant, transport, MCP,
установленный загрузчик пакетов и CLI-команда обзора, третий процесс,
дополнительные поверхности, реальные процессы и данные, расписания и
автономность, product remote/push/merge, CI/CD/Actions/уведомления, новые
внешние или денежные права. Core и оба правила не менялись, предметных
исключений нет, гарантии P§30 и P§40 не ослаблены. Ни одна проверка не
ослаблена ради прохождения. T7 и M1 не закрываются, M0 остаётся partial,
Direction live/** не изменялся.

Известные пределы кандидата: обзор ограничен шестнадцатью явно выбранными
строками; между строками нет общей транзакции, поэтому соседняя workspace
может измениться между чтениями, что заявлено в envelope, а не скрыто;
порядок строк выбирает потребитель; длинный статус усекается по границе
знаков, поэтому вид не является полным текстом пакета; длинные наборы
процессов и любые нефиктивные данные не проверялись.

## cost

Одна правка вида в продукте, одна правка импорта в dev-инструменте, один
новый контроль и один новый тест. Dependencies, schema, package version 0.10.1
и установленный CLI не менялись. Токены и денежная стоимость не измерялись;
новых расходов нет.

## manual-acceptance

T6 pending. Прежний кандидат не прошёл обязательный gate и был отклонён по
done_when 2; новый ещё не проверен. Native gates, сценарий и binding fresh G5
здесь не выполнялись и не заменяются этим отчётом. Owner acceptance и Direction
close автором не объявляются. Actual owner acceptance
owner-ack:solmax-m1-second-process-accepted-20260910 относится только к T5.

## next

solmax

Свежий Claude Code: полный `tools.check --deliver`, затем
`tools.probe_overview`, затем узкая повторная G5 по
docs/m1-overview/CLAUDE-RECHECK.md. FAIL возвращает автору точный raw и
воспроизведение; PASS возвращается HOME вместе с показом самого обзора и
контрольного `overview-forged-status` владельцу. Только HOME продолжает к T7.

END_OF_FILE: RESULT.md
