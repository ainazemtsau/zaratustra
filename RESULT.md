# T6 — пять возвратов G5 закрыты, полный gate ждёт повтора

## outcome

CALL c-solmax-zaratustra-m1-overview-20260910-exec, PROBA36, basis
c4b8aea4db38fea4c39a473c47d0d8b82d8552a0. Worktree
C:/projects/zaratustra/_scratch/m1-overview-20260910, branch
codex/m1-overview-20260910. STOP/STEER отсутствуют; validation.config
сверен: synced_contract_version 36, default_mode PROBA.

Две свежие G5 вернули пять пунктов. Все приняты, ни одна проверка не ослаблена.

Первый круг, source 87db23ae. G5-T6-01: обязательный deliver падал на
`ruff check`, лишний алиас развёл два импорта одного модуля. G5-T6-02,
содержательный дефект: `current_status` приходит от пакета свободным текстом,
где перенос строки разрешён, поэтому человекочитаемый вид получал лишнюю
строку, читаемую как настоящая строка обзора, вместе с поддельными counts и
заявлением, противоречащим envelope. Исправлено в `overview_lines`: каждое
прочитанное из документа значение проходит через `_flat`, который заменяет
непечатаемые символы пробелом и схлопывает пробельные последовательности.
Инвариант: один возвращённый элемент — ровно одна печатаемая строка.

Второй круг, source 4dad08e. Подделка подтверждена закрытой: собственный probe
рецензента прогнал двадцать случаев, включая CR, CRLF, VT, FF, U+0085, U+2028,
U+2029, ESC, безобидный двухстрочный статус, границы усечения и статус из
девяноста девяти строк. Оставались три возврата, они исправлены здесь.

G5-T6-03: отказанная строка печатала `revision=` из эха собственного запроса,
хотя ничего не прочитала. Теперь строка без envelope печатает
`requested_revision=`, строка с envelope берёт `state_revision` из своего
ответа. У успешной строки Core требует равенства этих чисел, её вид не меняется.

G5-T6-04: проекция хранила только состояние и счётчик, поэтому conflict и
missing_pack в тексте не различались, а REPRODUCE.md обещал их наблюдать.
Проекция теперь зеркалит и `code` того же ответа, вид печатает `state:code`.
Словарь кодов задают Core и capabilities; свободного текста пакета в нём нет.

G5-T6-05: в baseline-ids.json учтены оба изменённых AGENTS.md.

JSON-документ по-прежнему дословно вкладывает ответ T3, включая точное значение
статуса с переносами; байтовое равенство строки собственному
`read_capabilities` сохранено. Менялись только производная проекция и вид.
Решение W19 не пересматривалось: обзор остаётся производным потребителем одного
публичного контракта, строки независимы, общей revision и межбазовой транзакции
не обещается, новых поверхностей нет.

Это снова кандидат. Тесты, native gates и сценарии здесь не запускались.

## evidence

Разбор всех пяти возвратов и границы правок: docs/m1-overview/FIX-G5-20260910.md.
Прежние документы без перезаписи: PLAN.md, CALL.md, CLAUDE-CHECK.md,
CLAUDE-RECHECK.md. Новый handoff: docs/m1-overview/CLAUDE-RECHECK-2.md.

Commits: 05398cd PLAN/CALL/baseline; 87db23a первый source; a457a18 первый
handoff; 4dad08e второй source; 6fdfbea и 8b07b7a второй handoff;
456776a1e346e28952a6da7813a629e4a079a2ba текущий source.

Первая G5 на 87db23ae: docs/g5/m1-overview-20260910/G5-REPORT.md, commit
9d46e2e8b3af2924d19c17835c0e5647eedd8ff6. Вторая G5 на 4dad08e:
docs/g5/m1-overview-recheck-20260910/G5-ADDENDUM.md, commit
31e6a47056ebc2056327910265bfbd1afe31111a. Вторая подтвердила закрытие подделки,
изолировала причину прямым сравнением прежнего и нынешнего рендерера на одном
документе, переиспользовала done_when 1 и 3 после сверки узкой дельты и оставила
три возврата открытыми.

Отмеченное ею расхождение одного случая из девятнадцати — краевой перенос
строки, снимаемый прежним ограничением `Text` контракта T3. Это поведение T3,
не регрессия T6, и здесь оно не исправляется.

Второй отчёт не назвал результат обязательного полного `tools.check --deliver`
на pin 4dad08e. Он не считается пройденным и назначен заново на новом pin.

Дельта 4dad08e → 456776a: поле `code` в `_summary`, подпись revision и коды в
`overview_lines`, дополнительные утверждения о stale строке в
tools.probe_overview, обновлённые и новый тест
`test_a_refused_row_names_its_code_and_only_the_requested_revision`, учёт в
baseline-ids.json. Ровно четыре файла.

Неизменность по docs/m1-overview/baseline-ids.json сохраняется: tree
src/zaratustra/core a93fdbb567c69c14dc1c645bede50157d3c3fc00, tests/fixtures
c8b4594826173680343566049945cac14fad082b, fictional_lot
562949e5984875f50d58c22848a6f279e19821ef, fictional_signal
29708288ddaaadaba45184da8e04385847dca551; blobs capabilities.py 2579131,
capability_models.py f1b2c66, lifecycle.py f5c3c62, runner.py 5ed0d7d.
Это байтовая идентичность, а не семантический PASS.

Здесь выполнено только чтение исходников, Git и byte inspection и обычная
commit hygiene: `.githooks/pre-commit` прошёл на итоговом дереве, exit 0.
Полный `tools.check --deliver` и `tools.probe_overview` назначены свежему
Claude Code; третья G5 — отдельная физическая сессия.

## assumptions

Распределение владельца «Реализация здесь, проверки в Claude Code»
сохраняется, поэтому исправления предъявляются непроверенными.

Расширение проекции полем `code` не раскрывает нового: код уже принадлежит
тому же ответу строки, вложенному дословно, и его словарь задают Core и
capabilities. Вид остаётся производным и не управляется текстом пакета.

Нормализация вида означает, что человекочитаемый обзор не воспроизводит
форматирование пакета дословно; авторитетный источник — документ.

Done_when 1 и 3 подтверждены двумя G5 на прежних pin и могут быть
переиспользованы после сверки узкой дельты. Done_when 2 и полный deliver
проверяются заново на новом pin.

Приёмка T5 не является приёмкой T6. Прежние G5 не являются G5 этой версии.

## cuts

Новых cuts нет. Не вводились: новый frontend, assistant, transport, MCP,
установленный загрузчик пакетов и CLI-команда обзора, третий процесс,
дополнительные поверхности, реальные процессы и данные, расписания и
автономность, product remote/push/merge, CI/CD/Actions/уведомления, новые
внешние или денежные права. Core и оба правила не менялись, предметных
исключений нет, гарантии P§30 и P§40 не ослаблены. Ни одна проверка не
ослаблена ради прохождения. T7 и M1 не закрываются, M0 остаётся partial,
Direction live/** не изменялся.

Известные пределы кандидата: обзор ограничен шестнадцатью явно выбранными
строками, и наборы у этого предела не проверялись; проверены две строки.
Между строками нет общей транзакции, поэтому соседняя workspace может
измениться между чтениями, что заявлено в envelope, а не скрыто. Порядок строк
выбирает потребитель. Длинный статус усекается по границе знаков, поэтому вид
не является полным текстом пакета. Краевой перенос строки в сохранённом
значении снимается ограничением `Text` контракта T3 и здесь не правится.

## cost

Три круга правок: вид, импорт, проекция и подписи, плюс контроль подделки,
утверждения о stale строке и три новых или обновлённых теста. Dependencies,
schema, package version 0.10.1 и установленный CLI не менялись. Токены и
денежная стоимость не измерялись; новых расходов нет.

## manual-acceptance

T6 pending. Первый кандидат отклонён по обязательному gate и по done_when 2;
второй закрыл подделку, но оставил три возврата и не получил названного
verdict по полному deliver. Native gates, сценарий и binding fresh G5 здесь
не выполнялись и не заменяются этим отчётом. Owner acceptance и Direction close
автором не объявляются. Actual owner acceptance
owner-ack:solmax-m1-second-process-accepted-20260910 относится только к T5.

## next

solmax

Свежий Claude Code: полный `tools.check --deliver` с явно названным
результатом, затем `tools.probe_overview`, затем третья G5 по
docs/m1-overview/CLAUDE-RECHECK-2.md. FAIL возвращает автору точный raw и
воспроизведение, перечисляя все возвраты явно. PASS возвращается HOME вместе с
показом владельцу обзора, обезвреженной подделки и честной подписи отказанной
строки. Только HOME продолжает к T7.

END_OF_FILE: RESULT.md
