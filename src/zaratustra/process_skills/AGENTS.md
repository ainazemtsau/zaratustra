# Managed Process skills

Reuse public journal revisions and Core audit; no separate canonical store.
Skill text has one versioned owner. Agent Skills Markdown is a derived view.
Bindings pin exact revisions per Process and use one versioned configuration.
No latest-following, automatic merge, dynamic code loading or new executors.
Readiness comes from actual registered commands/types and installed resource checks.
Do not infer semantic conflicts or truth from keywords. Scope links never grant access.
Common context compilation is host-independent; the Pi adapter owns only its session.
END_OF_FILE: src/zaratustra/process_skills/AGENTS.md
