# Process creation fixtures

These JSON definitions and their exact result values are invented development inputs
for the generic product constructor. They are not installed templates or user data.
The package may parse them through public product models; installed product code must
never import this fixture, and the fixture must not perform IO beyond reading its own
tracked JSON definitions.
END_OF_FILE: tests/fixtures/process_creation/AGENTS.md
