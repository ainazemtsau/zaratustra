# Fictional development fixtures
These examples exist only to exercise real public Core/pack APIs in development.
Never ship them in zaratustra or create example state during product installation.
Development tools may explicitly import them; product code cannot import tests/tools.
Keep rule behavior independent of trusted adapters and fixture storage paths.
END_OF_FILE: tests/fixtures/AGENTS.md
